/**
 * Retired managed-directory names accepted only by explicit one-way
 * migrations. Runtime code must not import this boundary.
 */

export const LEGACY_MANAGED_DIRECTORY_NAMES = Object.freeze([
  ".sdd-forge",
  ".senti",
  ".senrail",
]);
