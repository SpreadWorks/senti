services:
  app:
    build: .
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
  db:
    image: mysql:5.7
    environment:
      MYSQL_DATABASE: cakephp_app
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
