# Agent tests

This is the sole home for real agent CLI/provider evaluation, including quality
review code under `acceptance/`. It is opt-in (`npm run test:agent`) and may require
credentials. Use temporary roots and clean them even when provider execution fails.
