# E2E tests

Keep only representative public CLI entrypoint scenarios. Use isolated temporary
projects and invoke the public executable. Run `npm run test:e2e`; broad Flow and
fixture matrices belong in integration.
