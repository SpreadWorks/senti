{
  "R1": ["248-merge-checkout.test.js > runMerge — worktree squash route with baseBranch checkout (R1, R2, R3) > R1: squash merge creates commit on baseBranch even when main repo is on a different branch"],
  "R2": ["248-merge-checkout.test.js > runMerge — worktree squash route with baseBranch checkout (R1, R2, R3) > R2: squash merge succeeds when baseBranch is checked out in another worktree"],
  "R3": ["248-merge-checkout.test.js > runMerge — worktree squash route with baseBranch checkout (R1, R2, R3) > R3: temporary worktree is cleaned up after merge"],
  "R4": ["248-merge-checkout.test.js > rebaseOnto — dirty worktree detection (R4) > returns reason:'dirty' when worktree has unstaged changes", "248-merge-checkout.test.js > rebaseOnto — dirty worktree detection (R4) > returns reason:'conflict' when rebase has actual conflicts"],
  "R5": ["248-merge-checkout.test.js > runPreSync — dirty worktree error message (R5) > throws with 'uncommitted changes' message when worktree is dirty"]
}
