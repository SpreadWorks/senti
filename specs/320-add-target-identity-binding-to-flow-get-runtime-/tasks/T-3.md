# T-3: Preserve runtime-log read contracts

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Retain successful content, block-selection, error, and read-only semantics after target binding.

## Acceptance Criteria
- Raw and JSON content match the same pre-change block.
- Sequence and run-ID selectors retain parsing and conflict behavior.
- Missing logs remain structured failures.
- Success and error paths do not mutate product state or log content.

## Implementation Notes
Keep `RuntimeLogFile` and `RuntimeLogBlock` as the storage and metadata owners. Snapshot relevant files and metadata around reads.

## Test Strategy
Add spec-local focused runtime-log unit/integration tests with output parity, missing-log, no-mutation, and adjacent get regression assertions. Every file must begin with a `// spec: R<N> ...` header covering its requirements.

---
Status: pending | Parent: (root) | Added Round: 0
