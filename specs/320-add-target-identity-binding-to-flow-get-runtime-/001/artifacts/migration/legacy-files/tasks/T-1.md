# T-1: Declare runtime-log target options

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Expose the three expectation options and the explicit-target requirement through the runtime-log registry contract without changing adjacent get commands.

## Acceptance Criteria
- Registry parsing accepts each expectation and existing block options.
- Help documents target expectations and the at-least-one rule.
- Other get registry entries are unchanged.

## Implementation Notes
Use the shared target-option constants in `src/flow/registry.js`. Preserve raw success output and structured failure output.

## Test Strategy
Add spec-local registry/CLI parsing tests for single, combined, missing, invalid, and legacy options. Every spec-local test file must begin with a `// spec: R<N> ...` header covering its requirements.

---
Status: pending | Parent: (root) | Added Round: 0
