# T-3: Cover concurrent finalize

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add spec-local regression coverage that ties the metadata ownership invariant and cleanup warning behavior to the Issue #345 concurrent-flow scenario.

## Acceptance Criteria
- Tests include // spec headers covering R1 through R5.
- At least one test covers two active flows with one worktree-mode finalized target and one separate active flow in the main repository.
- Tests verify both warning presence for dirty other-flow flow.json and warning absence for the clean/single-flow case.
- Tests verify a staged other-flow flow.json is not included in the target finalize commit.
- Tests avoid network, gh, and external dependencies.

## Implementation Notes
Keep fixtures synthetic and repository-local. If full cleanup orchestration is too costly for a unit test, isolate the dirty-flow detection helper and add one integration-style fixture for command wiring.

## Test Strategy
Run the spec-local tests via the SDD test-execute phase and include them in normal Node test execution where practical.

---
Status: pending | Parent: (root) | Added Round: 0
