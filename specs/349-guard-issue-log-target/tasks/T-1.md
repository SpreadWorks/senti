# T-1: Resolve issue-log target

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Enable set issue-log to select the explicit target flow through the shared FlowCommand context boundary while preserving its existing entry behavior.

## Acceptance Criteria
- R1 and R2 hold for combined matching guards and each independent mismatch.
- R3 guard-free append behavior remains unchanged.
- R4 help and behavior-level test coverage are present.

## Implementation Notes
Prefer the existing FlowCommand constructor and registry target guard helpers. Do not add direct flow file lookup or new persistence code.

## Test Strategy
Extend tests/unit/flow/set-issue-log.test.js for command behavior and add spec-local coverage that creates two flows, asserts matching target append, asserts three mismatch no-write cases, and checks help parity.

---
Status: pending | Parent: (root) | Added Round: 0
