{
  "goal": "Prevent finalize-cleanup from dirtying the worktree it is about to remove, and make finalize-cleanup --force pass force removal through to dirty worktree teardown.",
  "scope": {
    "in": [
      "During `senti flow run finalize-cleanup`, all cleanup-time writes for flow state, metrics, notes, issue logs, plugin artifacts, and runtime-derived artifacts must target the main repository side or a durable cleanup location, not the worktree being removed.",
      "`senti flow run finalize-cleanup --force` must invoke force worktree removal for dirty external-state cases instead of retrying the same non-force removal path.",
      "Retained finalize-cleanup behavior must continue through the new write path: final flow state, finalize-cleanup step status, issue-log audit, embedded report, plugin hook result, runtime/metrics persistence, `.senti/last-finalized-spec`, active-flow clearing, worktree removal, and feature-branch deletion.",
      "Spec-local regression coverage must reproduce the cleanup-time flow.json metrics dirtying failure and the explicit dirty-worktree --force path."
    ],
    "out": [
      "Changing finalize-commit, finalize-sync, or merge strategy behavior outside what finalize-cleanup needs for read-only teardown.",
      "Redesigning the plugin lifecycle API. This spec only constrains finalize-cleanup write destinations for existing cleanup hooks.",
      "Repairing historical Spec #303 artifacts or rewriting repository history."
    ]
  },
  "constraints": [
    "Use only Node.js built-in modules and existing project helpers.",
    "Do not add project-specific values to src/; Issue #396 and prior spec references stay in spec/test artifacts.",
    "The target worktree read-only boundary starts after finalize-cleanup argument validation once the cleanup target worktree is resolved.",
    "No retained public finalize-cleanup behavior is intentionally removed; behavior is retained with cleanup-time write ownership moved away from the target worktree."
  ],
  "design_principles": [
    "Prefer existing FlowManager, FlowStore, FinalizeCleanupPathResolver, runtime log, plugin lifecycle, and git helper responsibilities over new parallel cleanup state mechanisms.",
    "Keep cleanup teardown transactional: final metadata is persisted before deleting the worktree, and recovery envelopes retain enough data for manual retry.",
    "Make force behavior explicit at the git worktree removal boundary so `--force` is observable in diff and tests."
  ],
  "overview": {
    "modules": [
      {
        "text": "`src/flow/lib/run-finalize-cleanup.js` owns finalize-cleanup teardown, metadata sync, plugin lifecycle handling, report attachment, worktree removal, and feature branch deletion."
      },
      {
        "text": "`src/lib/finalize-cleanup-paths.js` resolves cleanup-time paths so writes that would target the removable worktree can be relocated to the main repository or durable agent work directory."
      },
      {
        "text": "`src/lib/flow-store.js` persists flow.json metrics and notes; finalize-cleanup must route these mutations to the durable owner once the target worktree is read-only."
      },
      {
        "text": "`src/lib/dispatcher.js` and `src/lib/runtime-log.js` own command runtime log completion after `run-finalize-cleanup.js` returns; post-command cleanup metadata must use non-worktree durable storage."
      }
    ],
    "data_flow": [
      {
        "text": "Before teardown removes the worktree, finalize-cleanup resolves `worktreePath` and `mainRepoPath`, syncs retained metadata to main, performs final state updates on the durable owner, then removes the worktree and branch."
      },
      {
        "text": "Cleanup-time metrics, notes, issue-log entries, runtime log pointers, and plugin hook artifacts must flow to main/durable locations so `git worktree remove` sees no cleanup-created changes in the target worktree."
      },
      {
        "text": "With `--force`, worktree teardown must pass a force flag to the git worktree removal operation when dirty files or initialized submodule state remain due to external causes."
      },
      {
        "text": "Post-command dispatcher/agent runtimeLog metadata and metrics created after the final flow.json snapshot is committed must not reopen or mutate that final flow.json; they must be written to a target-worktree-external durable sidecar or runtime log store."
      }
    ],
    "decisions": [
      {
        "text": "[VERIFY] finalize-cleanup teardown already centralizes the affected behavior in `run-finalize-cleanup.js`; this spec changes write routing and force removal, not the public command surface.",
        "evidence": "Read `src/flow/lib/run-finalize-cleanup.js`: `runTeardown` handles metadata sync, plugin lifecycle, step status, git commit, last-finalized pointer, active-flow clear, worktree removal, branch deletion, validation, and report attachment.",
        "consideredAlternatives": "Splitting cleanup behavior across new commands was rejected because Issue #396 scopes a single finalize-cleanup concern."
      },
      {
        "text": "[VERIFY] existing path relocation support should be reused for cleanup-time durable writes.",
        "evidence": "Read `src/lib/finalize-cleanup-paths.js`: `FinalizeCleanupPathResolver` can identify paths inside a worktree and relocate them to the main repository or `.senti/agent-work`.",
        "consideredAlternatives": "Adding a second path relocation mechanism was rejected because it would duplicate existing cleanup-specific behavior."
      },
      {
        "text": "Migration parity inventory: retained surfaces are the finalize-cleanup command, final flow.json commit, step status, issue-log audit, metrics/notes/runtime logs, plugin hook artifacts, report envelope, last-finalized pointer, active-flow clear, worktree removal, and branch deletion.",
        "evidence": "Draft gate migration-parity review required inventory, owner mapping, and behavior-level verification for every retained public surface.",
        "consideredAlternatives": "Only testing flow.json dirtying was rejected because it would not prove retained finalize-cleanup behavior survives the relocation."
      },
      {
        "text": "Owner mapping: final flow state, step status, issue-log audit, metrics, notes, and report state belong to the main repo FlowManager/FlowStore or durable cleanup path after the read-only boundary.",
        "evidence": "Issue #396 says cleanup logs/metrics/recovery notes should be written outside the target worktree or completed before cleanup; main repo final state updates remain allowed.",
        "consideredAlternatives": "Continuing to write these surfaces inside the target worktree was rejected because it recreates the observed remove failure."
      },
      {
        "text": "Transaction boundary: the final flow.json snapshot is committed before teardown deletion; dispatcher/agent metrics or runtimeLog metadata produced after command return use a durable sidecar and are not part of that final commit.",
        "evidence": "Spec review identified dispatcher-owned runtime log completion and FlowStore agent metrics as post-command writes that otherwise could dirty main flow.json after the final commit or recreate the deleted worktree path.",
        "consideredAlternatives": "Appending post-command metadata to final flow.json after the cleanup commit was rejected because it leaves main dirty after finalize-cleanup."
      },
      {
        "text": "Owner mapping: plugin hook artifacts and follow-up data remain observable through the plugin lifecycle result and durable artifact path, not through new files created inside the target worktree during cleanup.",
        "evidence": "Issue #396 explicitly lists plugin artifacts among target worktree writes that must not occur after finalize-cleanup starts.",
        "consideredAlternatives": "Excluding plugins from read-only cleanup was rejected because plugin-created dirtiness would block the same teardown operation."
      },
      {
        "text": "Force cleanup applies to dirty root files and initialized submodule dirty state when the user explicitly passed `--force`.",
        "evidence": "Issue #396 asks to verify and fix `finalize-cleanup --force` so it performs the equivalent of `git worktree remove --force` for dirty external-factor cases.",
        "consideredAlternatives": "Keeping submodule dirty state as a hard halt under `--force` was rejected because it leaves a force cleanup retry with the same failure mode."
      }
    ]
  },
  "background": "Spec #303 observed finalize-cleanup failing because the cleanup command itself appended agent metrics to specs/<spec>/flow.json inside the worktree it was about to delete. That made the target worktree dirty and caused git worktree remove to fail. Issue #396 identifies the CLI command as the root cause and requires finalize-cleanup to treat the target worktree as read-only after cleanup starts, while still preserving final state updates on the main repository side. The same issue also requires `--force` to actually perform force worktree removal when external dirtiness remains.",
  "requirements": [
    {
      "id": "R1",
      "desc": "After finalize-cleanup resolves the target worktree, cleanup-time flow state, metrics, notes, issue-log, runtime-derived, and plugin artifact writes must not create or modify files under that target worktree.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R2",
      "desc": "Retained finalize-cleanup surfaces must still be persisted or reported through the main repository FlowManager/FlowStore before the final flow.json commit, or through a durable non-worktree sidecar/runtime log for dispatcher and agent metadata produced after that commit.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R6",
      "desc": "After the final flow.json snapshot is committed during finalize-cleanup, dispatcher-owned runtimeLog completion and agent metric writes must not mutate the committed flow.json; they must persist to target-worktree-external durable storage.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R3",
      "desc": "`senti flow run finalize-cleanup --force` must call git worktree removal with force semantics for dirty root files and initialized submodule dirty state caused by external factors.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R4",
      "desc": "Spec-local regression tests must cover cleanup-time metrics append relocation, retained public finalize-cleanup surfaces, normal worktree removal, feature branch deletion, and dirty worktree force removal.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R5",
      "desc": "Existing finalize-cleanup report warnings, plugin hook warning/follow-up data, and recovery envelopes must remain available to callers after write relocation.",
      "priority": "should",
      "status": "done"
    }
  ],
  "acceptance_criteria": [
    "A regression test fails against the previous behavior where cleanup-time metrics append mutates the target worktree's specs/<spec>/flow.json before removal.",
    "The same regression test passes after cleanup-time metrics/notes/runtime writes are routed to main/durable storage and the target worktree remains unmodified by cleanup-created writes.",
    "A behavior-level test verifies final flow state, finalize-cleanup step status, issue-log audit, embedded report data, plugin hook output, last-finalized pointer, active-flow clear, worktree removal, and feature branch deletion through the retained path.",
    "A behavior-level test verifies dispatcher/agent metadata produced after finalize-cleanup command return is stored outside the target worktree and does not leave the main repo flow.json dirty after the final commit.",
    "A dirty worktree force-removal test verifies `--force` passes force semantics to git worktree removal for dirty root files.",
    "A submodule dirty force-removal test or existing submodule robustness test verifies `--force` uses force removal for initialized submodule dirtiness instead of returning the non-force dirty halt."
  ],
  "clarifications": [
    {
      "q": "Does this spec remove any finalize-cleanup public behavior?",
      "a": "No. It relocates cleanup-time writes away from the target worktree and preserves existing observable command behavior through main/durable owners."
    },
    {
      "q": "When does target worktree read-only mode begin?",
      "a": "After argument validation, once finalize-cleanup has resolved the target worktree that may be deleted."
    },
    {
      "q": "What does `--force` mean for this spec?",
      "a": "`--force` remains the explicit destructive recovery choice and must also be reflected at the git worktree removal call when dirty external state is present."
    }
  ],
  "alternatives_considered": [
    {
      "option": "Only prevent the known metrics append and leave other cleanup-time write surfaces unchanged.",
      "reason": "Rejected because issue-log, notes, plugin artifacts, and runtime-derived writes can dirty the same target worktree and recreate the removal failure."
    },
    {
      "option": "Treat submodule dirty state as non-forceable even when `--force` is passed.",
      "reason": "Rejected because Issue #396 requires force cleanup to be equivalent to git worktree remove --force for abnormal dirty cases."
    },
    {
      "option": "Move cleanup responsibilities into a new command.",
      "reason": "Rejected because Issue #396 defines one finalize-cleanup concern and existing code already centralizes teardown in `run-finalize-cleanup.js`."
    }
  ],
  "open_questions": [],
  "keywords": [
    "finalize-cleanup",
    "worktree remove",
    "FlowStore",
    "FinalizeCleanupPathResolver",
    "agent metrics",
    "runtime log",
    "plugin lifecycle",
    "issue-log",
    "last-finalized-spec",
    "active flow",
    "submodule dirty",
    "force cleanup"
  ],
  "implementationTargets": [
    "src/flow/lib/run-finalize-cleanup.js",
    "src/lib/finalize-cleanup-paths.js",
    "src/lib/flow-store.js",
    "tests/unit/flow/finalize-cleanup-robustness.test.js",
    "specs/304-finalize-cleanup-readonly/tests/"
  ],
  "tasks": [
    {
      "id": "T-1",
      "title": "Relocate cleanup writes",
      "goal": "Route finalize-cleanup-time flow state, metrics, notes, issue-log, runtime-derived, and plugin artifact writes away from the target worktree after the read-only boundary.",
      "acceptance": [
        "Cleanup-time writes after target resolution use main repo or durable cleanup locations.",
        "The target worktree remains unchanged by cleanup-created flow.json metrics or plugin artifact writes before removal.",
        "Final flow state, step status, issue-log audit, report data, plugin hook result, pointer update, and active-flow clear remain observable.",
        "Post-command dispatcher/agent runtimeLog metadata and metrics do not mutate final flow.json after the cleanup commit."
      ],
      "implementation_notes": "Reuse FinalizeCleanupPathResolver and existing FlowManager/FlowStore routing. Keep source code generic and avoid references to Issue #396 in src/.",
      "test_strategy": "Spec-local tests should simulate cleanup-time metrics/plugin writes and post-command dispatcher/agent metadata, then assert target worktree flow.json does not change and main flow.json is not dirtied after the final commit.",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending"
    },
    {
      "id": "T-2",
      "title": "Apply force teardown",
      "goal": "Make `finalize-cleanup --force` pass force semantics to dirty worktree removal for root-file and initialized-submodule dirty states.",
      "acceptance": [
        "`--force` invokes git worktree removal with force semantics when dirty root files remain.",
        "`--force` invokes force removal for initialized submodule dirtiness rather than returning the non-force dirty halt.",
        "Non-force cleanup keeps the existing dirty-state halt and recovery envelope behavior."
      ],
      "implementation_notes": "Preserve orphan-drop audit behavior while extending force semantics to the worktree removal boundary.",
      "test_strategy": "Unit tests should cover removeWorktreeForCleanup or the command path with dirty root and submodule-like failure conditions.",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending"
    },
    {
      "id": "T-3",
      "title": "Add cleanup regressions",
      "goal": "Add spec-local and focused regression coverage proving read-only cleanup and migration parity for retained finalize-cleanup surfaces.",
      "acceptance": [
        "Spec-local tests include `// spec: R<N>` headers for every testable requirement.",
        "Coverage detects the previous flow.json metrics append dirtying failure.",
        "Coverage verifies retained public surfaces and dirty-worktree force removal."
      ],
      "implementation_notes": "Prefer existing finalize-cleanup robustness helpers and avoid broad project-wide test setup when focused fixtures are sufficient.",
      "test_strategy": "Run spec-local tests in scenario-validity and implementation test-execute, then rely on final-regression for full project coverage.",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending"
    }
  ],
  "user_approval": {
    "approved": true,
    "confirmed_at": "2026-06-17T08:27:45.458Z",
    "notes": "autoApprove: spec gate passed; approval prompt rendered and option [1] selected automatically."
  }
}
