# T-3: Add cleanup regressions

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add spec-local and focused regression coverage proving read-only cleanup and migration parity for retained finalize-cleanup surfaces.

## Acceptance Criteria
- Spec-local tests include `// spec: R<N>` headers for every testable requirement.
- Coverage detects the previous flow.json metrics append dirtying failure.
- Coverage verifies retained public surfaces and dirty-worktree force removal.

## Implementation Notes
Prefer existing finalize-cleanup robustness helpers and avoid broad project-wide test setup when focused fixtures are sufficient.

## Test Strategy
Run spec-local tests in scenario-validity and implementation test-execute, then rely on final-regression for full project coverage.

---
Status: pending | Parent: (root) | Added Round: 0
