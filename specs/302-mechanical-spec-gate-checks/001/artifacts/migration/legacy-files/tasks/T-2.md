# T-2: Add gate regressions

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add regression coverage proving the new mechanical failures and retained spec gate behavior work through the existing unit and command-level test surfaces.

## Acceptance Criteria
- R1-R6 regression cases exist in the recommended test targets.
- A command-level or equivalent test proves `--skip-guardrail` does not bypass spec mechanical issues.
- Targeted tests pass locally.

## Implementation Notes
Prefer existing helpers in `tests/unit/flow/gate-spec-sanity.test.js` and `tests/unit/specs/commands/gate.test.js`; avoid adding new test infrastructure.

## Test Strategy
Add spec-local `// spec: R6` coverage for retained behavior, then run `node --test specs/302-mechanical-spec-gate-checks/tests/gate-spec-precheck.test.js`, `node --test tests/unit/flow/gate-spec-sanity.test.js`, and `node --test tests/unit/specs/commands/gate.test.js`; run broader unit scope if touched helpers affect shared behavior.

---
Status: pending | Parent: (root) | Added Round: 0
