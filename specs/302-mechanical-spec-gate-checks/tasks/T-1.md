# T-1: Add spec prechecks

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add deterministic `checkSpecJson()` issues for missing requirement priority and missing or empty task test strategy while preserving schema and gate execution boundaries.

## Acceptance Criteria
- R1-R5 behavior is implemented in `src/flow/lib/run-gate.js`, including null-as-absence handling for migrated optional fields.
- Failure messages include field path, index, and id for each affected requirement or task.
- No schema or guardrail definition files are changed for this task.

## Implementation Notes
Keep checks near existing spec sanity checks in `checkSpecJson()`. Extract a helper only if repeated logic appears within the touched file.

## Test Strategy
Add spec-local tests in `specs/302-mechanical-spec-gate-checks/tests/gate-spec-precheck.test.js` with `// spec: R1`, `// spec: R2`, `// spec: R3`, `// spec: R4`, and `// spec: R5` headers, and mirror durable shared regression cases in `tests/unit/flow/gate-spec-sanity.test.js` or `tests/unit/specs/commands/gate.test.js` without invoking AI.

---
Status: pending | Parent: (root) | Added Round: 0
