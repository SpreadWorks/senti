# T-1: Enable guarded resolve-context dispatch

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Expose the standard target guard metadata for resolve-context and verify the public CLI contract without changing shared validation ownership.

## Acceptance Criteria
- R1 registry/help parity is implemented in `src/flow/registry.js` and covered by the dispatcher unit test.
- R2 matching and mismatch behavior is covered through CLI dispatch for run ID, Issue, and spec guards.
- R3 guard-free behavior remains covered and no duplicate validation or bypass is added.

## Implementation Notes
Add the existing `FLOW_TARGET_GUARD_OPTIONS` to the resolve-context registry entry and expand its help text consistently. Extend only the two named shared unit-test files; rely on the inherited `FlowCommand` target guard behavior.

## Test Strategy
Add `specs/316-accept-resolve-context-guards/tests/resolve-context-target-guards.test.js` as executable spec-local coverage with `// spec: R1` for registry/help parity, `// spec: R2` for matching and mismatching CLI guards, and `// spec: R3` for guard-free compatibility and shared-validation ownership. Extend ctx-dispatch parity coverage for R1 and resolve-context-extended CLI-dispatch coverage for R2-R3. Run the spec-local test, both targeted shared unit files, and the configured project regression stages.

---
Status: pending | Parent: (root) | Added Round: 0
