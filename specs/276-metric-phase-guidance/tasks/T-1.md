# T-1: Correct metric guidance

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Update SDD flow metric recording guidance so direct file-read examples use an accepted metric phase and no longer suggest `plan`.

## Acceptance Criteria
- Skill source guidance uses `draft` for planning/draft metric examples.
- Skill source guidance does not tell users to pass status-derived phase names such as `plan` or `finalize` to `flow set metric`.
- No guidance example presents `flow set metric plan docsRead`, `flow set metric plan srcRead`, `flow set metric finalize docsRead`, or `flow set metric finalize srcRead` as valid.

## Implementation Notes
Keep the CLI phase validator unchanged. If `src/skills/` changes, run `sdd-forge upgrade` so generated skill artifacts are refreshed.

## Test Strategy
Add a spec-local Node test with `// spec: R1 R2 R3` that checks guidance text for invalid status-derived metric phase examples and checks metric CLI behavior in an active flow context.

---
Status: pending | Parent: (root) | Added Round: 0
