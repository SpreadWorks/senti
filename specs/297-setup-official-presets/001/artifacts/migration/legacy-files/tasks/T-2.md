# T-2: Wire setup candidates

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Use the setup-safe official source discovery path in interactive setup candidate rendering, summary, final type minimization, and candidate-chain validation.

## Acceptance Criteria
- R1 official presets appear in fresh setup candidates without env setup.
- R5 candidate discovery does not silently return only `base` on official source failure.
- R7 existing core and installed plugin preset candidates remain present.

## Implementation Notes
Keep one candidate set for wizard display, summary rendering, final leaf type resolution, and validation so summary and final config cannot diverge.

## Test Strategy
Add or update spec-local setup candidate tests under `specs/297-setup-official-presets/` with `*** spec: R<N> ...` headers, covering fresh no-env discovery, env override discovery, source failure, and installed plugin candidate preservation.

---
Status: pending | Parent: (root) | Added Round: 0
