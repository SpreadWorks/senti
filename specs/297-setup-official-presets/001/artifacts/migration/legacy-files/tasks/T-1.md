# T-1: Resolve official source for setup

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add a setup-safe official source discovery path that can materialize or reuse the default official source before preset candidate rendering.

## Acceptance Criteria
- R1 and R2 source resolution behavior is available to setup candidate discovery.
- Official source failures are surfaced with cause information for R5.
- Tests can substitute the default official source descriptor/root while `SENTI_OFFICIAL_PRESETS_REPO` remains unset.
- No external dependencies are added.

## Implementation Notes
Prefer a helper in `src/lib/plugin-registry.js` so setup and upgrade share the official source definition. Preserve `SENTI_OFFICIAL_PRESETS_REPO` override semantics. The helper may accept an internal options object or test hook for substituting the default source descriptor/root; production callers use the existing `DEFAULT_OFFICIAL_PRESET_SOURCE`.

## Test Strategy
Spec-local tests under `specs/297-setup-official-presets/` should use fixture official repositories or controlled source roots through the resolver test-control path, keep `SENTI_OFFICIAL_PRESETS_REPO` unset for default-source cases, and include `*** spec: R<N> ...` headers.

---
Status: pending | Parent: (root) | Added Round: 0
