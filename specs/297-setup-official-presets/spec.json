{
  "goal": "`SENTI_OFFICIAL_PRESETS_REPO` が未設定の fresh project でも、`senti setup` の preset 選択で official presets を候補表示できるようにする。",
  "scope": {
    "in": [
      "`senti setup` の interactive preset candidate discovery。",
      "official preset source resolution の default remote 利用。",
      "official preset 選択後の `.senti/config.json` source/package persistence。",
      "base-only 選択時に official plugin state を保存しない挙動。",
      "setup/official preset 関連の regression tests。"
    ],
    "out": [
      "official preset repository の preset 内容変更。",
      "workflow plugin の board / hook / command behavior 変更。",
      "setup と無関係な plugin registry redesign。",
      "npm publish / dist-tag 操作。"
    ]
  },
  "constraints": [
    "外部依存を追加しない。Git clone/fetch と manifest 読み取りは Node.js built-in と既存 process helper を使う。",
    "`src/` に特定 project 固有の情報を入れない。official preset source は汎用の official plugin source として扱う。",
    "`SENTI_OFFICIAL_PRESETS_REPO` は local verification override として保持し、default remote より優先する。",
    "`config.local.json` の private plugin source/package を public `.senti/config.json` に漏らさない。",
    "base-only setup は official plugin source/package state を `.senti/config.json` に保存しない。",
    "official source 解決に失敗した場合、`base` だけを黙って表示する fallback は禁止する。",
    "default official source behavior は deterministic にテストできること。テストは `SENTI_OFFICIAL_PRESETS_REPO` を使わずに default source descriptor/root を差し替えられる内部 helper/options を使い、production default source は変更しない。",
    "新しい spec behavior coverage は `specs/297-setup-official-presets/` 配下に置き、各 spec-local test file に `*** spec: R<N> ...` header を付ける。",
    "plugin/preset/setup source を変更した場合は関連テストに加え、必要に応じて `senti upgrade` artifact を作成する。"
  ],
  "design_principles": [
    "setup と upgrade の official source resolution を同じ source-of-truth に寄せる。",
    "candidate discovery と persistence を分ける。候補表示に必要な manifest 取得は可能にし、保存は official preset 選択時だけ行う。",
    "silent fallback ではなく原因付き failure を返し、ユーザーが official presets が出ない理由を判別できるようにする。",
    "migration parity を public behavior 単位で検証する。候補表示だけでなく base-only persistence、official persistence、chain validation まで確認する。"
  ],
  "overview": {
    "modules": [
      {
        "text": "`src/setup.js`: interactive setup wizard。preset candidate display、summary、final type minimization、official package state persistence を呼び出す。"
      },
      {
        "text": "`src/lib/presets.js`: core presets、installed plugin presets、optional official preset manifest を候補として統合する。"
      },
      {
        "text": "`src/lib/plugin-registry.js`: plugin source/package install、default official preset source、official package persistence、config/local separation を扱う。"
      },
      {
        "text": "`src/lib/official-plugins.js`: `SENTI_OFFICIAL_PRESETS_REPO` override を解決する。"
      },
      {
        "text": "setup/official preset tests: fresh setup candidate discovery、base-only no-persistence、official preset persistence、failure behavior を検証する。"
      }
    ],
    "data_flow": [
      {
        "text": "setup candidate discovery は core presets と enabled plugin presets を読み込む。"
      },
      {
        "text": "official preset source は `SENTI_OFFICIAL_PRESETS_REPO` があれば local override を使い、無ければ default official source を materialize して manifest を読む。"
      },
      {
        "text": "candidate list は summary と final type minimization と candidate-chain validation に同じ candidate set として渡される。"
      },
      {
        "text": "base-only 選択時は official source/package state を保存せず、setup の通常 config だけを書く。"
      },
      {
        "text": "official preset 選択時は official source/package state を保存し、最終 config の plugin section に反映してから agent/template/validation 経路で参照できるようにする。"
      },
      {
        "text": "official source 解決に失敗した場合は原因付き failure を返し、`base` だけの候補表示へ黙って縮退しない。"
      }
    ],
    "decisions": [
      {
        "text": "[VERIFY] setup candidate display currently depends on env-provided official root.",
        "evidence": "`src/setup.js` の `officialPresetCandidateOptions()` は `officialPresetPluginRoot()` を呼び、root が truthy の場合だけ `includeOfficialPresets` を true にする。`runWizard()` はこの candidate list から treeItems を作る。"
      },
      {
        "text": "[VERIFY] official candidate manifest loading currently expects a filesystem root.",
        "evidence": "`src/lib/presets.js` の `listSetupPresetCandidates()` は `includeOfficialPresets` が true の場合に `PluginManifest.fromRoot(officialPresetRoot, \"official-presets\")` を呼ぶ。URL source resolution は行わない。"
      },
      {
        "text": "[VERIFY] plugin registry already owns the default official remote and install behavior.",
        "evidence": "`src/lib/plugin-registry.js` は `DEFAULT_OFFICIAL_PRESET_SOURCE` として `git@github.com:SpreadWorks/senti-presets.git` を定義し、`ensureOfficialPackage()` は `sourceRoot` が無い場合にその source を使える。"
      },
      {
        "text": "[VERIFY] setup persistence currently still depends on env-provided official root.",
        "evidence": "`src/setup.js` の write phase は `ensureSetupOfficialPresetState(workRoot, { selectedTypes, officialPresetRoot: officialPresetPluginRoot() })` を呼ぶ。`ensureSetupOfficialPresetState()` は official root が無いと early return する。"
      },
      {
        "text": "Migration inventory: setup must retain core preset display, enabled plugin preset display, env override display, base-only no-persistence, official preset persistence, and preset chain validation.",
        "evidence": "Draft gate migration parity required inventory. Issue #387 changes only missing default official source resolution; existing setup behavior around other candidate sources and persistence must remain."
      },
      {
        "text": "Migration mapping: official manifest resolution moves from env-only local-root discovery to a setup discovery helper backed by plugin-registry official source resolution.",
        "evidence": "Issue #387 identifies duplicated setup/upgrade official source resolution. Mapping the default remote to plugin-registry removes that divergence while keeping `SENTI_OFFICIAL_PRESETS_REPO` as override."
      },
      {
        "text": "Failure behavior: official source resolution failure is a cause-bearing setup failure, not warning plus silent base-only fallback.",
        "evidence": "QA q1 selected failure because silent fallback preserves the bug and hides why official presets are unavailable."
      },
      {
        "text": "Network behavior: setup may retrieve the default official source before candidate display when no local override/source is available.",
        "evidence": "QA q2 allows pre-display retrieval because manifest data is required before official presets can be listed."
      },
      {
        "text": "Testability decision: the setup-safe official source resolver provides deterministic test control for the default source descriptor/root without using `SENTI_OFFICIAL_PRESETS_REPO`.",
        "evidence": "Spec review found R1/R5 otherwise require real network access or cannot test default-source failure while the env override remains unset. Production keeps `DEFAULT_OFFICIAL_PRESET_SOURCE` unchanged."
      }
    ]
  },
  "background": "`senti setup` already has a default official preset remote in the plugin registry, but fresh interactive setup does not use it when building preset candidates. The setup path only includes official presets when `SENTI_OFFICIAL_PRESETS_REPO` points to a local repository. This leaves fresh projects with only `base` as a candidate even though the official source is known. The fix is to reuse official source resolution for setup candidate discovery while preserving existing persistence boundaries.",
  "requirements": [
    {
      "id": "R1",
      "desc": "Fresh setup candidate discovery includes official presets when `SENTI_OFFICIAL_PRESETS_REPO` is unset and the target project has no installed official-presets plugin, by resolving the default official preset source.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R2",
      "desc": "`SENTI_OFFICIAL_PRESETS_REPO` remains a local override for official preset discovery and takes precedence over the default remote.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R3",
      "desc": "Base-only setup does not write official-presets source/package state into `.senti/config.json` solely because candidates were discovered.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R4",
      "desc": "When an official non-base preset is selected, setup writes official-presets source/package state into `.senti/config.json`, installs or materializes the package as needed, and final preset chain validation resolves through that state.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R5",
      "desc": "Official source resolution failure during setup candidate discovery returns a cause-bearing failure and does not silently fall back to showing only `base`.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R6",
      "desc": "Setup does not copy private plugin source/package entries from `.senti/config.local.json` into public `.senti/config.json` while preparing official preset state.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R7",
      "desc": "Existing core preset and enabled plugin preset candidate behavior remains available after the official source discovery change.",
      "priority": "must",
      "status": "done"
    }
  ],
  "acceptance_criteria": [
    "R1: In a fresh temporary project with no official-presets plugin and no `SENTI_OFFICIAL_PRESETS_REPO`, setup candidate discovery returns `base` plus official presets from the default official source.",
    "R2: With `SENTI_OFFICIAL_PRESETS_REPO` pointing at a fixture official preset repository, setup candidate discovery uses that fixture and does not require the default remote.",
    "R3: A base-only setup path leaves `.senti/config.json` without official-presets source/package entries after candidate discovery.",
    "R4: Selecting an official preset writes official-presets source/package entries, installs/materializes `.senti/plugins/official-presets/plugin.json`, and validates the selected preset chain.",
    "R5: If the official default source cannot be resolved or materialized, setup candidate discovery fails with an error containing the official source cause and does not return a base-only candidate list.",
    "R5: Default-source success and failure tests keep `SENTI_OFFICIAL_PRESETS_REPO` unset and use deterministic test-controlled default source descriptors/roots instead of contacting the production remote.",
    "R6: When `.senti/config.local.json` contains private plugin sources/packages, selecting an official preset preserves those local entries only in local config and does not copy them into public `.senti/config.json`.",
    "R7: A project with an installed non-official plugin preset still sees that enabled plugin preset in setup candidates alongside core and official candidates."
  ],
  "clarifications": [
    {
      "q": "Does this change remove `SENTI_OFFICIAL_PRESETS_REPO`?",
      "a": "No. The environment variable remains a local override and test fixture path. It only stops being the only way for fresh setup to see official presets."
    },
    {
      "q": "Should setup persist official plugin state just because it fetched candidates?",
      "a": "No. Candidate discovery and persistence are separate. Base-only selection must not save official source/package state."
    },
    {
      "q": "What happens when the default official source cannot be reached?",
      "a": "Setup fails with the source-resolution cause instead of silently showing only `base`."
    },
    {
      "q": "Is the official preset repository content in scope?",
      "a": "No. The spec only changes how setup resolves and persists the official source."
    }
  ],
  "alternatives_considered": [
    {
      "option": "Require users to set `SENTI_OFFICIAL_PRESETS_REPO` before running setup",
      "reason": "Rejected because Issue #387 is specifically about fresh setup using the default official remote without manual environment setup."
    },
    {
      "option": "Keep silent base-only fallback when official source resolution fails",
      "reason": "Rejected because it preserves the bug symptom and hides why official presets are unavailable."
    },
    {
      "option": "Persist official source/package state during candidate discovery",
      "reason": "Rejected because base-only setup must not leave official plugin state. Persistence belongs only after an official preset is selected."
    },
    {
      "option": "Duplicate default remote logic in setup.js",
      "reason": "Rejected because setup and upgrade already diverged. The default source should be owned by plugin-registry source resolution."
    }
  ],
  "open_questions": [],
  "keywords": [
    "setup",
    "official-presets",
    "preset candidates",
    "SENTI_OFFICIAL_PRESETS_REPO",
    "DEFAULT_OFFICIAL_PRESET_SOURCE",
    "plugin-registry",
    "listSetupPresetCandidates",
    "ensureSetupOfficialPresetState",
    "PluginManifest",
    "config.local",
    "preset chain validation"
  ],
  "tasks": [
    {
      "id": "T-1",
      "title": "Resolve official source for setup",
      "goal": "Add a setup-safe official source discovery path that can materialize or reuse the default official source before preset candidate rendering.",
      "acceptance": [
        "R1 and R2 source resolution behavior is available to setup candidate discovery.",
        "Official source failures are surfaced with cause information for R5.",
        "Tests can substitute the default official source descriptor/root while `SENTI_OFFICIAL_PRESETS_REPO` remains unset.",
        "No external dependencies are added."
      ],
      "implementation_notes": "Prefer a helper in `src/lib/plugin-registry.js` so setup and upgrade share the official source definition. Preserve `SENTI_OFFICIAL_PRESETS_REPO` override semantics. The helper may accept an internal options object or test hook for substituting the default source descriptor/root; production callers use the existing `DEFAULT_OFFICIAL_PRESET_SOURCE`.",
      "test_strategy": "Spec-local tests under `specs/297-setup-official-presets/` should use fixture official repositories or controlled source roots through the resolver test-control path, keep `SENTI_OFFICIAL_PRESETS_REPO` unset for default-source cases, and include `*** spec: R<N> ...` headers.",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending"
    },
    {
      "id": "T-2",
      "title": "Wire setup candidates",
      "goal": "Use the setup-safe official source discovery path in interactive setup candidate rendering, summary, final type minimization, and candidate-chain validation.",
      "acceptance": [
        "R1 official presets appear in fresh setup candidates without env setup.",
        "R5 candidate discovery does not silently return only `base` on official source failure.",
        "R7 existing core and installed plugin preset candidates remain present."
      ],
      "implementation_notes": "Keep one candidate set for wizard display, summary rendering, final leaf type resolution, and validation so summary and final config cannot diverge.",
      "test_strategy": "Add or update spec-local setup candidate tests under `specs/297-setup-official-presets/` with `*** spec: R<N> ...` headers, covering fresh no-env discovery, env override discovery, source failure, and installed plugin candidate preservation.",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending"
    },
    {
      "id": "T-3",
      "title": "Preserve setup persistence boundaries",
      "goal": "Ensure candidate discovery does not persist official plugin state, while official preset selection saves public official state without leaking local private plugin config.",
      "acceptance": [
        "R3 base-only setup does not write official source/package state.",
        "R4 official preset selection writes official source/package state and validates the chain.",
        "R6 private local plugin entries remain only in `.senti/config.local.json`."
      ],
      "implementation_notes": "Reuse or adjust `ensureSetupOfficialPresetState()` so it can use the same resolved official source as candidate discovery while writing only public official state.",
      "test_strategy": "Add spec-local behavior-level tests under `specs/297-setup-official-presets/` with `*** spec: R<N> ...` headers for base-only no-persistence, official preset persistence, chain validation, and config.local separation.",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending"
    }
  ],
  "user_approval": {
    "approved": true,
    "confirmed_at": "2026-06-15T00:27:42.882Z",
    "notes": "Auto-approved because autoApprove is enabled by the user's prior selection."
  }
}
