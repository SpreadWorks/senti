# T-2: spec.json タスク定義スキーマの構造化

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
spec.json.tasks[*] を description 単一フィールドから構造化フィールド群 (goal 必須、acceptance / implementation_notes / test_strategy / parent を optional) に再設計する。REQ-2 / REQ-3 / REQ-8 を満たす。

## Acceptance Criteria
- src/flow/schemas/spec.schema.json の tasks[*].properties から description を削除
- tasks[*].required は [id, title, goal, origin, added_round, status]
- goal (string, 1-1000)、acceptance (array of string 各 500 文字)、implementation_notes (string, 5000)、test_strategy (string, 2000)、parent (string|null) を properties に追加
- 既存 326 spec.json (tasks undefined) が引き続き schema validation で valid
- unit test で新スキーマの required / optional / 型制約を検証

## Implementation Notes
- reference.md セクション 3.1 のスキーマ例を参照
- additionalProperties: false を維持。新フィールドを properties に明示的に定義
- parent は type: [string, null] または anyOf で string/null 許容

## Test Strategy
- unit: tests/unit/spec-schema-tasks-v2.test.js (required / optional / 型制約 / additionalProperties / 既存 tasks undefined 互換)
- integration: T-3 で render と組み合わせて smoke

---
Status: pending | Parent: (root) | Added Round: 0
