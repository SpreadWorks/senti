# T-4: Update skill templates and propagate to installed copies

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
src/skills/ 配下の skill テンプレートの旧 step 名を新名へ更新し、sdd-forge upgrade で installed skill copies へ反映する。

## Acceptance Criteria
- skill テンプレートの旧 step 名が新名
- upgrade 後 .claude/skills, .agents/skills が更新
- authored sources + installed copies の旧 step 名 grep が 0 件（対象外を除く）

## Implementation Notes
src/skills/sdd-forge.flow/SKILL.md の step-key examples / draft review routes table 等。upgrade 実行。

## Test Strategy
specs/269-unify-flow-step-names/tests/ に spec-local テストを新規作成（`// spec: R5 ...` ヘッダ）。authored sources（src/ コード/skill/prompts）と installed skill copies（.claude/skills, .agents/skills）に対する旧 step 名（rename 対象 10 種、対象外 legacy 名・issue-log 衝突名を除く）の grep が 0 件であることを検証する。

---
Status: pending | Parent: (root) | Added Round: 0
