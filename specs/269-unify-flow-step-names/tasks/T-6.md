# T-6: Implement historical-data migration tool under src/scripts

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
src/scripts/rename-phase-steps.js を実装する。1:1 の 7 種は値置換、flow.json 衝突名は構造判別、issue-log 衝突名は据え置き、report/retro/review はコードブロック内のみ、active flow 除外、dry-run default + --apply、git clean 前提、終了コード規約。

## Acceptance Criteria
- fixture で 1:1 値置換・flow.json 構造判別・issue-log 据え置き・active flow 除外を検証
- --apply 省略時は dry-run で diff のみ表示
- git not clean / 不正引数 / 書込みエラーで非0 終了、成功で 0

## Implementation Notes
Node 組み込みのみ。.active-flow を読み active spec を除外。JSON は step フィールド完全一致 + 構造位置で置換。markdown はコードブロック/パス文字列のみ。

## Test Strategy
specs/269-unify-flow-step-names/tests/ に spec-local テストを新規作成（`// spec: R7 ...` ヘッダ）。tmp fixture（flow.json の steps[]/tasks[].steps[]、issue-log.json、active flow マーカー）で 1:1 値置換・flow.json 構造判別・issue-log 衝突名据え置き・active flow 除外・dry-run/--apply・終了コード（成功 0 / git not clean・不正引数で非0）を検証する。

---
Status: pending | Parent: (root) | Added Round: 0
