# T-5: Document naming convention in src/AGENTS.md

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
`<phase>-<concern>-<action>` 命名規則（定義・例・phase 接頭辞必須）を src/AGENTS.md に明文化する。

## Acceptance Criteria
- src/AGENTS.md に命名規則セクションが存在

## Implementation Notes
flow アーキテクチャ記述付近に追記。

## Test Strategy
specs/269-unify-flow-step-names/tests/ に spec-local テストを新規作成（`// spec: R6 ...` ヘッダ）。src/AGENTS.md を読み、命名規則セクション（`<phase>-<concern>-<action>` の語と phase/concern/action の定義・例）が存在することを assertion する。

---
Status: pending | Parent: (root) | Added Round: 0
