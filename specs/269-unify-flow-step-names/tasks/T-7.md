# T-7: Apply migration to this repo's historical specs and commit

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
移行ツールを本リポジトリ specs/ に --apply で実行し（active flow 269 除外）、変換結果を 1 コミットで記録する。

## Acceptance Criteria
- specs/*/flow.json 等が新名へ変換
- active flow 269 の flow.json は未変換
- 変換が単一コミットで記録

## Implementation Notes
finalize-commit とは別の論理コミットとして移行データを分離（後から revert 可能に）。

## Test Strategy
specs/269-unify-flow-step-names/tests/ に spec-local テストを新規作成（`// spec: R8 ...` ヘッダ）。複数 spec を含む tmp fixture ツリー（active-flow マーカー付き）に対しツールを --apply で実行し、flow.json/report/retro/review が新名へ変換され、active flow の flow.json は未変換、issue-log 衝突名は据え置きであることを end-to-end で assertion する（live repo ではなく fixture で決定的に検証）。

---
Status: pending | Parent: (root) | Added Round: 0
