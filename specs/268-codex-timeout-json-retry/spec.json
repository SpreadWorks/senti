{
  "goal": "Fix codex provider {{text}} generation failures so timeout exits are retried by the existing retry policy, agent subprocess failures expose a 200-character stdout preview, and docs text JSON parse failures expose a 200-character normalized response preview.",
  "scope": {
    "in": [
      "Verify codex provider JSON output configuration in built-in profiles.",
      "Retry agent subprocess timeout exits under the existing retryCount policy.",
      "Include provider/profile context and stdout preview capped at 200 characters in relevant agent failure errors when stdout exists.",
      "Include normalized agent response preview capped at 200 characters in docs text batch JSON parse failure errors.",
      "Add regression tests covering timeout retry, codex JSON profile settings, and parse error preview."
    ],
    "out": [
      "Do not add external dependencies.",
      "Do not rename or remove docs build, docs text, or agent profile CLI interfaces.",
      "Do not add project-specific handling for OOS_spread_commerce paths or logs.",
      "Do not rewrite the provider architecture or introduce a new output normalization model.",
      "Do not publish to npm or change package release metadata."
    ]
  },
  "constraints": [
    "Use only Node.js built-in modules.",
    "Preserve backward-compatible CLI behavior: existing commands and options keep their names and meanings.",
    "Preserve docs text fail-fast behavior: a batch JSON parse failure still makes the affected command fail with a non-zero exit code.",
    "Do not change default timeout duration or config.agent.timeout semantics.",
    "Do not hardcode project-specific absolute paths or observed project names in src/.",
    "Retries must remain bounded by existing retryCount normalization and MAX_RETRY."
  ],
  "design_principles": [
    "Keep provider-specific parsing in src/lib/provider.js and shared subprocess behavior in src/lib/agent.js.",
    "Prefer explicit diagnostic snippets over silent fallback when provider output is malformed.",
    "Use small helper functions for repeated error preview formatting."
  ],
  "overview": {
    "modules": [
      {
        "text": "src/lib/agent.js owns external agent subprocess execution, timeout handling, retry policy, provider/profile resolution, and agent telemetry."
      },
      {
        "text": "src/lib/provider.js owns built-in claude/codex profile definitions and provider stdout parsing into text/usage."
      },
      {
        "text": "src/docs/commands/text.js owns {{text}} batch generation, batch JSON parsing, and failure propagation for docs text/docs build."
      }
    ],
    "data_flow": [
      {
        "text": "docs text builds a batch prompt, calls agent.call(commandId=docs.text), receives provider-normalized text, parses JSON, and applies generated text to directive locations."
      },
      {
        "text": "agent.call resolves the profile, builds CLI args, spawns the provider command, collects stdout/stderr, parses provider JSON output when jsonOutputFlag is set, and records telemetry."
      },
      {
        "text": "On timeout, the child exits by SIGTERM; the retry layer must treat that timeout error as retryable until retryCount is exhausted."
      }
    ],
    "decisions": [
      {
        "text": "[VERIFY] Codex JSON profile configuration matches the draft policy.",
        "evidence": "src/lib/provider.js CodexProvider builtinProfiles for codex/gpt-5.4 and codex/gpt-5.3 include args with --json and jsonOutputFlag='--json'. .sdd-forge/config.json also defines codex profiles with --json/jsonOutputFlag."
      },
      {
        "text": "[VERIFY] The no-retry timeout bug is source-verifiable.",
        "evidence": "src/lib/agent.js _callOnceWithRetry catches errors and currently throws immediately when err.killed or err.signal is set, bypassing retryCount."
      },
      {
        "text": "[VERIFY] docs text batch parse failure currently lacks thrown preview context.",
        "evidence": "src/docs/commands/text.js parseBatchJsonResponse(result) receives the normalized agent response returned by agent.call(); processTemplateFileBatch logs result.slice(0, 200) but throws Error('batch JSON parse failed for ...') without that response preview."
      },
      {
        "text": "Default timeout duration remains unchanged.",
        "evidence": "Draft QA q2 resolved that timeout duration policy is out of scope; the fix targets retry and diagnostics only.",
        "consideredAlternatives": "Increasing DEFAULT_AGENT_TIMEOUT_MS was rejected because it broadens runtime policy and can hide hangs."
      },
      {
        "text": "Provider output normalization redesign is out of scope.",
        "evidence": "Draft QA q4 resolved that existing CodexProvider parsing remains the contract for this bugfix.",
        "consideredAlternatives": "A broader normalization redesign was deferred to the related agent_json_output proposal named in the issue."
      }
    ]
  },
  "background": "Issue #346 reports docs build/docs text {{text}} generation hanging with codex provider, then failing after timeout without retry and with insufficient partial JSON parse diagnostics. The relevant code paths are the shared agent subprocess runner, codex provider profile/parser configuration, and docs text batch JSON parsing.",
  "requirements": [
    {
      "id": "R1",
      "desc": "Timeout exits identified by SIGTERM from agent subprocess timeout must be retried by _callOnceWithRetry until the normalized retryCount is exhausted; after exhaustion, the final timeout error must be thrown.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R2",
      "desc": "Built-in codex profiles used by ProviderRegistry must keep --json in args and jsonOutputFlag set to --json for codex/gpt-5.4 and codex/gpt-5.3.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R3",
      "desc": "Agent errors that include captured stdout must include provider key, profile key, and a stdout preview capped at 200 characters in the thrown error message.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R4",
      "desc": "docs text batch JSON parse failures must throw an error containing the file name and a preview of the normalized agent response capped at 200 characters.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R5",
      "desc": "docs text batch JSON parse failures must preserve fail-fast behavior: the affected file is counted as an error and the command exits non-zero through the existing DocsTextCommand error path.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R6",
      "desc": "Regression coverage must include spec-local tests for timeout retry, codex JSON profile settings, and docs text parse error preview without waiting for a real 300 second codex CLI timeout.",
      "priority": "must",
      "status": "done"
    }
  ],
  "acceptance_criteria": [
    "A controlled timeout/SIGTERM agent failure is retried according to retryCount and throws only after retries are exhausted.",
    "The final timeout/provider error message includes provider key, profile key, and no more than 200 characters of stdout preview when stdout exists.",
    "ProviderRegistry exposes codex/gpt-5.4 and codex/gpt-5.3 profiles with --json args and jsonOutputFlag='--json'.",
    "A malformed docs text batch response throws an error that includes the target file name and no more than 200 characters from the normalized agent response passed to parseBatchJsonResponse.",
    "docs text still reports parse-failed files through the existing command error path rather than silently skipping or applying fallback content.",
    "Spec-local tests under specs/268-codex-timeout-json-retry/tests/ cover R1-R6 with // spec: R<N> headers."
  ],
  "clarifications": [
    {
      "q": "Should this change increase the default timeout duration?",
      "a": "No. Timeout duration and config.agent.timeout semantics remain unchanged."
    },
    {
      "q": "Should this change introduce provider-wide output normalization?",
      "a": "No. Existing provider parse contracts remain in place; this spec only fixes retry and diagnostics around the current contracts."
    },
    {
      "q": "Should tests invoke a real codex CLI and wait for 300 seconds?",
      "a": "No. Tests must simulate or control the relevant failure path and run without a long external timeout."
    }
  ],
  "alternatives_considered": [
    {
      "option": "Increase DEFAULT_AGENT_TIMEOUT_MS or config defaults.",
      "reason": "Rejected because it broadens runtime policy, can hide hangs, and is not required to verify retry behavior."
    },
    {
      "option": "Silently fall back to raw text when batch JSON parsing fails.",
      "reason": "Rejected because docs text expects JSON keyed by directive id; applying malformed output risks corrupting generated documentation."
    },
    {
      "option": "Redesign provider output normalization in this spec.",
      "reason": "Rejected because CodexProvider already normalizes codex JSON events for the affected path, and the related normalization proposal is separate from this timeout/diagnostics bug."
    }
  ],
  "open_questions": [],
  "keywords": [
    "Agent",
    "CodexProvider",
    "ProviderRegistry",
    "docs text",
    "parseBatchJsonResponse",
    "processTemplateFileBatch",
    "retryCount",
    "SIGTERM timeout",
    "jsonOutputFlag",
    "stdout preview",
    "response preview",
    "docs build",
    "text directive"
  ],
  "implementationTargets": [
    "src/lib/agent.js",
    "src/lib/provider.js",
    "src/docs/commands/text.js",
    "specs/268-codex-timeout-json-retry/tests/"
  ],
  "tasks": [
    {
      "id": "T-1",
      "title": "Retry timeout exits",
      "goal": "Make agent subprocess timeout exits participate in the existing bounded retry policy and preserve final failure after retries are exhausted.",
      "acceptance": [
        "A timeout/SIGTERM error is retried until retryCount is exhausted.",
        "Non-timeout subprocess failures keep existing retry behavior.",
        "Default timeout duration is unchanged."
      ],
      "implementation_notes": "Modify src/lib/agent.js retry classification around err.killed/err.signal without changing profile resolution or timeout configuration.",
      "test_strategy": "Use a spec-local unit test seam or controlled agent helper to simulate timeout errors without sleeping for 300 seconds.",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending"
    },
    {
      "id": "T-2",
      "title": "Add agent diagnostics",
      "goal": "Add provider/profile context and a stdout preview capped at 200 characters to relevant agent failure errors.",
      "acceptance": [
        "Errors with stdout include provider key and profile key.",
        "stdout preview is capped at 200 characters.",
        "Errors without stdout do not fabricate a preview."
      ],
      "implementation_notes": "Keep formatting in src/lib/agent.js small and reusable; avoid logging secrets beyond already captured stdout and keep the preview cap fixed.",
      "test_strategy": "Spec-local tests assert error message content and preview length.",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending"
    },
    {
      "id": "T-3",
      "title": "Verify codex JSON profiles",
      "goal": "Lock the codex JSON profile contract with regression coverage for built-in codex profiles.",
      "acceptance": [
        "codex/gpt-5.4 has --json in args and jsonOutputFlag='--json'.",
        "codex/gpt-5.3 has --json in args and jsonOutputFlag='--json'."
      ],
      "implementation_notes": "Production code may not need changes if existing src/lib/provider.js already satisfies the contract; add tests to prevent regression.",
      "test_strategy": "Spec-local tests instantiate ProviderRegistry and inspect profile resolution results.",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending"
    },
    {
      "id": "T-4",
      "title": "Expose batch parse preview",
      "goal": "Make docs text batch JSON parse failures throw a file-specific error containing a response preview capped at 200 characters while preserving fail-fast behavior.",
      "acceptance": [
        "Malformed batch response errors include the file name.",
        "Malformed batch response errors include no more than 200 response characters.",
        "The existing DocsTextCommand non-zero failure path is preserved."
      ],
      "implementation_notes": "Modify src/docs/commands/text.js around parseBatchJsonResponse/processTemplateFileBatch error construction; do not add fallback content application.",
      "test_strategy": "Spec-local tests call exported docs text helpers with malformed responses or stubbed agents and assert thrown error content.",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending"
    }
  ],
  "user_approval": {
    "approved": true,
    "confirmed_at": "2026-05-29T01:08:02.493Z",
    "notes": "User approved spec without changes."
  }
}
