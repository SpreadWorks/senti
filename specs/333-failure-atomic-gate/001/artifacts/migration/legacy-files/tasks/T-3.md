# T-3: Reconcile repair fingerprint ledger

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Append one verified bridge from the preserved repair ledger tail to the current manifest so the ordinary impl-repair chain can resume.

## Acceptance Criteria
- R8 preserves repair-001 through repair-005 unchanged and adds one matching repair-006 ledger/delta pair.
- The reconciliation authority binds the active run, spec, Issue, expected hashes, changed paths, and delta digest.
- Identity, hash, path, or digest mismatch fixtures fail without changing ledger or delta bytes.

## Implementation Notes
Use a spec-local, one-time reconciliation authority and dedicated validation class. Reuse the existing impl-repair ledger and delta schemas for the appended bridge, retain all prior entries, and do not add or change a public flow command. This is the approved `code-placement` temporary-adapter case: exact run/spec/Issue authority is project-specific and cannot be placed in distributed `src/`, so the adapter composes existing typed owner classes without changing their schemas. Task review and gate receive the cumulative feature diff: gate-transition changes belong to completed T-1 (R1-R6), the resume-help line belongs to completed T-2 (R7), and T-3 owns only the spec-local reconciliation authority, repair-006 ledger/delta evidence, and R8 test.

## Test Strategy
Add a spec-local R8 test that snapshots the existing five entries, validates the applied bridge and delta, and exercises fail-closed copies with each authority field changed.

---
Status: pending | Parent: (root) | Added Round: 2
