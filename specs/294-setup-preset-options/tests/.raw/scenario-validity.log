[senti] scenario-validity tests start
command: node --test specs/294-setup-preset-options/tests/official-state.test.js specs/294-setup-preset-options/tests/preset-candidates.test.js specs/294-setup-preset-options/tests/setup-parity.test.js
[senti] scenario-validity file start command=node specs/294-setup-preset-options/tests/official-state.test.js
TAP version 13
# Subtest: setup official preset state
    # Subtest: R3: selected non-base official presets persist resolvable plugin state
    not ok 1 - R3: selected non-base official presets persist resolvable plugin state
      ---
      duration_ms: 20.368345
      type: 'test'
      location: '/home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/official-state.test.js:56:3'
      failureType: 'testCodeFailure'
      error: |-
        Expected values to be strictly equal:
        + actual - expected
        
        + 'undefined'
        - 'function'
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected: 'function'
      actual: 'undefined'
      operator: 'strictEqual'
      stack: |-
        loadPluginHelpers (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/official-state.test.js:12:10)
        async TestContext.<anonymous> (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/official-state.test.js:64:48)
        async Test.run (node:internal/test_runner/test:1054:7)
        async Promise.all (index 0)
        async Suite.run (node:internal/test_runner/test:1442:7)
        async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
      ...
    # Subtest: R4: base-only selections do not persist official preset plugin state
    not ok 2 - R4: base-only selections do not persist official preset plugin state
      ---
      duration_ms: 5.217627
      type: 'test'
      location: '/home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/official-state.test.js:83:3'
      failureType: 'testCodeFailure'
      error: |-
        Expected values to be strictly equal:
        + actual - expected
        
        + 'undefined'
        - 'function'
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected: 'function'
      actual: 'undefined'
      operator: 'strictEqual'
      stack: |-
        loadPluginHelpers (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/official-state.test.js:12:10)
        async TestContext.<anonymous> (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/official-state.test.js:91:48)
        async Test.run (node:internal/test_runner/test:1054:7)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: R8: official state writes do not publish config.local plugin entries
    not ok 3 - R8: official state writes do not publish config.local plugin entries
      ---
      duration_ms: 6.225712
      type: 'test'
      location: '/home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/official-state.test.js:103:3'
      failureType: 'testCodeFailure'
      error: |-
        Expected values to be strictly equal:
        + actual - expected
        
        + 'undefined'
        - 'function'
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected: 'function'
      actual: 'undefined'
      operator: 'strictEqual'
      stack: |-
        loadPluginHelpers (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/official-state.test.js:12:10)
        async TestContext.<anonymous> (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/official-state.test.js:117:48)
        async Test.run (node:internal/test_runner/test:1054:7)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    1..3
not ok 1 - setup official preset state
  ---
  duration_ms: 33.272291
  type: 'suite'
  location: '/home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/official-state.test.js:48:1'
  failureType: 'subtestsFailed'
  error: '3 subtests failed'
  code: 'ERR_TEST_FAILURE'
  ...
1..1
# tests 3
# suites 1
# pass 0
# fail 3
# cancelled 0
# skipped 0
# todo 0
# duration_ms 46.867081
Command failed: node specs/294-setup-preset-options/tests/official-state.test.js
exitCode: 1
[senti] scenario-validity file end command=node specs/294-setup-preset-options/tests/official-state.test.js
[senti] scenario-validity file start command=node specs/294-setup-preset-options/tests/preset-candidates.test.js
TAP version 13
# Subtest: setup preset candidate discovery
    # Subtest: R1: candidate provider includes core presets and installed plugin presets in deterministic order
    not ok 1 - R1: candidate provider includes core presets and installed plugin presets in deterministic order
      ---
      duration_ms: 21.527911
      type: 'test'
      location: '/home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/preset-candidates.test.js:75:3'
      failureType: 'testCodeFailure'
      error: |-
        Expected values to be strictly equal:
        + actual - expected
        
        + 'undefined'
        - 'function'
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected: 'function'
      actual: 'undefined'
      operator: 'strictEqual'
      stack: |-
        loadPresetHelpers (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/preset-candidates.test.js:12:10)
        async TestContext.<anonymous> (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/preset-candidates.test.js:86:43)
        async Test.run (node:internal/test_runner/test:1054:7)
        async Promise.all (index 0)
        async Suite.run (node:internal/test_runner/test:1442:7)
        async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
      ...
    # Subtest: R2: fresh official discovery is read-only before setup confirmation
    not ok 2 - R2: fresh official discovery is read-only before setup confirmation
      ---
      duration_ms: 4.277422
      type: 'test'
      location: '/home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/preset-candidates.test.js:90:3'
      failureType: 'testCodeFailure'
      error: |-
        Expected values to be strictly equal:
        + actual - expected
        
        + 'undefined'
        - 'function'
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected: 'function'
      actual: 'undefined'
      operator: 'strictEqual'
      stack: |-
        loadPresetHelpers (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/preset-candidates.test.js:12:10)
        async TestContext.<anonymous> (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/preset-candidates.test.js:98:43)
        async Test.run (node:internal/test_runner/test:1054:7)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: R2: unresolved official discovery fails loudly without writing project state
    not ok 3 - R2: unresolved official discovery fails loudly without writing project state
      ---
      duration_ms: 3.548298
      type: 'test'
      location: '/home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/preset-candidates.test.js:109:3'
      failureType: 'testCodeFailure'
      error: |-
        Expected values to be strictly equal:
        + actual - expected
        
        + 'undefined'
        - 'function'
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected: 'function'
      actual: 'undefined'
      operator: 'strictEqual'
      stack: |-
        loadPresetHelpers (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/preset-candidates.test.js:12:10)
        async TestContext.<anonymous> (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/preset-candidates.test.js:114:43)
        async Test.run (node:internal/test_runner/test:1054:7)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: R5: missing non-official installed manifests are ignored without source fetch or update
    not ok 4 - R5: missing non-official installed manifests are ignored without source fetch or update
      ---
      duration_ms: 3.89606
      type: 'test'
      location: '/home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/preset-candidates.test.js:126:3'
      failureType: 'testCodeFailure'
      error: |-
        Expected values to be strictly equal:
        + actual - expected
        
        + 'undefined'
        - 'function'
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected: 'function'
      actual: 'undefined'
      operator: 'strictEqual'
      stack: |-
        loadPresetHelpers (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/preset-candidates.test.js:12:10)
        async TestContext.<anonymous> (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/preset-candidates.test.js:133:43)
        async Test.run (node:internal/test_runner/test:1054:7)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: R9: candidate discovery enforces existing enabled package bounds
    not ok 5 - R9: candidate discovery enforces existing enabled package bounds
      ---
      duration_ms: 3.666419
      type: 'test'
      location: '/home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/preset-candidates.test.js:138:3'
      failureType: 'testCodeFailure'
      error: |-
        Expected values to be strictly equal:
        + actual - expected
        
        + 'undefined'
        - 'function'
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected: 'function'
      actual: 'undefined'
      operator: 'strictEqual'
      stack: |-
        loadPresetHelpers (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/preset-candidates.test.js:12:10)
        async TestContext.<anonymous> (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/preset-candidates.test.js:149:43)
        async Test.run (node:internal/test_runner/test:1054:7)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: R9: candidate discovery enforces plugin path safety bounds
    not ok 6 - R9: candidate discovery enforces plugin path safety bounds
      ---
      duration_ms: 3.94894
      type: 'test'
      location: '/home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/preset-candidates.test.js:156:3'
      failureType: 'testCodeFailure'
      error: |-
        Expected values to be strictly equal:
        + actual - expected
        
        + 'undefined'
        - 'function'
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected: 'function'
      actual: 'undefined'
      operator: 'strictEqual'
      stack: |-
        loadPresetHelpers (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/preset-candidates.test.js:12:10)
        async TestContext.<anonymous> (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/preset-candidates.test.js:171:43)
        async Test.run (node:internal/test_runner/test:1054:7)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: R9: candidate discovery preserves preset chain depth bounds
    not ok 7 - R9: candidate discovery preserves preset chain depth bounds
      ---
      duration_ms: 7.387038
      type: 'test'
      location: '/home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/preset-candidates.test.js:178:3'
      failureType: 'testCodeFailure'
      error: |-
        Expected values to be strictly equal:
        + actual - expected
        
        + 'undefined'
        - 'function'
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected: 'function'
      actual: 'undefined'
      operator: 'strictEqual'
      stack: |-
        loadPresetHelpers (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/preset-candidates.test.js:12:10)
        async TestContext.<anonymous> (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/preset-candidates.test.js:190:74)
        async Test.run (node:internal/test_runner/test:1054:7)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    1..7
not ok 1 - setup preset candidate discovery
  ---
  duration_ms: 49.707535
  type: 'suite'
  location: '/home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/preset-candidates.test.js:67:1'
  failureType: 'subtestsFailed'
  error: '7 subtests failed'
  code: 'ERR_TEST_FAILURE'
  ...
1..1
# tests 7
# suites 1
# pass 0
# fail 7
# cancelled 0
# skipped 0
# todo 0
# duration_ms 70.024
Command failed: node specs/294-setup-preset-options/tests/preset-candidates.test.js
exitCode: 1
[senti] scenario-validity file end command=node specs/294-setup-preset-options/tests/preset-candidates.test.js
[senti] scenario-validity file start command=node specs/294-setup-preset-options/tests/setup-parity.test.js
TAP version 13
# Subtest: setup retained behavior parity
    # Subtest: R6: summary and agent template lookup use project-root-aware preset resolution
    not ok 1 - R6: summary and agent template lookup use project-root-aware preset resolution
      ---
      duration_ms: 18.032493
      type: 'test'
      location: '/home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/setup-parity.test.js:113:3'
      failureType: 'testCodeFailure'
      error: |-
        Expected values to be strictly equal:
        + actual - expected
        
        + 'undefined'
        - 'function'
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected: 'function'
      actual: 'undefined'
      operator: 'strictEqual'
      stack: |-
        TestContext.<anonymous> (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/setup-parity.test.js:118:12)
        async Test.run (node:internal/test_runner/test:1054:7)
        async Promise.all (index 0)
        async Suite.run (node:internal/test_runner/test:1442:7)
        async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)
      ...
    # Subtest: R6: existing defaults preserve installed plugin preset selections
    not ok 2 - R6: existing defaults preserve installed plugin preset selections
      ---
      duration_ms: 5.634349
      type: 'test'
      location: '/home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/setup-parity.test.js:140:3'
      failureType: 'testCodeFailure'
      error: |-
        Expected values to be strictly equal:
        + actual - expected
        
        + 'undefined'
        - 'function'
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected: 'function'
      actual: 'undefined'
      operator: 'strictEqual'
      stack: |-
        TestContext.<anonymous> (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/setup-parity.test.js:145:12)
        async Test.run (node:internal/test_runner/test:1054:7)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: R6: non-interactive --type saves and validates an official preset
    not ok 3 - R6: non-interactive --type saves and validates an official preset
      ---
      duration_ms: 93.697922
      type: 'test'
      location: '/home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/setup-parity.test.js:151:3'
      failureType: 'testCodeFailure'
      error: |-
        
        Error: Preset not found: webapp
            at resolveChain (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/src/lib/presets.js:85:11)
            at file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/src/lib/presets.js:127:36
            at Array.map (<anonymous>)
            at resolveMultiChains (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/src/lib/presets.js:127:25)
            at resolveLeafTypes (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/src/setup.js:352:18)
            at Module.main (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/src/setup.js:486:21)
            at file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/src/senti.js:126:15
        
        
        1 !== 0
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected: 0
      actual: 1
      operator: 'strictEqual'
      stack: |-
        resolveChain (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/src/lib/presets.js:85:11)
        file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/src/lib/presets.js:127:36
        Array.map (<anonymous>)
        resolveMultiChains (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/src/lib/presets.js:127:25)
        resolveLeafTypes (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/src/setup.js:352:18)
        Module.main (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/src/setup.js:486:21)
        file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/src/senti.js:126:15
        TestContext.<anonymous> (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/setup-parity.test.js:183:12)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: R6: type minimization uses project-root-aware preset chains
    not ok 4 - R6: type minimization uses project-root-aware preset chains
      ---
      duration_ms: 7.638879
      type: 'test'
      location: '/home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/setup-parity.test.js:189:3'
      failureType: 'testCodeFailure'
      error: |-
        Expected values to be strictly equal:
        + actual - expected
        
        + 'undefined'
        - 'function'
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected: 'function'
      actual: 'undefined'
      operator: 'strictEqual'
      stack: |-
        TestContext.<anonymous> (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/setup-parity.test.js:194:12)
        async Test.run (node:internal/test_runner/test:1054:7)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: R7: setup no longer renders preset candidates from the core-only PRESETS constant
    not ok 5 - R7: setup no longer renders preset candidates from the core-only PRESETS constant
      ---
      duration_ms: 2.921575
      type: 'test'
      location: '/home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/setup-parity.test.js:201:3'
      failureType: 'testCodeFailure'
      error: |-
        The input was expected to not match the regular expression /buildTreeItems\(PRESETS\)/. Input:
        
        '#!/usr/bin/env node\n' +
          '/**\n' +
          ' * senti/setup/setup.js\n' +
          ' *\n' +
          ' * Interactive setup wizard.\n' +
          ' * Registers a project and generates .senti/config.json.\n' +
          ' *\n' +
          ' * Usage:\n' +
          ' *   senti setup\n' +
          ' *   senti setup --name myapp --path /path/to/src --type webapp/cakephp2\n' +
          ' */\n' +
          '\n' +
          'import fs from "fs";\n' +
          'import path from "path";\n' +
          'import readline from "readline";\n' +
          'import { parseArgs } from "./lib/cli.js";\n' +
          'import { EXIT_ERROR } from "./lib/constants.js";\n' +
          'import { validate } from "./lib/config.js";\n' +
          'import { DEFAULT_LANG, sentiDir as sentiDirFn } from "./lib/config.js";\n' +
          'import { createI18n } from "./lib/i18n.js";\n' +
          'import { PRESETS, resolveMultiChains, validatePresetChain } from "./lib/presets.js";\n' +
          'import { buildTreeItems, select } from "./lib/multi-select.js";\n' +
          'import { loadSpecDrivenDevelopmentTemplate } from "./lib/agents-md.js";\n' +
          'import { resolveWorkDir } from "./lib/config.js";\n' +
          'import { mergeAgentDefaults } from "./lib/agent-defaults.js";\n' +
          'import { deploySkills } from "./lib/skills.js";\n' +
          'import { SENTI_GITIGNORE_LINES, hasSentiGitignore, normalizeSentiGitignore } from "./lib/gitignore.js";\n' +
          '\n' +
          '// ---------------------------------------------------------------------------\n' +
          '// readline helpers\n' +
          '// ---------------------------------------------------------------------------\n' +
          '\n' +
          'function ask(prompt, prefill) {\n' +
          '  return new Promise((resolve) => {\n' +
          '    const rl = readline.createInterface({\n' +
          '      input: process.stdin,\n' +
          '      output: process.stdout,\n' +
          '    });\n' +
          '    rl.question(prompt, (answer) => {\n' +
          '      rl.close();\n' +
          '      resolve(answer.trim());\n' +
          '    });\n' +
          '    if (prefill) rl.write(prefill);\n' +
          '  });\n' +
          '}\n' +
          '\n' +
          '// ---------------------------------------------------------------------------\n' +
          '// CLI args\n' +
          '// ---------------------------------------------------------------------------\n' +
          '\n' +
          'function parseSetupArgs(argv) {\n' +
          '  return parseArgs(argv, {\n' +
          '    flags: ["--dry-run"],\n' +
          '    options: [\n' +
          '      "--name", "--path", "--work-root",\n' +
          '      "--type", "--purpose", "--tone",\n' +
          '      "--agent",\n' +
          '      "--lang",\n' +
          '    ],\n' +
          '    defaults: {\n' +
          '      name: "",\n' +
          '      path: "",\n' +
          '      workRoot: "",\n' +
          '      type: "",\n' +
          '      purpose: "",\n' +
          '      tone: "",\n' +
          '      agent: "",\n' +
          '      lang: "",\n' +
          '      dryRun: false,\n' +
          '    },\n' +
          '  });\n' +
          '}\n' +
          '\n' +
          '// ---------------------------------------------------------------------------\n' +
          '// Config file I/O\n' +
          '// ---------------------------------------------------------------------------\n' +
          '\n' +
          'function readConfigFile(configPath) {\n' +
          '  if (!fs.existsSync(configPath)) return null;\n' +
          '  try {\n' +
          '    return JSON.parse(fs.readFileSync(configPath, "utf8"));\n' +
          '  } catch (_) {\n' +
          '    return null;\n' +
          '  }\n' +
          '}\n' +
          '\n' +
          '// ---------------------------------------------------------------------------\n' +
          '// Load existing config as defaults\n' +
          '// ---------------------------------------------------------------------------\n' +
          '\n' +
          'function loadExistingDefaults(workRoot) {\n' +
          '  const configPath = path.join(sentiDirFn(workRoot), "config.json");\n' +
          '  const cfg = readConfigFile(configPath);\n' +
          '  if (!cfg) return null;\n' +
          '  const types = Array.isArray(cfg.type) ? cfg.type : cfg.type ? [cfg.type] : [];\n' +
          '  return {\n' +
          '    projectName: cfg.name || "",\n' +
          '    lang: cfg.lang || DEFAULT_LANG,\n' +
          '    type: types[0] || "",\n' +
          '    additionalTypes: types.slice(1),\n' +
          '    outputLangs: cfg.docs?.languages || [],\n' +
          '    outputDefault: cfg.docs?.defaultLanguage || "",\n' +
          '    purpose: cfg.docs?.style?.purpose || "",\n' +
          '    tone: cfg.docs?.style?.tone || "",\n' +
          '    agent: cfg.agent?.default || "",\n' +
          '  };\n' +
          '}\n' +
          '\n' +
          '// ---------------------------------------------------------------------------\n' +
          '// Project registration\n' +
          '// ---------------------------------------------------------------------------\n' +
          '\n' +
          'function ensureProjectDirs(workRoot) {\n' +
          '  const sentiDir = path.join(workRoot, ".senti");\n' +
          '  const outputDir = path.join(sentiDir, "output");\n' +
          '  const docsDir = path.join(workRoot, "docs");\n' +
          '  const specsDir = path.join(workRoot, "specs");\n' +
          '  [sentiDir, outputDir, docsDir, specsDir].forEach((d) =>\n' +
          '    fs.mkdirSync(d, { recursive: true }),\n' +
          '  );\n' +
          '  fs.writeFileSync(path.join(outputDir, ".gitkeep"), "");\n' +
          '}\n' +
          '\n' +
          'function ensureGitignore(workRoot) {\n' +
          '  const rootGitignore = path.join(workRoot, ".gitignore");\n' +
          '  const block = [\n' +
          '    ".tmp/",\n' +
          '    "",\n' +
          '    ...SENTI_GITIGNORE_LINES,\n' +
          '    "",\n' +
          '    ".agents/*",\n' +
          '    "!.agents/skills*",\n' +
          '    ".claude/*",\n' +
          '    "!.claude/skills*",\n' +
          '    ".codex",\n' +
          '  ];\n' +
          '  if (fs.existsSync(rootGitignore)) {\n' +
          '    const content = fs.readFileSync(rootGitignore, "utf8");\n' +
          '    const normalized = normalizeSentiGitignore(content, { appendIfMissing: false });\n' +
          '    if (normalized !== content) {\n' +
          '      fs.writeFileSync(rootGitignore, normalized, "utf8");\n' +
          '    }\n' +
          '    if (hasSentiGitignore(normalized)) return;\n' +
          '    const prefix = normalized.endsWith("\\n") || normalized === "" ? "" : "\\n";\n' +
          '    fs.appendFileSync(rootGitignore, `${prefix}${block.join("\\n")}\\n`);\n' +
          '  } else {\n' +
          '    fs.writeFileSync(rootGitignore, `${block.join("\\n")}\\n`);\n' +
          '  }\n' +
          '}\n' +
          '\n' +
          'function ensureGitattributes(workRoot) {\n' +
          '  const gitattributesPath = path.join(workRoot, ".gitattributes");\n' +
          '  const entry = ".senti/output/analysis.json merge=ours";\n' +
          '  if (fs.existsSync(gitattributesPath)) {\n' +
          '    const content = fs.readFileSync(gitattributesPath, "utf8");\n' +
          '    if (!content.includes(entry)) {\n' +
          '      fs.appendFileSync(gitattributesPath, `\\n${entry}\\n`);\n' +
          '    }\n' +
          '  } else {\n' +
          '    fs.writeFileSync(gitattributesPath, `${entry}\\n`);\n' +
          '  }\n' +
          '}\n' +
          '\n' +
          'function registerProject(projectName, sourcePath, workRootPath, t) {\n' +
          '  const resolved = path.resolve(sourcePath);\n' +
          '  if (!fs.existsSync(resolved)) {\n' +
          '    throw new Error(t("common.error.pathNotFound", { path: resolved }));\n' +
          '  }\n' +
          '\n' +
          '  const workRoot = workRootPath ? path.resolve(workRootPath) : resolved;\n' +
          '  ensureProjectDirs(workRoot);\n' +
          '  ensureGitignore(workRoot);\n' +
          '  ensureGitattributes(workRoot);\n' +
          '\n' +
          '  return { workRoot };\n' +
          '}\n' +
          '\n' +
          '// ---------------------------------------------------------------------------\n' +
          '// Agent config file (CLAUDE.md / AGENTS.md) setup\n' +
          '// ---------------------------------------------------------------------------\n' +
          '\n' +
          'function buildAgentContent(lang, options = {}) {\n' +
          '  const specDrivenDevelopmentContent = loadSpecDrivenDevelopmentTemplate(lang, options);\n' +
          '  const lines = [];\n' +
          `  lines.push('<!-- {{data("agents.senti")}} -->');\n` +
          '  if (specDrivenDevelopmentContent) lines.push(specDrivenDevelopmentContent.trimEnd());\n' +
          "  lines.push('<!-- {{/data}} -->');\n" +
          "  lines.push('');\n" +
          `  lines.push('<!-- {{data("agents.project")}} -->');\n` +
          "  lines.push('<!-- {{/data}} -->');\n" +
          '  return lines.join("\\n");\n' +
          '}\n' +
          '\n' +
          'const SENTI_DIRECTIVE_RE = /<!-- \\{\\{data\\("agents\\.senti"\\)\\}\\} -->[\\s\\S]*?<!-- \\{\\{\\/data\\}\\} -->/;\n' +
          '\n' +
          'function ensureAgentConfigFile(filePath, lang, t, options = {}) {\n' +
          '  const fileName = path.basename(filePath);\n' +
          '  const agentContent = buildAgentContent(lang, options);\n' +
          '\n' +
          '  if (!fs.existsSync(filePath)) {\n' +
          '    fs.writeFileSync(filePath, agentContent + "\\n", "utf8");\n' +
          '    console.log(t("setup.messages.agentFileCreated", { file: fileName }));\n' +
          '    return;\n' +
          '  }\n' +
          '\n' +
          '  const content = fs.readFileSync(filePath, "utf8");\n' +
          '\n' +
          '  if (SENTI_DIRECTIVE_RE.test(content)) {\n' +
          '    const sentiBlock = agentContent.match(SENTI_DIRECTIVE_RE)?.[0];\n' +
          '    if (sentiBlock) {\n' +
          '      const updated = content.replace(SENTI_DIRECTIVE_RE, sentiBlock);\n' +
          '      if (updated !== content) {\n' +
          '        fs.writeFileSync(filePath, updated, "utf8");\n' +
          '        console.log(t("setup.messages.agentFileUpdated", { file: fileName }));\n' +
          '      } else {\n' +
          '        console.log(t("setup.messages.agentFileUpToDate", { file: fileName }));\n' +
          '      }\n' +
          '    }\n' +
          '    return;\n' +
          '  }\n' +
          '\n' +
          '  const separator = content.endsWith("\\n") ? "\\n" : "\\n\\n";\n' +
          '  fs.writeFileSync(filePath, content + separator + agentContent + "\\n", "utf8");\n' +
          '  console.log(t("setup.messages.agentFileUpdated", { file: fileName }));\n' +
          '}\n' +
          '\n' +
          'function fixClaudeMdSymlink(sourceDir) {\n' +
          '  const claudePath = path.join(sourceDir, "CLAUDE.md");\n' +
          '  try {\n' +
          '    const stat = fs.lstatSync(claudePath);\n' +
          '    if (stat.isSymbolicLink()) {\n' +
          '      const content = fs.readFileSync(claudePath, "utf8");\n' +
          '      fs.unlinkSync(claudePath);\n' +
          '      fs.writeFileSync(claudePath, content, "utf8");\n' +
          '    }\n' +
          '  } catch (err) { if (err.code !== "ENOENT") console.error(err); }\n' +
          '}\n' +
          '\n' +
          '// ---------------------------------------------------------------------------\n' +
          '// Interactive wizard (returns settings object)\n' +
          '// ---------------------------------------------------------------------------\n' +
          '\n' +
          'async function runWizard(defaults, t) {\n' +
          '  const s = { ...defaults };\n' +
          '\n' +
          '  // --- Project name ---\n' +
          '  s.projectName = await ask(\n' +
          '    t("setup.questions.projectName", { default: s.projectName }),\n' +
          '    s.projectName,\n' +
          '  );\n' +
          '  if (!s.projectName) s.projectName = defaults.projectName;\n' +
          '\n' +
          '  // --- Output language ---\n' +
          '  const outputLangList = t.raw("setup.choices.outputLang");\n' +
          '  const outputLangItems = outputLangList.map((item) => ({\n' +
          '    key: item.key,\n' +
          '    label: item.label,\n' +
          '  }));\n' +
          '  console.log(`\\n${t("setup.questions.outputLang")}`);\n' +
          '  s.outputLangs = await select(outputLangItems, {\n' +
          '    mode: "multi",\n' +
          '    default: s.outputLangs,\n' +
          '  });\n' +
          '  if (s.outputLangs.length === 0) s.outputLangs = [s.lang];\n' +
          '\n' +
          '  if (s.outputLangs.length === 1) {\n' +
          '    s.outputDefault = s.outputLangs[0];\n' +
          '  } else {\n' +
          '    const defaultChoices = t.raw("setup.choices.outputDefault");\n' +
          '    const defaultItems = s.outputLangs.map((lang) => ({\n' +
          '      key: lang,\n' +
          '      label: defaultChoices[lang] || lang,\n' +
          '    }));\n' +
          '    console.log(`\\n${t("setup.questions.outputDefault")}`);\n' +
          '    s.outputDefault = await select(defaultItems, {\n' +
          '      mode: "single",\n' +
          '      default: s.outputDefault,\n' +
          '    });\n' +
          '  }\n' +
          '\n' +
          '  // --- Preset selection ---\n' +
          '  const treeItems = buildTreeItems(PRESETS);\n' +
          '  const presetDefaults = s.additionalTypes.length > 0\n' +
          '    ? [s.type, ...s.additionalTypes]\n' +
          '    : s.type ? [s.type] : [];\n' +
          '  console.log(`\\n${t("setup.questions.fwType")}`);\n' +
          '  const selectedPresets = await select(treeItems, {\n' +
          '    mode: "multi",\n' +
          '    autoSelectAncestors: true,\n' +
          '    default: presetDefaults,\n' +
          '  });\n' +
          '  if (selectedPresets.length === 0) {\n' +
          '    s.type = "base";\n' +
          '    s.additionalTypes = [];\n' +
          '  } else {\n' +
          '    s.type = selectedPresets[0];\n' +
          '    s.additionalTypes = selectedPresets.slice(1);\n' +
          '  }\n' +
          '\n' +
          '  // --- Document purpose ---\n' +
          '  const purposeChoices = t.raw("setup.choices.purpose");\n' +
          '  console.log(`\\n${t("setup.questions.purpose")}`);\n' +
          '  s.purpose = await '... 9667 more characters
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        #!/usr/bin/env node
        /**
         * senti/setup/setup.js
         *
         * Interactive setup wizard.
         * Registers a project and generates .senti/config.json.
         *
         * Usage:
         *   senti setup
         *   senti setup --name myapp --path /path/to/src --type webapp/cakephp2
         */
        
        import fs from "fs";
        import path from "path";
        import readline from "readline";
        import { parseArgs } from "./lib/cli.js";
        import { EXIT_ERROR } from "./lib/constants.js";
        import { validate } from "./lib/config.js";
        import { DEFAULT_LANG, sentiDir as sentiDirFn } from "./lib/config.js";
        import { createI18n } from "./lib/i18n.js";
        import { PRESETS, resolveMultiChains, validatePresetChain } from "./lib/presets.js";
        import { buildTreeItems, select } from "./lib/multi-select.js";
        import { loadSpecDrivenDevelopmentTemplate } from "./lib/agents-md.js";
        import { resolveWorkDir } from "./lib/config.js";
        import { mergeAgentDefaults } from "./lib/agent-defaults.js";
        import { deploySkills } from "./lib/skills.js";
        import { SENTI_GITIGNORE_LINES, hasSentiGitignore, normalizeSentiGitignore } from "./lib/gitignore.js";
        
        // ---------------------------------------------------------------------------
        // readline helpers
        // ---------------------------------------------------------------------------
        
        function ask(prompt, prefill) {
          return new Promise((resolve) => {
            const rl = readline.createInterface({
              input: process.stdin,
              output: process.stdout,
            });
            rl.question(prompt, (answer) => {
              rl.close();
              resolve(answer.trim());
            });
            if (prefill) rl.write(prefill);
          });
        }
        
        // ---------------------------------------------------------------------------
        // CLI args
        // ---------------------------------------------------------------------------
        
        function parseSetupArgs(argv) {
          return parseArgs(argv, {
            flags: ["--dry-run"],
            options: [
              "--name", "--path", "--work-root",
              "--type", "--purpose", "--tone",
              "--agent",
              "--lang",
            ],
            defaults: {
              name: "",
              path: "",
              workRoot: "",
              type: "",
              purpose: "",
              tone: "",
              agent: "",
              lang: "",
              dryRun: false,
            },
          });
        }
        
        // ---------------------------------------------------------------------------
        // Config file I/O
        // ---------------------------------------------------------------------------
        
        function readConfigFile(configPath) {
          if (!fs.existsSync(configPath)) return null;
          try {
            return JSON.parse(fs.readFileSync(configPath, "utf8"));
          } catch (_) {
            return null;
          }
        }
        
        // ---------------------------------------------------------------------------
        // Load existing config as defaults
        // ---------------------------------------------------------------------------
        
        function loadExistingDefaults(workRoot) {
          const configPath = path.join(sentiDirFn(workRoot), "config.json");
          const cfg = readConfigFile(configPath);
          if (!cfg) return null;
          const types = Array.isArray(cfg.type) ? cfg.type : cfg.type ? [cfg.type] : [];
          return {
            projectName: cfg.name || "",
            lang: cfg.lang || DEFAULT_LANG,
            type: types[0] || "",
            additionalTypes: types.slice(1),
            outputLangs: cfg.docs?.languages || [],
            outputDefault: cfg.docs?.defaultLanguage || "",
            purpose: cfg.docs?.style?.purpose || "",
            tone: cfg.docs?.style?.tone || "",
            agent: cfg.agent?.default || "",
          };
        }
        
        // ---------------------------------------------------------------------------
        // Project registration
        // ---------------------------------------------------------------------------
        
        function ensureProjectDirs(workRoot) {
          const sentiDir = path.join(workRoot, ".senti");
          const outputDir = path.join(sentiDir, "output");
          const docsDir = path.join(workRoot, "docs");
          const specsDir = path.join(workRoot, "specs");
          [sentiDir, outputDir, docsDir, specsDir].forEach((d) =>
            fs.mkdirSync(d, { recursive: true }),
          );
          fs.writeFileSync(path.join(outputDir, ".gitkeep"), "");
        }
        
        function ensureGitignore(workRoot) {
          const rootGitignore = path.join(workRoot, ".gitignore");
          const block = [
            ".tmp/",
            "",
            ...SENTI_GITIGNORE_LINES,
            "",
            ".agents/*",
            "!.agents/skills*",
            ".claude/*",
            "!.claude/skills*",
            ".codex",
          ];
          if (fs.existsSync(rootGitignore)) {
            const content = fs.readFileSync(rootGitignore, "utf8");
            const normalized = normalizeSentiGitignore(content, { appendIfMissing: false });
            if (normalized !== content) {
              fs.writeFileSync(rootGitignore, normalized, "utf8");
            }
            if (hasSentiGitignore(normalized)) return;
            const prefix = normalized.endsWith("\n") || normalized === "" ? "" : "\n";
            fs.appendFileSync(rootGitignore, `${prefix}${block.join("\n")}\n`);
          } else {
            fs.writeFileSync(rootGitignore, `${block.join("\n")}\n`);
          }
        }
        
        function ensureGitattributes(workRoot) {
          const gitattributesPath = path.join(workRoot, ".gitattributes");
          const entry = ".senti/output/analysis.json merge=ours";
          if (fs.existsSync(gitattributesPath)) {
            const content = fs.readFileSync(gitattributesPath, "utf8");
            if (!content.includes(entry)) {
              fs.appendFileSync(gitattributesPath, `\n${entry}\n`);
            }
          } else {
            fs.writeFileSync(gitattributesPath, `${entry}\n`);
          }
        }
        
        function registerProject(projectName, sourcePath, workRootPath, t) {
          const resolved = path.resolve(sourcePath);
          if (!fs.existsSync(resolved)) {
            throw new Error(t("common.error.pathNotFound", { path: resolved }));
          }
        
          const workRoot = workRootPath ? path.resolve(workRootPath) : resolved;
          ensureProjectDirs(workRoot);
          ensureGitignore(workRoot);
          ensureGitattributes(workRoot);
        
          return { workRoot };
        }
        
        // ---------------------------------------------------------------------------
        // Agent config file (CLAUDE.md / AGENTS.md) setup
        // ---------------------------------------------------------------------------
        
        function buildAgentContent(lang, options = {}) {
          const specDrivenDevelopmentContent = loadSpecDrivenDevelopmentTemplate(lang, options);
          const lines = [];
          lines.push('<!-- {{data("agents.senti")}} -->');
          if (specDrivenDevelopmentContent) lines.push(specDrivenDevelopmentContent.trimEnd());
          lines.push('<!-- {{/data}} -->');
          lines.push('');
          lines.push('<!-- {{data("agents.project")}} -->');
          lines.push('<!-- {{/data}} -->');
          return lines.join("\n");
        }
        
        const SENTI_DIRECTIVE_RE = /<!-- \{\{data\("agents\.senti"\)\}\} -->[\s\S]*?<!-- \{\{\/data\}\} -->/;
        
        function ensureAgentConfigFile(filePath, lang, t, options = {}) {
          const fileName = path.basename(filePath);
          const agentContent = buildAgentContent(lang, options);
        
          if (!fs.existsSync(filePath)) {
            fs.writeFileSync(filePath, agentContent + "\n", "utf8");
            console.log(t("setup.messages.agentFileCreated", { file: fileName }));
            return;
          }
        
          const content = fs.readFileSync(filePath, "utf8");
        
          if (SENTI_DIRECTIVE_RE.test(content)) {
            const sentiBlock = agentContent.match(SENTI_DIRECTIVE_RE)?.[0];
            if (sentiBlock) {
              const updated = content.replace(SENTI_DIRECTIVE_RE, sentiBlock);
              if (updated !== content) {
                fs.writeFileSync(filePath, updated, "utf8");
                console.log(t("setup.messages.agentFileUpdated", { file: fileName }));
              } else {
                console.log(t("setup.messages.agentFileUpToDate", { file: fileName }));
              }
            }
            return;
          }
        
          const separator = content.endsWith("\n") ? "\n" : "\n\n";
          fs.writeFileSync(filePath, content + separator + agentContent + "\n", "utf8");
          console.log(t("setup.messages.agentFileUpdated", { file: fileName }));
        }
        
        function fixClaudeMdSymlink(sourceDir) {
          const claudePath = path.join(sourceDir, "CLAUDE.md");
          try {
            const stat = fs.lstatSync(claudePath);
            if (stat.isSymbolicLink()) {
              const content = fs.readFileSync(claudePath, "utf8");
              fs.unlinkSync(claudePath);
              fs.writeFileSync(claudePath, content, "utf8");
            }
          } catch (err) { if (err.code !== "ENOENT") console.error(err); }
        }
        
        // ---------------------------------------------------------------------------
        // Interactive wizard (returns settings object)
        // ---------------------------------------------------------------------------
        
        async function runWizard(defaults, t) {
          const s = { ...defaults };
        
          // --- Project name ---
          s.projectName = await ask(
            t("setup.questions.projectName", { default: s.projectName }),
            s.projectName,
          );
          if (!s.projectName) s.projectName = defaults.projectName;
        
          // --- Output language ---
          const outputLangList = t.raw("setup.choices.outputLang");
          const outputLangItems = outputLangList.map((item) => ({
            key: item.key,
            label: item.label,
          }));
          console.log(`\n${t("setup.questions.outputLang")}`);
          s.outputLangs = await select(outputLangItems, {
            mode: "multi",
            default: s.outputLangs,
          });
          if (s.outputLangs.length === 0) s.outputLangs = [s.lang];
        
          if (s.outputLangs.length === 1) {
            s.outputDefault = s.outputLangs[0];
          } else {
            const defaultChoices = t.raw("setup.choices.outputDefault");
            const defaultItems = s.outputLangs.map((lang) => ({
              key: lang,
              label: defaultChoices[lang] || lang,
            }));
            console.log(`\n${t("setup.questions.outputDefault")}`);
            s.outputDefault = await select(defaultItems, {
              mode: "single",
              default: s.outputDefault,
            });
          }
        
          // --- Preset selection ---
          const treeItems = buildTreeItems(PRESETS);
          const presetDefaults = s.additionalTypes.length > 0
            ? [s.type, ...s.additionalTypes]
            : s.type ? [s.type] : [];
          console.log(`\n${t("setup.questions.fwType")}`);
          const selectedPresets = await select(treeItems, {
            mode: "multi",
            autoSelectAncestors: true,
            default: presetDefaults,
          });
          if (selectedPresets.length === 0) {
            s.type = "base";
            s.additionalTypes = [];
          } else {
            s.type = selectedPresets[0];
            s.additionalTypes = selectedPresets.slice(1);
          }
        
          // --- Document purpose ---
          const purposeChoices = t.raw("setup.choices.purpose");
          console.log(`\n${t("setup.questions.purpose")}`);
          s.purpose = await select([
            { key: "developer-guide", label: purposeChoices["developer-guide"] },
            { key: "user-guide", label: purposeChoices["user-guide"] },
            { key: "api-reference", label: purposeChoices["api-reference"] },
            { key: "__other__", label: purposeChoices.other },
          ], { mode: "single", default: s.purpose });
          if (s.purpose === "__other__") {
            const BUILTIN_PURPOSES = ["developer-guide", "user-guide", "api-reference", "__other__"];
            const prefill = BUILTIN_PURPOSES.includes(defaults.purpose) ? "" : defaults.purpose;
            s.purpose = await ask(t("setup.questions.purposeCustom"), prefill);
            if (!s.purpose) s.purpose = "developer-guide";
          }
        
          // --- Tone ---
          const toneChoices = t.raw("setup.choices.tone");
          console.log(`\n${t("setup.questions.tone")}`);
          s.tone = await select([
            { key: "polite", label: toneChoices.polite },
            { key: "formal", label: toneChoices.formal },
            { key: "casual", label: toneChoices.casual },
          ], { mode: "single", default: s.tone });
        
          // --- Agent ---
          const agentChoices = t.raw("setup.choices.agent");
          console.log(`\n${t("setup.questions.agent")}`);
          s.agent = await select([
            { key: "claude", label: agentChoices.claude },
            { key: "codex", label: agentChoices.codex },
          ], { mode: "single", default: s.agent });
        
          // --- Agent config file ---
          const agentFileName = s.agent === "claude" ? "CLAUDE.md" : "AGENTS.md";
          const agentsChoices = t.raw("setup.choices_agents");
          console.log(`\n${agentFileName}:`);
          s.agentFileMode = await select([
            { key: "generate", label: agentsChoices.rewrite },
            { key: "skip", label: agentsChoices.skip },
          ], { mode: "single", default: s.agentFileMode });
        
          return s;
        }
        
        // ---------------------------------------------------------------------------
        // Summary display
        // ---------------------------------------------------------------------------
        
        function resolveLeafTypes(primaryType, additionalTypes, projectRoot) {
          const allTypes = additionalTypes.length > 0
            ? [primaryType, ...additionalTypes] : [primaryType];
          const chains = resolveMultiChains(allTypes, projectRoot);
          return chains.map((chain) => chain[chain.length - 1].key);
        }
        
        function buildSummaryLines(s, t, projectRoot) {
          const leafTypes = resolveLeafTypes(s.type, s.additionalTypes, projectRoot);
          const agentFile = s.agent === "claude" ? "CLAUDE.md" : "AGENTS.md";
        
          return [
            `  ${t("setup.messages.summary")}`,
            `    project:    ${s.projectName}`,
            `    lang:       ${s.lang}`,
            `    output:     ${s.outputLangs.join(", ")} (default: ${s.outputDefault})`,
            `    type:       ${leafTypes.join(", ")}`,
            `    purpose:    ${s.purpose}`,
            `    tone:       ${s.tone}`,
            `    agent:      ${s.agent}`,
            `    ${agentFile}: ${s.agentFileMode === "generate" ? "✓" : "skip"}`,
          ];
        }
        
        // ---------------------------------------------------------------------------
        // Main
        // ---------------------------------------------------------------------------
        
        async function main() {
          const cli = parseSetupArgs(process.argv.slice(2));
        
          if (cli.help) {
            const tu = createI18n(cli.lang || DEFAULT_LANG);
            const h = tu.raw("help.cmdHelp.setup");
            const o = h.options;
            console.log([
              h.usage, "", `  ${h.desc}`, "", "Options:",
              `  ${o.name}`, `  ${o.path}`, `  ${o.workRoot}`, `  ${o.type}`,
              `  ${o.purpose}`, `  ${o.tone}`, `  ${o.agent}`,
              `  ${o.lang}`, `  ${o.dryRun}`, `  ${o.help}`,
            ].join("\n"));
            return;
          }
        
          const defaultPath = process.cwd();
          const defaultName = path.basename(defaultPath);
          const sourcePath = cli.path || defaultPath;
          const workRootPath = cli.workRoot || "";
        
          // Non-interactive mode: all required values provided via CLI
          const hasAllRequired = cli.name && cli.type && cli.purpose && cli.tone;
        
          let settings;
        
          if (hasAllRequired) {
            const operatingLang = cli.lang || DEFAULT_LANG;
            const types = cli.type.includes(",") ? cli.type.split(",") : [cli.type];
            settings = {
              projectName: cli.name || defaultName,
              lang: operatingLang,
              outputLangs: [operatingLang],
              outputDefault: operatingLang,
              type: types[0],
              additionalTypes: types.slice(1),
              purpose: cli.purpose,
              tone: cli.tone,
              agent: cli.agent || "",
              agentFileMode: "generate",
            };
          } else {
            // Load existing config as defaults
            const existing = loadExistingDefaults(defaultPath);
        
            let defaults = {
              projectName: cli.name || defaultName,
              lang: cli.lang || existing?.lang || DEFAULT_LANG,
              outputLangs: existing?.outputLangs || [],
              outputDefault: existing?.outputDefault || "",
              type: cli.type || existing?.type || "",
              additionalTypes: existing?.additionalTypes || [],
              purpose: cli.purpose || existing?.purpose || "",
              tone: cli.tone || existing?.tone || "",
              agent: cli.agent || existing?.agent || "",
              agentFileMode: "generate",
            };
        
            // --- Language selection (always in English first) ---
            let t = createI18n("en");
            console.log(`\n  ${t("setup.title")}`);
            console.log(`  ${t("setup.separator")}\n`);
        
            console.log(t("setup.questions.uiLang"));
            const langChoices = t.raw("setup.choices.uiLang");
            defaults.lang = await select([
              { key: "en", label: langChoices.en },
              { key: "ja", label: langChoices.ja },
            ], { mode: "single", default: defaults.lang });
        
            t = createI18n(defaults.lang);
        
            // --- Wizard loop with confirmation ---
            while (true) {
              settings = await runWizard(defaults, t);
        
              // Show summary
              const lines = buildSummaryLines(settings, t, defaultPath);
              console.log("");
              for (const line of lines) console.log(line);
        
              // Confirm
              const confirmChoices = t.raw("setup.choices.confirm");
              console.log(`\n${confirmChoices?.prompt || "Save this configuration?"}`);
              const confirmed = await select([
                { key: "yes", label: confirmChoices?.yes || "OK" },
                { key: "no", label: confirmChoices?.no || "Edit" },
              ], { mode: "single" });
        
              if (confirmed === "yes") break;
        
              // Use current settings as defaults for next round
              defaults = { ...settings };
              console.log("");
            }
          }
        
          // --- Write phase ---
          const { workRoot } = registerProject(
            settings.projectName, sourcePath, workRootPath,
            createI18n(settings.lang),
          );
          const t = createI18n(settings.lang);
        
          // Build config: merge wizard values into existing config to preserve customizations
          const configPath = path.join(workRoot, ".senti", "config.json");
          let config = readConfigFile(configPath) || {};
        
          // Minimize type array: remove parents that are already implied by children
          const leafTypes = resolveLeafTypes(settings.type, settings.additionalTypes, workRoot);
          const finalType = leafTypes.length === 1 ? leafTypes[0] : leafTypes;
        
          // Wizard-managed fields (overwrite)
          config.name = settings.projectName;
          config.lang = settings.lang;
          config.type = finalType;
          config.docs = {
            ...config.docs,
            languages: settings.outputLangs,
            defaultLanguage: settings.outputDefault,
            style: { ...config.docs?.style, purpose: settings.purpose, tone: settings.tone },
          };
          if (!config.flow) config.flow = { merge: "squash" };
        
          if (settings.agent) {
            // Normalize short names to namespaced provider keys
            const AGENT_PROVIDER_DEFAULT = { claude: "claude/sonnet", codex: "codex/gpt-5.4" };
            const resolvedDefault = AGENT_PROVIDER_DEFAULT[settings.agent] || settings.agent;
            const defaultAgent = { default: resolvedDefault, workDir: ".tmp" };
        
            if (config.agent) {
              // Preserve existing customizations, only update wizard-managed fields
              config.agent.default = resolvedDefault;
              if (!config.agent.workDir) config.agent.workDir = defaultAgent.workDir;
            } else {
              config.agent = defaultAgent;
            }
        
            // Seed default agent profiles + their referenced providers (add-only;
            // existing user values win). `useProfile` is intentionally left unset —
            // which profile to use is the user's choice.
            mergeAgentDefaults(config.agent);
          }
        
          validate(config);
        
          // Fail-fast: chapters ↔ templates static integrity check (spec 218).
          validatePresetChain(config.type, workRoot, {
            languages: config.docs?.languages || [],
            configChapters: config.chapters,
          });
        
          if (cli.dryRun) {
            console.log("[setup] DRY-RUN: config.json content:");
            console.log(JSON.stringify(config, null, 2));
            return;
          }
        
          // Write config.json
          fs.writeFileSync(configPath, JSON.stringify(config, null, 2) + "\n", "utf8");
          console.log(t("setup.messages.configGenerated", { path: configPath }));
        
          // Ensure the resolved agent work directory exists
          if (config.agent) {
            const workDir = resolveWorkDir(workRoot, config);
            fs.mkdirSync(workDir, { recursive: true });
          }
        
          // Agent config file
          if (settings.agentFileMode === "generate") {
            fixClaudeMdSymlink(workRoot);
            const agentConfigFile = settings.agent === "claude"
              ? path.join(workRoot, "CLAUDE.md")
              : path.join(workRoot, "AGENTS.md");
            ensureAgentConfigFile(agentConfigFile, settings.lang, t, {
              projectRoot: workRoot,
              presetTypes: config.type,
            });
          }
        
          // Skills
          try {
            deploySkills(workRoot);
            console.log(t("setup.messages.skillsDeployed"));
          } catch (e) {
            console.error(`skill deployment failed: ${e.message}`);
            process.exit(EXIT_ERROR);
          }
        
          // Final summary
          console.log(`\n  ${t("setup.messages.nextSteps")}`);
          console.log(`    ${t("setup.messages.step1")}`);
          console.log(`    ${t("setup.messages.step2")}`);
          console.log("");
        
          process.stdin.unref();
        }
        
        
        export { main };
        
      operator: 'doesNotMatch'
      stack: |-
        TestContext.<anonymous> (file:///home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/setup-parity.test.js:203:12)
        Test.runInAsyncScope (node:async_hooks:214:14)
        Test.run (node:internal/test_runner/test:1047:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:744:18)
        Test.postRun (node:internal/test_runner/test:1173:19)
        Test.run (node:internal/test_runner/test:1101:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:744:7)
      ...
    # Subtest: R10: spec-local tests declare requirement coverage headers
    ok 6 - R10: spec-local tests declare requirement coverage headers
      ---
      duration_ms: 0.844564
      type: 'test'
      ...
    1..6
not ok 1 - setup retained behavior parity
  ---
  duration_ms: 130.27203
  type: 'suite'
  location: '/home/nakano/workspace/senti/.senti/worktree/feature-294-setup-preset-options/specs/294-setup-preset-options/tests/setup-parity.test.js:105:1'
  failureType: 'subtestsFailed'
  error: '5 subtests failed'
  code: 'ERR_TEST_FAILURE'
  ...
1..1
# tests 6
# suites 1
# pass 1
# fail 5
# cancelled 0
# skipped 0
# todo 0
# duration_ms 146.265393
Command failed: node specs/294-setup-preset-options/tests/setup-parity.test.js
exitCode: 1
[senti] scenario-validity file end command=node specs/294-setup-preset-options/tests/setup-parity.test.js
exitCode: 1
[senti] requirement R1 observed: R1: candidate provider includes core presets and installed plugin presets in deterministic order
[senti] requirement R2 observed: R2: fresh official discovery is read-only before setup confirmation
[senti] requirement R3 observed: R3: selected non-base official presets persist resolvable plugin state
[senti] requirement R4 observed: R4: base-only selections do not persist official preset plugin state
[senti] requirement R5 observed: R5: missing non-official installed manifests are ignored without source fetch or update
[senti] requirement R6 observed: R6: summary and agent template lookup use project-root-aware preset resolution
[senti] requirement R7 observed: R7: setup no longer renders preset candidates from the core-only PRESETS constant
[senti] requirement R8 observed: R8: official state writes do not publish config.local plugin entries
[senti] requirement R9 observed: R9: candidate discovery enforces existing enabled package bounds
[senti] requirement R10 observed: R10: spec-local tests declare requirement coverage headers
[senti] scenario-validity tests end
