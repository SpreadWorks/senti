# T-5: Add spec-local coverage

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add spec-local tests that document the requirement coverage headers and exercise setup candidate, official state, and retained behavior contracts.

## Acceptance Criteria
- `specs/294-setup-preset-options/tests/preset-candidates.test.js` starts with `// spec: R1 R2 R5 R9`.
- `specs/294-setup-preset-options/tests/official-state.test.js` starts with `// spec: R3 R4 R8`.
- `specs/294-setup-preset-options/tests/setup-parity.test.js` starts with `// spec: R6 R7 R10`.

## Implementation Notes
Keep spec-local tests focused on public setup behavior and helper contracts; shared root tests may also be updated when the implementation touches existing command tests.

## Test Strategy
Run the spec-local tests directly and ensure their headers collectively cover R1 through R10.

---
Status: pending | Parent: (root) | Added Round: 0
