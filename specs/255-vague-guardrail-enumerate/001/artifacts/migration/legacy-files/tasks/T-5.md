# T-5: Rewrite the article-eval prompt builder

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Rename buildGuardrailPromptFromFiltered to buildGuardrailArticleEvalPrompt and export it. Update its rules and section ordering per R6.

## Acceptance Criteria
- src/flow/lib/run-gate.js exports buildGuardrailArticleEvalPrompt.
- Prompt rules section instructs (a) per-occurrence enumeration with required target+where+why_violates; (b) one-or-more entries for document-level guardrails (target = gap descriptor, where = artifact name); (c) diff-scope inheritance.
- Section order is rules → schema → fmt fallback → previously-passed (if any) → diff-scope (if any) → articles → content.
- buildGuardrailPromptFromFiltered name is removed (renamed).

## Implementation Notes
Update the call site in the gate command. Update GUARDRAIL_FMT_FALLBACK to match the article shape (T-6).

## Test Strategy
Pure-function test on buildGuardrailArticleEvalPrompt: assert the prompt text contains literal substrings for the enumeration directive and document-level guidance; assert section ordering by index of header strings in the rendered prompt.

---
Status: pending | Parent: (root) | Added Round: 0
