# T-10c: Migrate existing parser / schema / prompt tests to new exports

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Update all tests that reference the removed names to use the new exports. Affected files: tests/unit/specs/commands/guardrail.test.js, tests/unit/specs/commands/guardrail-metadata.test.js, tests/unit/flow/gate-evaluation-schema.test.js, tests/unit/flow/gate-pass-history-prompt.test.js. Migrate parser imports, schema imports, prompt builder name, and update assertions accordingly.

## Acceptance Criteria
- None of the four files references parseEvaluationResponse, GUARDRAIL_EVAL_SCHEMA, or buildGuardrailPromptFromFiltered.
- Article-path test cases assert violations[] presence on FAIL and the parser-derived reason summary.
- Requirement-path test cases retain the single-reason contract.
- gate-pass-history-prompt.test.js assertions match the new section ordering (rules → fmt fallback → previously-passed → diff-scope → articles → content) and the renamed prompt builder.
- All four test files pass under `npm test`.

## Implementation Notes
Identify per file: which test cases exercise article-style vs requirement-style. guardrail-metadata.test.js likely tests metadata fields on guardrails — minor adjustments only. gate-pass-history-prompt.test.js tests prompt text, so update both the import name and the asserted prompt text/order.

## Test Strategy
Run npm test on the affected test files; assert pass.

---
Status: pending | Parent: (root) | Added Round: 0
