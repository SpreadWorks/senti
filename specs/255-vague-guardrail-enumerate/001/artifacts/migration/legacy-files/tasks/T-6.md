# T-6: Update fmt fallback for the article path

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Update GUARDRAIL_FMT_FALLBACK to describe the article shape (violations on FAIL, reason on PASS/SKIP). Add a separate fmt fallback aligned with IMPL_REQUIREMENT_EVAL_SCHEMA used by buildImplCheckPrompt.

## Acceptance Criteria
- GUARDRAIL_FMT_FALLBACK text matches GUARDRAIL_ARTICLE_EVAL_SCHEMA.
- buildImplCheckPrompt uses a separate fmt fallback constant aligned with IMPL_REQUIREMENT_EVAL_SCHEMA.

## Implementation Notes
Define a new constant (e.g. IMPL_REQUIREMENT_FMT_FALLBACK) for the requirement path; keep the article-path constant under the existing GUARDRAIL_FMT_FALLBACK name (or rename to GUARDRAIL_ARTICLE_FMT_FALLBACK consistently).

## Test Strategy
Pure-function test asserting each fmt fallback string is referenced from the corresponding prompt builder.

---
Status: pending | Parent: (root) | Added Round: 0
