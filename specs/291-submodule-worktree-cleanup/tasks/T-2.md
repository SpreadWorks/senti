# T-2: Cover cleanup parity

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add spec-local and shared regression coverage for the submodule cleanup behavior and retained public cleanup surfaces.

## Acceptance Criteria
- Spec-local tests cover R1 through R9 with `// spec: R<N>` headers.
- Shared finalize-cleanup robustness tests cover clean submodule retry, dirty submodule halt, status failure halt, force retry failure halt, and non-submodule failure retention.
- Migration parity checks cover retained teardown side effects.

## Implementation Notes
Prefer focused tests around `runTeardown` and helper behavior. Avoid network-dependent submodule remotes; use local repositories when real submodules are needed.

## Test Strategy
Run the spec-local tests, the finalize-cleanup unit test file, and the unit test scope.

---
Status: pending | Parent: (root) | Added Round: 0
