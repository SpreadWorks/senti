# T-3: Verify rewind recovery route

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Provide behavior-level evidence that the new flow route renews planning and implementation verification without changing source or task history.

## Acceptance Criteria
- R1-R8 have spec-local `// spec: R<N>` coverage and the shared task-level reopen suite passes.
- AC1-AC10 pass, including the impl-gate to renewed verification end-to-end route and all immutable failure snapshots.

## Implementation Notes
Build fixtures from the current flow definition and use shared snapshot helpers when the same source/artifact/state comparison appears twice.

## Test Strategy
Run spec-local tests, affected unit suites, the plan-rewind E2E suite, configured targeted regression, and final full project regression through the flow.

---
Status: pending | Parent: (root) | Added Round: 0
