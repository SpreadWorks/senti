# T-1: Model atomic plan rewind

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Define class-based rewind values and make FlowStore apply the complete validated parent flow transition as one save.

## Acceptance Criteria
- R2, R3, R5, and R6 pass unit coverage for state reset, retry accounting, preservation, the 500-file and 268435456-byte evidence caps, and the 100-record append-only audit cap.
- Candidate validation proves exactly one active leaf and leaves flow.json unchanged on every thrown invariant.

## Implementation Notes
Add the plan-rewind domain module and one public FlowStore method. Reuse step-tree traversal and reset-aware metric conventions; do not issue repeated store setters.

## Test Strategy
Use table-driven FlowStore/domain fixtures for every supported source, step reset set, retry phase, audit field, the 500-file, 268435456-byte, 65536-byte-chunk, 1000-character-path, 500-character-reason, and 100-record boundaries, and preservation snapshots.

---
Status: pending | Parent: (root) | Added Round: 0
