# T-2: Lock prompt structure

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add regression coverage proving gap-analysis and gap-fix prompts keep `testDesign` in `systemPrompt` and do not copy `## Test Design` into `userPrompt`.

## Acceptance Criteria
- R2 is covered by spec-local tests with `// spec: R2 ...` headers.
- Both `buildGapAnalysisPrompt` and `buildTestFixPrompt` are asserted.

## Implementation Notes
Use existing prompt builder return objects. If helper exports need adjustment, expose only existing behavior needed by the tests.

## Test Strategy
Assert prompt object fields directly and run the focused review command tests.

---
Status: pending | Parent: (root) | Added Round: 0
