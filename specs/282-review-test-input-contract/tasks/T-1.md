# T-1: Lock collected inputs

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add regression coverage proving review-test input collection stays limited to spec-local test modules and excludes root tests, raw logs, and spec.md.

## Acceptance Criteria
- R1 is covered by spec-local tests with `// spec: R1 ...` headers.
- The test fixture includes root tests, spec-local tests, `.raw/test-execution.log`, and `spec.md` content.

## Implementation Notes
Prefer existing `collectTestFiles` tests where possible. Add spec-local coverage under `specs/282-review-test-input-contract/tests/` so requirement headers are visible to the flow.

## Test Strategy
Run the spec-local tests and the shared `tests/unit/flow/commands/review.test.js` coverage for test-review file scope.

---
Status: pending | Parent: (root) | Added Round: 0
