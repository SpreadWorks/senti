# T-3: Lock prompt limit

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add regression coverage proving review-test prompt input over 1,000,000 characters stops before the configured agent call.

## Acceptance Criteria
- R3 is covered by spec-local tests with `// spec: R3 ...` headers.
- R4 is satisfied because shared unit or e2e tests cover the same prompt-limit contract in the normal `npm test` path.
- R5 is checked by preserving existing CLI and review command expectations.

## Implementation Notes
Test the provider-call boundary without calling a real provider. Use existing test helpers or small stubs; do not add dependencies.

## Test Strategy
Run the new spec-local test, relevant shared unit/e2e tests, and `npm test` before finalization.

---
Status: pending | Parent: (root) | Added Round: 0
