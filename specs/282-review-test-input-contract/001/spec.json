{
  "goal": "Lock the review-test prompt input contract and 1,000,000 character limit with regression tests so future changes cannot reintroduce oversized prompt input.",
  "scope": {
    "in": [
      "Review-test prompt input collection and prompt construction in `src/flow/commands/review.js`.",
      "`collectTestFiles`, `buildGapAnalysisPrompt`, `buildTestFixPrompt`, `TEST_REVIEW_PROMPT_CHAR_LIMIT`, and the review-test provider-call boundary.",
      "Spec-local regression tests under `specs/282-review-test-input-contract/tests/` with `// spec: R<N> ...` headers.",
      "Shared unit or e2e tests under `tests/unit` or `tests/e2e` provide normal `npm test` coverage of the same contract."
    ],
    "out": [
      "Changing review-test AI scoring, finding classification, or auto-fix behavior.",
      "Changing provider configuration, model selection, or provider input limits.",
      "Adding saved prompt log analytics or metrics reporting.",
      "Changing CLI options, command names, or exit-code semantics.",
      "Running npm publish, npm dist-tag, or release preparation."
    ]
  },
  "constraints": [
    "Use only Node.js built-in modules and existing test infrastructure; do not add external dependencies.",
    "Do not weaken existing tests to make the new tests pass.",
    "Keep `src/` generic. Do not add project-specific paths or environment-specific values to production code.",
    "Do not change review-test runtime behavior except for exposing or structuring existing behavior so the contract can be tested.",
    "Spec-local coverage is required under `specs/282-review-test-input-contract/tests/`; shared tests may supplement but do not replace it.",
    "The existing CLI interface and exit-code contract remain unchanged.",
    "`bounded-resource-usage` is acknowledged for the existing recursive `collectTestsRecursive` traversal because this spec keeps the traversal scoped to `path.resolve(root, specDir, \"tests\")` and requires a pre-provider 1,000,000 character prompt limit that stops before the configured agent call."
  ],
  "design_principles": [
    "Prefer direct contract tests around the existing helpers and provider-call boundary over broad prompt rewrites.",
    "Keep unit tests responsible for detailed prompt shape assertions and use e2e coverage only for process boundaries not visible at helper level.",
    "Make excluded inputs explicit in fixtures so regressions show which input source leaked into the prompt."
  ],
  "overview": {
    "modules": [
      {
        "text": "`src/flow/commands/review.js` owns review-test input collection, prompt construction, prompt-size checks, and provider invocation for `flow run review --phase test`."
      },
      {
        "text": "`tests/unit/flow/commands/review.test.js` already covers part of the spec-local test-file collection contract and can be extended for shared regression coverage."
      },
      {
        "text": "`specs/282-review-test-input-contract/tests/` will hold spec-local coverage with requirement headers for the new contract tests."
      }
    ],
    "data_flow": [
      {
        "text": "review-test loads `spec.json`, generates or reads coverage context, collects spec-local test files, builds review prompts, checks prompt size, and only then calls the configured agent."
      },
      {
        "text": "gap-analysis and gap-fix prompt builders place stable test design text in `systemPrompt` and changing test code or gap text in `userPrompt`."
      }
    ],
    "decisions": [
      {
        "text": "[VERIFY] Source check: `collectTestFiles` collects only from the active spec's `tests/` directory.",
        "evidence": "`src/flow/commands/review.js` resolves `path.resolve(root, specDir, \"tests\")` and calls `collectTestsRecursive` only for that directory."
      },
      {
        "text": "[VERIFY] Source check: review-test has a fixed prompt character limit of 1,000,000.",
        "evidence": "`src/flow/commands/review.js` defines `TEST_REVIEW_PROMPT_CHAR_LIMIT = 1_000_000` and `assertTestReviewPromptWithinLimit` throws before the agent call when the measured prompt exceeds that limit."
      },
      {
        "text": "[VERIFY] Source check: gap-analysis and gap-fix prompts keep `Test Design` in `systemPrompt`.",
        "evidence": "`buildGapAnalysisPrompt` and `buildTestFixPrompt` call `.addSystemPrompt(\"## Test Design\", testDesign)` and put existing/current test code plus gaps in user prompt sections."
      },
      {
        "text": "Use unit tests for detailed prompt contract checks and e2e coverage only for provider boundary or CLI-path behavior not visible through exported helpers.",
        "evidence": "Draft QA q2 answered that unit tests must lock detailed prompt contracts, while e2e tests should cover process boundaries needed for normal `npm test` detection.",
        "consideredAlternatives": "Duplicating every prompt assertion in e2e tests was rejected because it raises fixture cost and makes failures less precise."
      },
      {
        "text": "Do not pin exact user-facing error wording for over-limit prompts.",
        "evidence": "Draft QA q3 answered that the critical acceptance condition is stopping before provider execution; exact error wording is less important than the provider boundary."
      }
    ]
  },
  "background": "Saved prompt logs showed review-test as a high-volume phase, and earlier failures were caused by Existing Test Code growing into a large prompt. Current source already narrows review-test input to spec-local tests, sets a 1,000,000 character limit, and keeps test design in systemPrompt for the gap-analysis and gap-fix prompt builders. The risk is regression: without explicit tests, future edits could mix project root tests, raw logs, or spec.md into review-test prompts or move stable test design text back into userPrompt.",
  "requirements": [
    {
      "id": "R1",
      "desc": "`collectTestFiles(root, specDir)` must include only `.test` or `.spec` JavaScript/TypeScript module files under `path.resolve(root, specDir, \"tests\")` and must not include project root `tests/`, `specs/<id>/tests/.raw/test-execution.log`, or `specs/<id>/spec.md` content.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R2",
      "desc": "`buildGapAnalysisPrompt(testDesign, testFiles)` and `buildTestFixPrompt(testDesign, gaps, testFiles)` must include `testDesign` in `systemPrompt` under `## Test Design` and must not include `## Test Design` in `userPrompt`.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R3",
      "desc": "review-test prompt size enforcement must use a 1,000,000 character limit and must stop before invoking the configured agent when the measured prompt exceeds that limit.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R4",
      "desc": "The normal `npm test` run must detect violations of R1, R2, and R3 through shared tests under `tests/unit` or `tests/e2e`; spec-local tests remain flow-local requirement coverage and are not required to be discovered directly by `npm test`.",
      "priority": "must",
      "status": "done",
      "testable": false
    },
    {
      "id": "R5",
      "desc": "The change must not alter review-test AI scoring, finding classification, auto-fix behavior, CLI options, or exit-code semantics.",
      "priority": "should",
      "status": "done"
    }
  ],
  "acceptance_criteria": [
    "R1: A regression test creates spec-local tests, project root tests, `.raw/test-execution.log`, and `spec.md` fixtures; the collected prompt input contains only spec-local `.test` or `.spec` module files.",
    "R2: Regression tests assert both gap-analysis and gap-fix prompt objects contain `## Test Design` in `systemPrompt` and do not contain `## Test Design` in `userPrompt`.",
    "R3: A regression test constructs review-test input over 1,000,000 characters and proves the configured agent call is not executed.",
    "R4: Shared tests under `tests/unit` or `tests/e2e` are added or updated so `npm test` fails if R1, R2, or R3 is broken; spec-local tests remain flow-local coverage.",
    "R5: Existing review-test command route and review artifact tests continue to pass without CLI option or exit-code expectation changes."
  ],
  "clarifications": [
    {
      "q": "Should implementation change review-test behavior or mainly add tests?",
      "a": "Mainly add tests. Production changes are allowed only when needed to expose existing behavior to tests or keep the current contract observable."
    },
    {
      "q": "What is the provider-boundary condition for over-limit prompts?",
      "a": "The configured agent must not be called when measured review-test prompt characters exceed `TEST_REVIEW_PROMPT_CHAR_LIMIT`."
    },
    {
      "q": "How should e2e coverage be used?",
      "a": "Use e2e coverage only for CLI/process behavior not visible through unit tests; detailed prompt content assertions belong in unit or spec-local tests."
    }
  ],
  "alternatives_considered": [
    {
      "option": "Rewrite review-test prompt wording while adding contract tests.",
      "reason": "Rejected because Issue #369 is about locking the current input contract, and wording changes would broaden the change beyond regression protection."
    },
    {
      "option": "Use only e2e tests for the contract.",
      "reason": "Rejected because prompt structure and excluded input sources are more precise and cheaper to verify at unit/spec-local level."
    },
    {
      "option": "Assert exact over-limit error text.",
      "reason": "Rejected because the required safety property is the provider-call boundary; exact wording can change without reintroducing input-too-large risk."
    }
  ],
  "open_questions": [],
  "keywords": [
    "review-test",
    "collectTestFiles",
    "buildGapAnalysisPrompt",
    "buildTestFixPrompt",
    "TEST_REVIEW_PROMPT_CHAR_LIMIT",
    "agent.call",
    "systemPrompt",
    "userPrompt",
    "Existing Test Code",
    "Test Design",
    "spec-local tests",
    "prompt limit",
    "test-execution.log"
  ],
  "implementationTargets": [
    "src/flow/commands/review.js",
    "tests/unit/flow/commands/review.test.js",
    "tests/e2e/flow/commands/",
    "specs/282-review-test-input-contract/tests/"
  ],
  "tasks": [
    {
      "id": "T-1",
      "title": "Lock collected inputs",
      "goal": "Add regression coverage proving review-test input collection stays limited to spec-local test modules and excludes root tests, raw logs, and spec.md.",
      "acceptance": [
        "R1 is covered by spec-local tests with `// spec: R1 ...` headers.",
        "The test fixture includes root tests, spec-local tests, `.raw/test-execution.log`, and `spec.md` content."
      ],
      "implementation_notes": "Prefer existing `collectTestFiles` tests where possible. Add spec-local coverage under `specs/282-review-test-input-contract/tests/` so requirement headers are visible to the flow.",
      "test_strategy": "Run the spec-local tests and the shared `tests/unit/flow/commands/review.test.js` coverage for test-review file scope.",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending",
      "key": "T-1"
    },
    {
      "id": "T-2",
      "title": "Lock prompt structure",
      "goal": "Add regression coverage proving gap-analysis and gap-fix prompts keep `testDesign` in `systemPrompt` and do not copy `## Test Design` into `userPrompt`.",
      "acceptance": [
        "R2 is covered by spec-local tests with `// spec: R2 ...` headers.",
        "Both `buildGapAnalysisPrompt` and `buildTestFixPrompt` are asserted."
      ],
      "implementation_notes": "Use existing prompt builder return objects. If helper exports need adjustment, expose only existing behavior needed by the tests.",
      "test_strategy": "Assert prompt object fields directly and run the focused review command tests.",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending",
      "key": "T-2"
    },
    {
      "id": "T-3",
      "title": "Lock prompt limit",
      "goal": "Add regression coverage proving review-test prompt input over 1,000,000 characters stops before the configured agent call.",
      "acceptance": [
        "R3 is covered by spec-local tests with `// spec: R3 ...` headers.",
        "R4 is satisfied because shared unit or e2e tests cover the same prompt-limit contract in the normal `npm test` path.",
        "R5 is checked by preserving existing CLI and review command expectations."
      ],
      "implementation_notes": "Test the provider-call boundary without calling a real provider. Use existing test helpers or small stubs; do not add dependencies.",
      "test_strategy": "Run the new spec-local test, relevant shared unit/e2e tests, and `npm test` before finalization.",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending",
      "key": "T-3"
    }
  ],
  "user_approval": {
    "approved": true,
    "confirmed_at": "2026-06-06T14:13:51.456Z",
    "notes": "User approved gate-passed spec for Issue #369."
  },
  "specId": "282-review-test-input-contract"
}
