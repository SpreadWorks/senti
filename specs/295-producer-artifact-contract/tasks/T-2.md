# T-2: Apply producer validation

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Call the shared completion contract from draft, spec, repair, and test artifact producer paths before downstream semantic checks trust their artifacts.

## Acceptance Criteria
- Draft and spec invalid artifacts fail before semantic guardrail calls.
- Scenario-validity, test-execute, and test-result-review artifacts keep current schema and raw evidence checks.
- Existing gate defensive checks remain unless fully replaced with parity tests.

## Implementation Notes
Use existing validators in run-gate.js and test-artifacts.js as the source of truth. Avoid broad rewrites of semantic gate prompt flow.

## Test Strategy
Spec-local tests plus targeted unit tests for producer completion call sites.

---
Status: pending | Parent: (root) | Added Round: 0
