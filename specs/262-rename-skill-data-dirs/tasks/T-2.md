# T-2: Move DataSources

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Move common DataSource modules from docs-owned naming to the package-level `src/data/` directory.

## Acceptance Criteria
- `src/data/` contains the previous common DataSource modules.
- `src/docs/lib/resolver-factory.js` resolves common DataSources from `src/data/`.
- Imports in tests and production code no longer target `src/docs/data/`.

## Implementation Notes
Preserve resolver layering order: common DataSources first, then preset-local `data/` directories.

## Test Strategy
Update resolver/import tests and add spec-local checks for the new common data path.

---
Status: pending | Parent: (root) | Added Round: 0
