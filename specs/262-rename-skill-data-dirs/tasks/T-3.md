# T-3: Update path consumers

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Update production code that deploys skills, resolves includes, loads DataSources, and discovers experimental workflow skills.

## Acceptance Criteria
- `MAIN_SKILLS_DIR` replaces `MAIN_SKILLS_TEMPLATES_DIR`.
- `@skills/` resolves skill partials and `@templates/` is not accepted as an alias.
- `src/upgrade.js` uses the new bundled and experimental skill source paths.

## Implementation Notes
Keep CLI argument behavior and exit-code contracts unchanged. Keep include recursion bounds unchanged.

## Test Strategy
Update include, upgrade, flow, and resolver tests that assert the old paths.

---
Status: pending | Parent: (root) | Added Round: 0
