# T-5: Add spec coverage

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add spec-local tests that prove the new directory contracts and stale-reference removals.

## Acceptance Criteria
- Spec-local tests under `specs/262-rename-skill-data-dirs/tests/` include `// spec: R<N>` headers.
- Spec-local tests cover every testable requirement R1 through R8.
- Scenario validity classifies the tests as expected to fail before implementation.

## Implementation Notes
Keep tests based on filesystem/path contracts and generated artifact checks so they remain independent of implementation details.

## Test Strategy
Run the spec-local tests during scenario validity and test execution.

---
Status: pending | Parent: (root) | Added Round: 0
