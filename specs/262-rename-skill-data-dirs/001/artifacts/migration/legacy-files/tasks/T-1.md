# T-1: Move skill sources

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Move bundled and experimental skill source directories to their new canonical locations.

## Acceptance Criteria
- `src/skills/` contains the previous bundled skill directories and `rules.json`.
- `src/skills/partials/` contains the previous shared partial markdown files.
- `experimental/workflow/skills/` contains the previous experimental workflow skill source.

## Implementation Notes
Use file moves, not content rewrites, except where include directives must change from `@templates/partials/` to `@skills/partials/`.

## Test Strategy
Spec-local tests should assert old directories are absent and new directories contain expected files.

---
Status: pending | Parent: (root) | Added Round: 0
