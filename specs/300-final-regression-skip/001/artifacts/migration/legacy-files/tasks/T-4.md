# T-4: Cover regression skip behavior

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add focused spec-local and shared regression tests that lock the new final-regression execution policy and artifact contract.

## Acceptance Criteria
- Spec-local tests cover R1 through R7 with `// spec: R<N>` headers.
- Shared unit tests cover skip planning, fail-closed fallback, artifact validation, post-hook completion, report rendering, and finalize artifact inclusion.
- Tests do not weaken existing pass/fail final-regression behavior.

## Implementation Notes
Reuse existing temporary project fixtures in `tests/unit/flow/final-regression.test.js` where possible.

## Test Strategy
Run spec-local tests, targeted unit tests, and the configured project regression required by the flow.

---
Status: pending | Parent: (root) | Added Round: 0
