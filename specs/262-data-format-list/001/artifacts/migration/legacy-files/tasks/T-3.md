# T-3: Add spec coverage

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add spec-local tests that connect the new directive format behavior to requirements R1 through R8.

## Acceptance Criteria
- Spec-local tests live under `specs/262-data-format-list/tests/`.
- Each spec-local test file includes `// spec: R<N>` headers covering every testable requirement.
- The tests can be run by the flow test execution step without external dependencies.

## Implementation Notes
Prefer small Node.js tests that import existing modules directly. Keep shared unit tests for reusable module contracts and spec-local tests for requirement traceability.

## Test Strategy
Run the spec-local tests during `test-execute`; run the relevant shared unit tests and final project regression later in the flow.

---
Status: pending | Parent: (root) | Added Round: 0
