# T-7: Cover migration contracts

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add spec-local tests that map every testable requirement to executable or static verification.

## Acceptance Criteria
- Each spec-local test file has a `// spec: R<N>` header.
- Resolver, upgrade, include, helper, test discovery, and deletion contracts have coverage.
- `npm test` passes after implementation.

## Implementation Notes
Use `specs/289-b6fb-3-3-preset-migration-fully-migrate-presets-/tests/` for required coverage; shared regression tests may be updated but do not replace spec-local headers.

## Test Strategy
Run spec-local tests through the flow test step and run full project regression before finalize.

---
Status: pending | Parent: (root) | Added Round: 0
