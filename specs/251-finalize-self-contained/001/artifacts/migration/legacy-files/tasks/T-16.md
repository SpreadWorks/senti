# T-16: Confirm post-cleanup inactive semantics in resolveActiveFlow

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
post-cleanup 状態 (.active-flow クリア + last-finalized-spec 書き込み済み) で flow get status / flow run resume が active:false を返す。resolveActiveFlow() のスキャンが finalized 済み spec を再 active 化しない。

## Acceptance Criteria
- cleanup 完了後の flow get status が active:false を返す
- cleanup 完了後の flow run resume も active:false で再開対象にしない
- resolveActiveFlow() が last-finalized-spec の spec を再スキャンで active 化しない

## Implementation Notes
src/lib/flow-manager.js の resolveActiveFlow() を確認し、`.active-flow` 不在 + last-finalized-spec マッチを 'inactive' と判定するロジックを追加または既存の挙動を確認

## Test Strategy
単体テスト: post-cleanup fixture で resolveActiveFlow() / flow get status / flow run resume を呼んで active:false を確認

---
Status: pending | Parent: (root) | Added Round: 0
