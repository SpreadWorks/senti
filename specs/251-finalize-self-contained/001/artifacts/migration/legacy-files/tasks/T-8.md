# T-8: Add e2e regression test for worktree finalize happy path

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
tests/e2e/flow/commands/ 配下に worktree finalize の通し e2e を追加する。各 leaf 完了後の step 遷移、authority 切替、next-action、cleanup envelope、worktree/branch 消滅、active flow 解消を検証。

## Acceptance Criteria
- tests/e2e/flow/commands/ 配下に該当 e2e ファイルが存在する
- happy path 1 シナリオで R1-R5, R7 の動作を検証
- 実行 cwd: finalize-* は worktree、cleanup 後の status は main repo cwd

## Implementation Notes
既存 e2e のパターン (createTmpDir / setup fixture) を踏襲。worktree mode の flow を準備し finalize 4 leaf を順次実行

## Test Strategy
本タスク自体が test 追加

---
Status: pending | Parent: (root) | Added Round: 0
