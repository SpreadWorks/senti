# T-18: Scope PR merge route as out-of-scope and document

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
PR merge route (commands.gh enabled AND gh available) は本 spec のスコープ外として明示し、実装が squash merge route のみを self-contained 化対象とすることをコード上で表現する。

## Acceptance Criteria
- PR route で finalize-merge が PR 作成までで終わる場合、post hook が finalize-merge step を 'done' に正規化することは行うが、finalize-sync / finalize-cleanup の post-merge 自動化はスコープ外として既存挙動を温存
- spec.md / コード comment で PR route が現状仕様 (各 leaf 個別実行) のまま温存されることを明記

## Implementation Notes
src/flow/commands/merge.js / run-finalize-merge.js の PR route 分岐を確認。本 spec では PR route の post-merge 流れに変更を加えない (既存挙動を温存)

## Test Strategy
なし (動作温存の確認のみ)

---
Status: pending | Parent: (root) | Added Round: 0
