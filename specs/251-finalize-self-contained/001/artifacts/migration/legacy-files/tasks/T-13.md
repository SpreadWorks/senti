# T-13: Update finalize-* AI prompt files

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
src/flow/prompts/impl/finalize-{commit,merge,sync,cleanup}.md の AI 向け prompt から旧手順 (cd <mainRepoPath>, flow report show, finalize-* に対する手動 flow set step) を削除する。finalize-cleanup.md は envelope.data.report.text を読む手順に書き換える。

## Acceptance Criteria
- 4 つの prompt ファイルから cd <mainRepoPath> の文字列が消えている
- 4 つの prompt ファイルから flow report show の文字列が消えている (cleanup プロンプトでは data.report.text 経由の手順に置き換え)
- 4 つの prompt ファイルに finalize-* 自身に対する flow set step 案内が無い

## Implementation Notes
T-7 (skill template 更新) と並行して同じ意図で prompt も更新する

## Test Strategy
なし (prompt 変更は人間レビュー)

---
Status: pending | Parent: (root) | Added Round: 0
