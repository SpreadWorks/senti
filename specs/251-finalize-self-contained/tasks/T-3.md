# T-3: Implement non-circular authority resolution in flow get next-action

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
flow get next-action が D4 の authority 解決ルール ((a)-(d)) に従って main repo 側 / worktree 側 / pointer fallback のいずれかを authority として採用するよう実装する。

## Acceptance Criteria
- merge 前 (main 側 specs/<id>/flow.json 不在) は worktree authority を返す
- merge 後 (main 側 specs/<id>/flow.json 存在) は main authority を返す
- cleanup 後 (worktree 削除済み) は last-finalized-spec pointer 経由で解決する

## Implementation Notes
src/flow/get/next-action.js または resolve-context.js 相当の実装層を確認。flow state metadata 取得と main repo flow.json 存在チェックの順序が循環しないように設計

## Test Strategy
単体テスト: 3 つの状態 (merge 前 / merge 後 / cleanup 後) を mock fixture で再現し、authority 選択が期待通りであることを検証

---
Status: pending | Parent: (root) | Added Round: 0
