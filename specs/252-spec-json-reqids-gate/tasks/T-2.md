# T-2: Update gate-impl e2e coverage

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Adjust gate-impl integration fixtures and stub responses so tests prove reqIds come from spec.json when available and from spec.md only as fallback.

## Acceptance Criteria
- R5 is satisfied with AI requirement-evaluation paths that do not use --skip-guardrail.
- R6 is covered with fallback fixture cases.
- Stale REQ-SPEC responses are rejected when spec.json provides R1.
- R9 is satisfied by using explicit per-case stub guardrail IDs without changing defaultPassResponse.
- R12 is satisfied by spec-local tests with // spec: R<N> headers for source-selection coverage.

## Implementation Notes
Focus on tests/e2e/flow/gate-impl-integration.test.js and tests/helpers/stub-agent.js. Avoid broad fixture rewrites unrelated to reqId source selection.

## Test Strategy
Run the affected gate-impl e2e test file and the spec-local tests added under specs/252-spec-json-reqids-gate/tests/.

---
Status: pending | Parent: (root) | Added Round: 0
