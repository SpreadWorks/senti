# T-2: Cover registry preset resolution

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add regression coverage proving docs enrich resolves plugin registry presets through the project root.

## Acceptance Criteria
- The test fails if docs enrich omits the root argument from resolveChaptersOrder.
- The fixture or test setup is generic and does not depend on local workspace paths.

## Implementation Notes
Prefer a temporary fixture project/plugin registry or a focused test seam already used by docs command tests.

## Test Strategy
Add spec-local node:test coverage under specs/308-docs-enrich-preset-root/tests with a // spec: R1/R2 header.

---
Status: pending | Parent: (root) | Added Round: 0
