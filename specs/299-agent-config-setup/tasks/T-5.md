# T-5: Add regression coverage

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Create spec-local behavior tests and update shared tests so the setup, upgrade, validation, runtime, and help contracts remain protected.

## Acceptance Criteria
- R1-R9 have spec-local coverage under `specs/299-agent-config-setup/tests/` with `// spec: R<N> ...` headers.
- Shared tests are updated only where production contracts changed.
- `npm test` covers non-agent AI changes; `npm run test:agent` is run if runtime agent command behavior changes.

## Implementation Notes
Keep tests focused on behavior visible from config output, validation results, Agent.resolve, upgrade output/config, and help/setup output.

## Test Strategy
Run targeted spec-local tests first, then project test command. Run agent tests if `src/lib/agent.js`, `src/docs/commands/enrich.js`, or related AI command runtime behavior changes.

---
Status: pending | Parent: (root) | Added Round: 0
