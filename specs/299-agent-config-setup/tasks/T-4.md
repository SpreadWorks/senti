# T-4: Update agent help

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Make built-in profile/provider discoverability explicit in setup output, help text, locale text, and docs without seeding config.

## Acceptance Criteria
- R7 setup completion output shows selected `agent.default`, `agent.useProfile`, and available built-in profile names.
- R7 help/docs explain `agent.default`, `agent.useProfile`, `agent.profiles`, `agent.providers`, and override/pin examples.
- R7 no `senti config example agent` command is introduced.

## Implementation Notes
Use existing i18n/help structures. If source help files under `src/skills` or `src/presets` are changed, run `senti upgrade`; otherwise no upgrade artifact is required solely for docs wording.

## Test Strategy
Add wording assertions for help/setup output and docs/help source text where local patterns exist.

---
Status: pending | Parent: (root) | Added Round: 0
