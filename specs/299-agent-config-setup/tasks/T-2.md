# T-2: Update setup agent selection

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Change setup to collect available agent families, main/default agent, and instruction file generation targets, then write the new compact agent config.

## Acceptance Criteria
- R1 interactive setup supports claude-only, codex-only, and claude+codex.
- R2 setup writes the expected `agent.default` and `agent.useProfile` matrix.
- R3 non-interactive `--agent` supports single and comma-separated multi-agent values with first-listed main semantics.
- R6 claude+codex setup can select AGENTS.md and/or CLAUDE.md generation targets interactively and generates both by default non-interactively.
- R9 existing agent config forms initialize wizard defaults correctly.

## Implementation Notes
Keep wizard-managed field updates scoped. Preserve existing project metadata, docs settings, workDir creation, skill deployment, and user agent overrides not managed by the wizard.

## Test Strategy
Add setup config generation tests for each matrix row, non-interactive first-listed main semantics, generation target behavior, and existing config default normalization, using existing setup helper patterns where available.

---
Status: pending | Parent: (root) | Added Round: 0
