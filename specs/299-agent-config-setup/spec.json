{
  "goal": "senti setup の agent 選択を複数選択化し、agent config は選択 intent と明示 override だけを保存する。",
  "scope": {
    "in": [
      "`senti setup` の interactive agent selection と main/default agent selection。",
      "`senti setup --agent` の non-interactive agent selection contract。",
      "setup が書く `.senti/config.json` の `agent.default`, `agent.useProfile`, `agent.workDir`。",
      "built-in agent profiles/providers の runtime resolution と config validation。",
      "`senti upgrade` の agent defaults seed 停止と既存 overrides preservation。",
      "AGENTS.md / CLAUDE.md generation target selection for setup.",
      "Rerunning setup on existing agent config and normalizing wizard defaults.",
      "setup completion output, help, locale, docs wording for agent settings.",
      "setup / upgrade / validation / runtime resolution / help wording regression tests."
    ],
    "out": [
      "`senti config example agent` のような sample generation command の追加。",
      "npm publish / npm dist-tag 操作。",
      "外部依存の追加。",
      "built-in provider/model の全面的な選定変更。",
      "docs/ 全体の自動再生成。ただし必要な help/docs source wording の変更は含む。"
    ]
  },
  "constraints": [
    "`src/` には特定 project 固有情報を入れない。agent defaults は全ユーザー向けの package-managed built-ins として扱う。",
    "外部依存を追加しない。Node.js built-in と既存 helper を使う。",
    "ユーザーが `config.agent.providers` または `config.agent.profiles` に明示した entries は built-in より優先し、自動上書きしない。",
    "既存 config の判定不能な profiles/providers は user-authored override として保持し、通常 setup/upgrade では削除や縮約をしない。",
    "通常 setup/upgrade は built-in profiles/providers を config に大量コピーしない。",
    "Concrete provider key を既に `agent.default` に持つ既存 config は、setup または明示編集で alias に変わるまで runtime resolution で扱えるようにする。",
    "Spec behavior coverage は `specs/299-agent-config-setup/tests/` 配下に置き、各 spec-local test file に `// spec: R<N> ...` header を付ける。",
    "`src/skills/` または `src/presets/` を変更した場合だけ `senti upgrade` を実行する。"
  ],
  "design_principles": [
    "Config に package-managed defaults をコピーせず、runtime resolver が package 側の built-ins を参照する。",
    "Setup の agent availability, main/default agent, and agent file generation targets を別々の intent として扱う。",
    "Migration parity は公開面ごとに検証する。config generation, upgrade, validation, runtime resolution, generated files, and help/docs を別々に確認する。",
    "既存 user override は pin とみなし、package updates に追従しない明示設定として尊重する。"
  ],
  "overview": {
    "modules": [
      {
        "text": "`src/lib/agent-defaults.js`: built-in agent profiles/providers の source of truth。seed-oriented API/comment から runtime built-in defaults へ役割を整理する。"
      },
      {
        "text": "`src/lib/agent.js`: `SENTI_PROFILE`, `agent.useProfile`, `agent.default` の優先順位を維持しながら built-in profiles を解決する。"
      },
      {
        "text": "`src/lib/config.js`: `agent.useProfile` と profile provider references を built-in profiles/providers も含めて validate する。"
      },
      {
        "text": "`src/lib/provider.js`: built-in provider family aliases と user provider overrides を解決する registry boundary。"
      },
      {
        "text": "`src/setup.js`: agent selection, main/default selection, config writing, summary, and AGENTS/CLAUDE generation targets を扱う。"
      },
      {
        "text": "`src/upgrade.js`: skill/preset/config migration を維持しつつ、agent built-ins の add-only seed を通常経路から外す。"
      },
      {
        "text": "`src/locale/**` and docs/help sources: agent.default, agent.useProfile, agent.profiles, agent.providers の説明と override 例を提示する。"
      }
    ],
    "data_flow": [
      {
        "text": "Setup は available agents を claude, codex, or claude+codex として受け取り、複数選択時だけ main agent を決める。"
      },
      {
        "text": "Setup config write は `agent.default` に claude/codex alias、`agent.useProfile` に selected availability + main に対応する built-in profile name、`agent.workDir` に `.tmp` を保存する。"
      },
      {
        "text": "Non-interactive setup treats the first family in comma-separated `--agent` as main/default. For example, `--agent claude,codex` writes `claude-main`, while `--agent codex,claude` writes `codex-main`."
      },
      {
        "text": "Non-interactive setup with both families selected generates both AGENTS.md and CLAUDE.md by default because no prompt can collect a narrower target set."
      },
      {
        "text": "Rerunning setup normalizes existing `agent.default` aliases, concrete provider keys, and built-in `agent.useProfile` names into wizard availability/main defaults before prompting or rewriting config."
      },
      {
        "text": "Runtime は `SENTI_PROFILE` または `agent.useProfile` で profile を選び、user-defined profile があればそれを優先し、無ければ package built-in profile を参照する。"
      },
      {
        "text": "Provider resolution は user provider override を優先し、built-in provider family or provider key fallback を維持する。"
      },
      {
        "text": "Upgrade は existing config の non-agent migrations and skill/preset deployment を維持し、agent profiles/providers の package defaults は追加しない。"
      },
      {
        "text": "Setup completion output and help/docs show selected default/useProfile and the available built-in profile names instead of copying built-in definitions into config."
      }
    ],
    "decisions": [
      {
        "text": "[VERIFY] setup currently stores concrete provider keys and seeds agent defaults.",
        "evidence": "`src/setup.js` maps `claude` to `claude/sonnet` and `codex` to `codex/gpt-5.4`, assigns that to `config.agent.default`, and then calls `mergeAgentDefaults(config.agent)`."
      },
      {
        "text": "[VERIFY] upgrade currently re-seeds built-in profiles/providers when any agent section exists.",
        "evidence": "`src/upgrade.js` checks `if (raw.agent && typeof raw.agent === \"object\")` and calls `mergeAgentDefaults(raw.agent)`, logging added profiles/slots/providers when changed."
      },
      {
        "text": "[VERIFY] validation currently treats `agent.useProfile` as config-local when `agent.profiles` exists.",
        "evidence": "`src/lib/config.js` validates profile provider references against `new ProviderRegistry(raw.agent?.providers || {})` and rejects `agent.useProfile` only by checking `raw.agent.profiles[raw.agent.useProfile]`."
      },
      {
        "text": "[VERIFY] runtime profile selection currently requires `agent.profiles` for selected profiles.",
        "evidence": "`src/lib/agent.js` chooses `options.profileName || SENTI_PROFILE || agentSection.useProfile`; if `profiles` is missing or lacks the name, it throws `Profile \"...\" is not defined in agent.profiles.`"
      },
      {
        "text": "[VERIFY] built-in profile definitions already contain the required useProfile names.",
        "evidence": "`src/lib/agent-defaults.js` defines `claude-main`, `codex-main`, `claude-only`, and `codex-only` profiles plus provider pool entries, but the file comments/API frame them as seed defaults."
      },
      {
        "text": "Migration inventory: retained public behavior includes setup config generation, workDir creation, AGENTS/CLAUDE generation, `upgrade` non-agent migrations, `validate()`, runtime agent resolution, and help/docs discoverability.",
        "evidence": "Draft impact and migration parity entries identify each surface and require behavior-level verification for retained surfaces."
      },
      {
        "text": "Migration mapping: built-in profile/provider ownership moves from copied config entries to a package-managed runtime source of truth.",
        "evidence": "Issue #390 says setup/upgrade seeded profiles/providers make existing configs stale and asks to avoid bulk-copying built-ins while resolving built-ins at runtime."
      },
      {
        "text": "Existing unknown profiles/providers are preserved as user overrides.",
        "evidence": "Issue #390 says entries that cannot be identified are safest to preserve as user changes. Draft q2 records this policy because current config has no provenance marker for managed seed entries."
      },
      {
        "text": "Non-interactive setup supports comma-separated multi-agent intent.",
        "evidence": "Draft q3 records this because `src/setup.js` already uses comma-separated parsing for `--type`, and `--agent` is part of non-interactive setup."
      },
      {
        "text": "Non-interactive multi-agent main selection uses first-listed family semantics.",
        "evidence": "Spec review found the main/default agent ambiguous. First-listed semantics keep one `--agent` option sufficient and make `claude,codex` and `codex,claude` deterministically testable."
      },
      {
        "text": "Non-interactive multi-agent file generation defaults to both agent files.",
        "evidence": "Spec review found non-interactive file targets undefined. Existing non-interactive setup has no prompt, and generating both matches the selected availability without adding another CLI option."
      },
      {
        "text": "Setup rerun defaults normalize old and new agent config forms.",
        "evidence": "Spec review found `loadExistingDefaults()` currently reads `cfg.agent.default` literally. Migration parity requires existing concrete provider keys and built-in useProfile names to preselect the intended availability/main state."
      },
      {
        "text": "No list command is added in this spec.",
        "evidence": "Issue #390 says list command is only to consider if necessary and `senti config example agent` is unnecessary. Draft q5 chooses setup output and help/docs as the discoverability mechanism."
      }
    ]
  },
  "background": "`senti setup` currently treats agent selection as a single claude/codex choice and writes a concrete provider key into `agent.default`. It also seeds built-in profiles/providers into config, and `senti upgrade` repeats that add-only seed whenever an agent section exists. That copied config makes model/provider defaults stale after package updates. The desired behavior is to store only selected user intent and explicit overrides in config while runtime and validation resolve package-managed built-ins.",
  "requirements": [
    {
      "id": "R1",
      "desc": "Interactive `senti setup` lets the user select claude only, codex only, or claude + codex as available agents, and asks for a main/default agent only when both families are selected.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R2",
      "desc": "Setup writes `agent.default` as `claude` or `codex` and writes `agent.useProfile` as `claude-only`, `codex-only`, `claude-main`, or `codex-main` according to selected availability and main agent.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R3",
      "desc": "Non-interactive `senti setup --agent` accepts single values `claude` / `codex` and comma-separated multi-agent values; for multi-agent values, the first listed family is the main/default agent.",
      "priority": "should",
      "status": "done"
    },
    {
      "id": "R4",
      "desc": "Runtime agent resolution and config validation resolve package built-in profiles/providers without requiring them to be copied into `.senti/config.json`, while user-defined `agent.profiles` and `agent.providers` override built-ins by key.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R5",
      "desc": "Normal setup and upgrade do not add package built-in `agent.profiles` or `agent.providers` entries to config; existing unknown entries remain preserved as user overrides.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R6",
      "desc": "When both claude and codex are selected interactively, setup lets the user choose AGENTS.md and/or CLAUDE.md generation targets; when both are selected non-interactively, setup generates both files by default.",
      "priority": "should",
      "status": "done"
    },
    {
      "id": "R7",
      "desc": "Setup completion output and help/docs explain selected `agent.default`, selected `agent.useProfile`, available built-in profile names, and override examples for `agent.profiles` and `agent.providers`.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R8",
      "desc": "Existing concrete provider key defaults such as `claude/sonnet` or `codex/gpt-5.4` remain resolvable until setup or an explicit user edit rewrites them to family aliases.",
      "priority": "should",
      "status": "done"
    },
    {
      "id": "R9",
      "desc": "Rerunning setup normalizes existing `agent.default` family aliases, concrete provider keys, and built-in `agent.useProfile` names into wizard availability and main/default defaults before prompting or rewriting config.",
      "priority": "must",
      "status": "done"
    }
  ],
  "acceptance_criteria": [
    "R1/R2: Interactive setup can produce configs for claude-only, codex-only, claude+codex with claude main, and claude+codex with codex main; each config has the expected `agent.default` alias and `agent.useProfile` value.",
    "R2/R5: A new setup config with agent enabled contains `agent.default`, `agent.useProfile`, and `agent.workDir`, but does not contain copied package built-in `agent.profiles` or `agent.providers` unless the user already had explicit overrides.",
    "R3: Non-interactive `--agent claude`, `--agent codex`, `--agent claude,codex`, and `--agent codex,claude` produce the corresponding setup config matrix, with the first listed family as main for multi-agent values.",
    "R4: `validate()` accepts config where `agent.useProfile` is one of the package built-in profile names without config-local `agent.profiles`, and still rejects unknown profile/provider references.",
    "R4/R8: Runtime `Agent.resolve()` can resolve built-in `agent.useProfile` names, user profile overrides, user provider overrides, and existing concrete provider-key defaults.",
    "R5: `senti upgrade` no longer logs or writes added agent default profiles/providers solely because `raw.agent` exists, while preserving unrelated upgrade config migrations.",
    "R5: Existing `agent.profiles` and `agent.providers` entries remain unchanged by setup/upgrade unless they are wizard-managed fields explicitly being updated.",
    "R6: With claude+codex selected interactively, setup can generate AGENTS.md, CLAUDE.md, both, or skip according to the selected generation targets.",
    "R6: With non-interactive `--agent claude,codex` or `--agent codex,claude`, setup generates both AGENTS.md and CLAUDE.md by default.",
    "R7: Help/docs and setup completion output mention `agent.default`, `agent.useProfile`, built-in profile names, and how to override/pin providers/profiles without requiring a sample generation command.",
    "R9: Existing configs with `agent.default` as `claude`, `codex`, `claude/sonnet`, or `codex/gpt-5.4`, and configs with `agent.useProfile` as `claude-only`, `codex-only`, `claude-main`, or `codex-main`, initialize setup defaults to the intended availability/main state.",
    "R1-R9: Spec-local tests under `specs/299-agent-config-setup/tests/` include `// spec: R<N> ...` headers and cover setup, upgrade, validation, runtime resolution, and help/docs wording."
  ],
  "clarifications": [
    {
      "q": "Should setup copy built-in profiles/providers into config for discoverability?",
      "a": "No. Discoverability is handled by setup completion output and help/docs. Config stores selected intent and explicit overrides only."
    },
    {
      "q": "How are existing seeded profiles/providers handled?",
      "a": "They are preserved as user overrides unless a future migration can prove they are managed by senti. This spec does not delete or compact unknown entries."
    },
    {
      "q": "Does this remove support for concrete provider keys in `agent.default`?",
      "a": "No. New setup writes family aliases, but existing concrete provider keys remain resolvable for migration parity."
    },
    {
      "q": "Is a list command for built-in profiles/providers required?",
      "a": "No. The spec uses help/docs and setup completion output. A list command can be considered later if those surfaces are insufficient."
    }
  ],
  "alternatives_considered": [
    {
      "option": "Only change `agent.default` to aliases",
      "reason": "Rejected because setup/upgrade would still seed built-in profiles/providers and existing configs would still stop following package-managed defaults."
    },
    {
      "option": "Delete seed-looking existing profiles/providers by value",
      "reason": "Rejected because config has no provenance marker; a user may have intentionally pinned the same values."
    },
    {
      "option": "Support multi-agent selection only in interactive setup",
      "reason": "Rejected because `--agent` is a public non-interactive setup contract and should express the same config intent."
    },
    {
      "option": "Generate only the main agent's instruction file when both agents are available",
      "reason": "Rejected because available agent selection and instruction file generation are separate user intents."
    },
    {
      "option": "Add `senti config example agent` or a built-in list command now",
      "reason": "Rejected because Issue #390 says the sample command is unnecessary and list command is only optional; help/docs and setup output satisfy current discoverability."
    }
  ],
  "open_questions": [],
  "keywords": [
    "setup",
    "agent selection",
    "agent.default",
    "agent.useProfile",
    "agent.profiles",
    "agent.providers",
    "agent-defaults",
    "mergeAgentDefaults",
    "ProviderRegistry",
    "resolveProfileSelection",
    "validate config",
    "upgrade migration",
    "AGENTS.md",
    "CLAUDE.md",
    "built-in profiles"
  ],
  "tasks": [
    {
      "id": "T-1",
      "title": "Add built-in agent resolution",
      "goal": "Expose package built-in profiles/providers as runtime defaults that validation and agent resolution can share without copying them into config.",
      "acceptance": [
        "R4 built-in `agent.useProfile` names resolve without config-local `agent.profiles`.",
        "R4 user-defined profiles/providers override built-ins by key.",
        "R8 existing concrete provider keys in `agent.default` remain resolvable."
      ],
      "implementation_notes": "Update `src/lib/agent-defaults.js` role/API as the built-in source of truth. Wire `src/lib/config.js`, `src/lib/agent.js`, and any needed `ProviderRegistry` boundary so validation and runtime use the same built-in data.",
      "test_strategy": "Add spec-local and shared unit coverage for built-in useProfile validation, unknown profile rejection, user override precedence, and Agent.resolve behavior.",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending"
    },
    {
      "id": "T-2",
      "title": "Update setup agent selection",
      "goal": "Change setup to collect available agent families, main/default agent, and instruction file generation targets, then write the new compact agent config.",
      "acceptance": [
        "R1 interactive setup supports claude-only, codex-only, and claude+codex.",
        "R2 setup writes the expected `agent.default` and `agent.useProfile` matrix.",
        "R3 non-interactive `--agent` supports single and comma-separated multi-agent values with first-listed main semantics.",
        "R6 claude+codex setup can select AGENTS.md and/or CLAUDE.md generation targets interactively and generates both by default non-interactively.",
        "R9 existing agent config forms initialize wizard defaults correctly."
      ],
      "implementation_notes": "Keep wizard-managed field updates scoped. Preserve existing project metadata, docs settings, workDir creation, skill deployment, and user agent overrides not managed by the wizard.",
      "test_strategy": "Add setup config generation tests for each matrix row, non-interactive first-listed main semantics, generation target behavior, and existing config default normalization, using existing setup helper patterns where available.",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending"
    },
    {
      "id": "T-3",
      "title": "Stop upgrade seeding",
      "goal": "Remove normal upgrade behavior that adds package built-in agent profiles/providers to config while preserving existing user entries and unrelated migrations.",
      "acceptance": [
        "R5 upgrade does not add built-in profiles/providers just because `raw.agent` exists.",
        "R5 existing `agent.profiles` and `agent.providers` entries remain unchanged.",
        "Unrelated upgrade config migrations and skill/preset deployment behavior remain covered."
      ],
      "implementation_notes": "Adjust `src/upgrade.js` comments and summary/logging so they no longer describe agent defaults seed as normal migration behavior.",
      "test_strategy": "Add or update upgrade tests for no agent-default expansion, preservation of existing entries, and unchanged non-agent migration behavior.",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending"
    },
    {
      "id": "T-4",
      "title": "Update agent help",
      "goal": "Make built-in profile/provider discoverability explicit in setup output, help text, locale text, and docs without seeding config.",
      "acceptance": [
        "R7 setup completion output shows selected `agent.default`, `agent.useProfile`, and available built-in profile names.",
        "R7 help/docs explain `agent.default`, `agent.useProfile`, `agent.profiles`, `agent.providers`, and override/pin examples.",
        "R7 no `senti config example agent` command is introduced."
      ],
      "implementation_notes": "Use existing i18n/help structures. If source help files under `src/skills` or `src/presets` are changed, run `senti upgrade`; otherwise no upgrade artifact is required solely for docs wording.",
      "test_strategy": "Add wording assertions for help/setup output and docs/help source text where local patterns exist.",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending"
    },
    {
      "id": "T-5",
      "title": "Add regression coverage",
      "goal": "Create spec-local behavior tests and update shared tests so the setup, upgrade, validation, runtime, and help contracts remain protected.",
      "acceptance": [
        "R1-R9 have spec-local coverage under `specs/299-agent-config-setup/tests/` with `// spec: R<N> ...` headers.",
        "Shared tests are updated only where production contracts changed.",
        "`npm test` covers non-agent AI changes; `npm run test:agent` is run if runtime agent command behavior changes."
      ],
      "implementation_notes": "Keep tests focused on behavior visible from config output, validation results, Agent.resolve, upgrade output/config, and help/setup output.",
      "test_strategy": "Run targeted spec-local tests first, then project test command. Run agent tests if `src/lib/agent.js`, `src/docs/commands/enrich.js`, or related AI command runtime behavior changes.",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending"
    }
  ],
  "user_approval": {
    "approved": true,
    "confirmed_at": "2026-06-15T03:32:38.197Z",
    "notes": "Auto-approved after draft-gate PASS, spec-review repair, and spec-gate PASS under user-enabled auto mode."
  }
}
