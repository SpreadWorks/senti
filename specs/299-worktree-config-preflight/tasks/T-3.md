# T-3: Preserve worktree success path

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Keep existing successful worktree prepare behavior unchanged when `.senti/config.json` is branch-reflected and clean.

## Acceptance Criteria
- R5 clean config worktree prepare succeeds and produces expected spec artifacts and docs scan output.
- R5 `.senti/config.local.json` overlay and plugin runtime sync still occur.
- R6 branch mode and no-branch mode behavior remain outside the new halt path.

## Implementation Notes
Do not reorder `syncPluginRuntimeToWorktree()` relative to `PostWorktree` and spec file creation except for adding the new preflight before `git worktree add`. Reuse existing post-worktree hook tests where possible.

## Test Strategy
Add spec-local success-path coverage and keep existing `tests/e2e/flow/commands/post-worktree-hook.test.js` passing.

---
Status: pending | Parent: (root) | Added Round: 0
