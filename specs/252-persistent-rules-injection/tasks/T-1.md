# T-1: Add rules.json SSOT file with the 10 migrated rule entries

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Bundle the canonical rule SSOT with the package, populated with the 10 entries per the inventory table.

## Acceptance Criteria
- File exists at the package's skills templates location.
- Contains exactly 10 entries with valid id, phase, state, body fields.
- Each drift-prone entry's body contains MUST + why + how-to-apply elements.
- `npm pack --dry-run` lists the file.

## Implementation Notes
Author wording extracted from the existing partials being deprecated; tighten to skill-rules house style.

## Test Strategy
Schema fixture loaded by loader test (T-2). Package-files test asserts inclusion in pack.

---
Status: pending | Parent: (root) | Added Round: 0
