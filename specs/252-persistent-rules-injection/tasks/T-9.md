# T-9: Add code-quality guardrail entry to base preset

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Inherit a single composite `code-quality` rule (no single-caller indirection / DRY first / present design direction) at integration and task-impl phases for every project on sdd-forge.

## Acceptance Criteria
- `src/presets/base/guardrail.json` gains a new entry with `meta.category: "code-quality"` and `meta.phase: ["integration", "task-impl"]`.
- `loadMergedGuardrails` returns the entry.
- filterByPhase positively matches `integration` and `task-impl`; negatively matches `spec` and `draft`.

## Implementation Notes
Compose the three concerns into a single rule body so gate output reads as one principle.

## Test Strategy
Unit test on guardrail-load filter behavior.

---
Status: pending | Parent: (root) | Added Round: 0
