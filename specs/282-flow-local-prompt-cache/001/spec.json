{
  "goal": "Agent.call 境界に flow-local prompt hash cache を追加し、同一 prompt 入力の再送で provider process を起動しないようにする。",
  "scope": {
    "in": [
      "Agent.call の同一 commandId/provider/profileKey/systemPrompt/userPrompt/jsonSchema/fmtFallback 入力に対して、同じ active flow 内の2回目以降は保存済みの成功応答を返す。",
      "cache key は commandId、provider、profileKey、resolved profile invocation identity、systemPrompt、userPrompt、jsonSchema、fmtFallback から sha256 で導出する。",
      "保存対象は成功した非空応答だけにする。",
      "provider/profileKey、systemPrompt、userPrompt、jsonSchema、fmtFallback、commandId のいずれかが異なる場合は cache miss にする。",
      "cache hit を metrics または runtime log で確認できるようにする。",
      "cache は active flow の current spec-local artifact に閉じ、別 project / 別 flow へ再利用しない。"
    ],
    "out": [
      "global prompt cache は作らない。",
      "provider 側 prompt cache の設定や integration は変更しない。",
      "prompt 本文の縮小、構造変更、phase 別 prompt 最適化は対象外。",
      "既存 CLI command の user-facing option、意味、exit code contract は変更しない。"
    ]
  },
  "constraints": [
    "外部依存は追加しない。hash 生成とファイル永続化は Node.js built-in module だけを使う。",
    "src/ 以下に特定 project や環境固有の値を書かない。",
    "cache は FlowManager で active flow と current spec が解決できる場合だけ有効にする。active flow がない Agent.call では cache を無効化する。",
    "cache artifact は active flow の current spec 配下から解決し、repository global / user global の保存先を使わない。",
    "`bounded-resource-usage`: cache artifact の読み書きは current flow / current spec の1ファイル単位に限定し、全 specs や prompt logs の横断 scan をしない。",
    "既存の Agent.call public option contract を維持し、既存 caller が新 option を渡さなくても動作する。"
  ],
  "design_principles": [
    "重複抑止は provider 呼び出し境界に置き、review / gate / docs など phase 個別の prompt 生成処理には広げない。",
    "provider callCount と cache hit count は区別し、metrics 利用者が実 provider 呼び出し数を読み取れる状態を保つ。",
    "失敗や空応答は retry / error handling の対象として扱い、cache によって再試行機会を失わせない。"
  ],
  "overview": {
    "modules": [
      {
        "text": "`src/lib/agent.js` owns provider/profile resolution, prompt invocation, retry, logging, and agent metric accumulation."
      },
      {
        "text": "`src/lib/flow-store.js` owns flow.json persistence and append-only metrics entries for active flow state."
      },
      {
        "text": "`src/lib/agent-metrics.js`, `src/flow/lib/get-status.js`, and `src/metrics/commands/token.js` own agent metric dimension normalization and aggregation."
      },
      {
        "text": "Spec-local tests under `specs/282-flow-local-prompt-cache/tests/` verify cache behavior and isolation."
      }
    ],
    "data_flow": [
      {
        "text": "Agent.call resolves commandId to provider/profile, builds a cache key from the resolved invocation identity plus prompt inputs, checks the active flow current-spec cache artifact, and returns a cached response on hit."
      },
      {
        "text": "On cache miss, Agent.call invokes the provider through the existing retry path. Only a successful non-empty parsed response is stored in the flow-local cache."
      },
      {
        "text": "Agent metrics or runtime logs record cache-hit evidence separately from real provider invocations so call reduction is auditable."
      }
    ],
    "decisions": [
      {
        "text": "[VERIFY] Source check: Agent.call is the centralized provider invocation boundary and already has access to commandId, systemPrompt, jsonSchema, fmtFallback, provider, and profileKey.",
        "evidence": "`src/lib/agent.js` resolves provider/profile in `call()`, passes commandId through options, builds invocation input from systemPrompt/jsonSchema/fmtFallback, and calls `_callOnceWithRetry()` before logging."
      },
      {
        "text": "[VERIFY] Source check: flow metrics are flat append-only entries and can represent cache-hit evidence without changing caller commands.",
        "evidence": "`src/lib/flow-store.js` exposes `appendMetric()` and `accumulateAgentMetrics()`. `src/flow/lib/get-status.js` and `src/metrics/commands/token.js` aggregate agent metrics from `state.metrics`."
      },
      {
        "text": "Use active-flow current-spec storage rather than global storage.",
        "evidence": "Issue #368 states the cache must be limited to active flow or spec-local artifact and must not leak to another project or another flow.",
        "consideredAlternatives": "A non-active spec-local cache would need a new scope input that Agent.call does not currently receive. A global cache could save more calls across flows but violates the Issue scope and creates cross-project reuse risk."
      },
      {
        "text": "[CORRECTION] Source check: cache is enabled only when FlowManager resolves an active flow/current spec; non-active Agent.call executions bypass cache.",
        "evidence": "`src/lib/agent.js` receives optional `flowManager`; review found no existing Agent.call option or service that identifies a non-active current spec-local scope."
      },
      {
        "text": "[CORRECTION] Source check: include resolved profile invocation identity in cache identity.",
        "evidence": "`src/lib/agent.js` uses profile.command, profile.args, jsonOutputFlag, jsonSchemaFlag, and jsonSchemaMode to shape the provider invocation; profileKey alone can stay the same while those values change."
      },
      {
        "text": "Cache only successful non-empty responses after required parsing has succeeded.",
        "evidence": "Issue #368 excludes empty responses, failures before schema parsing, and provider errors from caching.",
        "consideredAlternatives": "Caching raw stdout before parse was rejected because later cache hits could replay invalid provider output."
      },
      {
        "text": "Expose cache hits separately from provider callCount.",
        "evidence": "Draft QA q2 resolved that provider call totals must remain interpretable as actual provider invocations while cacheHit/cachedResponse evidence records skipped calls.",
        "consideredAlternatives": "Counting cache hits as normal callCount was rejected because it would hide provider process suppression."
      },
      {
        "text": "Existing feature impact: repeated identical successful Agent.call inputs in an active flow may return from cache. Existing caller options, provider configuration, CLI commands, and exit codes remain unchanged.",
        "evidence": "Scope targets Agent.call boundary and constraints preserve existing public option and CLI contracts."
      }
    ]
  },
  "background": "Saved prompt logs show exact duplicate prompt calls across multiple phases, with Issue #368 reporting 572 surplus calls and 12,703,553 duplicate characters. Provider-side cache may reduce token cost, but it does not remove CLI process startup, latency, retry, or log-volume overhead. Because review, gate, docs, and flow AI calls pass through Agent.call, suppressing exact duplicate successful calls at that boundary addresses the repeated invocation root cause without phase-specific prompt rewrites.",
  "requirements": [
    {
      "id": "R1",
      "desc": "On the second and later Agent.call with identical commandId, provider, profileKey, resolved profile invocation identity, systemPrompt, userPrompt, jsonSchema, and fmtFallback within the same active flow current-spec cache scope, return the saved response without invoking the provider process.",
      "priority": "must",
      "status": "pending"
    },
    {
      "id": "R2",
      "desc": "Compute the cache key as a sha256 digest over commandId, provider, profileKey, resolved profile invocation identity, systemPrompt, userPrompt, jsonSchema, and fmtFallback using deterministic serialization for structured fields.",
      "priority": "must",
      "status": "pending"
    },
    {
      "id": "R3",
      "desc": "Treat a changed provider, profileKey, resolved profile invocation identity, commandId, systemPrompt, userPrompt, jsonSchema, or fmtFallback as a cache miss.",
      "priority": "must",
      "status": "pending"
    },
    {
      "id": "R4",
      "desc": "Persist only successful non-empty responses after required provider output parsing has succeeded; do not cache empty responses, provider errors, or parse failures.",
      "priority": "must",
      "status": "pending"
    },
    {
      "id": "R5",
      "desc": "Record cache-hit evidence separately from real provider callCount in metrics or runtime logs.",
      "priority": "must",
      "status": "pending"
    },
    {
      "id": "R6",
      "desc": "Keep cached responses scoped to the current active flow's current spec-local artifact, and disable caching when no active flow/current spec is resolved, so another project or another flow cannot reuse them.",
      "priority": "must",
      "status": "pending"
    },
    {
      "id": "R7",
      "desc": "Preserve existing Agent.call caller behavior for cache misses, including retry handling, provider parsing, logging, and returned response text.",
      "priority": "must",
      "status": "pending"
    }
  ],
  "acceptance_criteria": [
    "R1: A spec-local test calls Agent.call twice with the same cache identity inside one active flow/current spec scope and observes that the provider process is invoked once while both calls return the same response.",
    "R2: A spec-local test verifies that equivalent jsonSchema/fmtFallback input produces a stable cache key and that the key includes commandId, provider, profileKey, resolved profile invocation identity, systemPrompt, userPrompt, jsonSchema, and fmtFallback.",
    "R3: Spec-local tests change provider/profileKey, resolved profile invocation identity, and prompt fields and observe cache misses.",
    "R4: Spec-local tests cover provider failure, empty response, and parse failure cases and observe that the next valid call does not reuse those outputs from cache.",
    "R5: Cache-hit evidence is visible in metrics or runtime log output, and provider callCount remains the count of real provider invocations.",
    "R6: A spec-local test creates a second active flow/current spec scope and observes no cache hit from the first scope; a no-active-flow Agent.call path does not read or write the cache.",
    "R7: Existing Agent.call retry, parse, stdin fallback, and logging tests continue to pass for cache misses."
  ],
  "clarifications": [
    {
      "q": "Should cache hits count as provider calls?",
      "a": "No. Cache-hit evidence must be recorded separately so provider callCount remains real provider invocation count."
    },
    {
      "q": "Can empty successful stdout be cached?",
      "a": "No. Empty responses are excluded from cache storage even when a caller has opted out of retry."
    },
    {
      "q": "Can another flow reuse a previous flow's cached response?",
      "a": "No. Cache reuse is limited to the current active flow's current spec-local artifact."
    },
    {
      "q": "What happens when no active flow/current spec is resolved?",
      "a": "Caching is disabled and Agent.call uses the existing provider invocation path."
    },
    {
      "q": "What profile fields are part of the invocation identity?",
      "a": "At minimum, the identity includes the resolved profile command, args, jsonOutputFlag, jsonSchemaFlag, and jsonSchemaMode, or an equivalent deterministic representation of the resolved invocation shape."
    }
  ],
  "alternatives_considered": [
    {
      "option": "Rely only on provider-side prompt cache.",
      "reason": "Rejected because provider-side cache does not remove local provider process startup, latency, retry, and log-volume overhead."
    },
    {
      "option": "Add phase-specific caches in review or gate commands.",
      "reason": "Rejected because duplicate prompts occur across multiple phases and Agent.call is the shared boundary."
    },
    {
      "option": "Create a global cache across projects and flows.",
      "reason": "Rejected because Issue #368 requires active-flow/spec-local scope and no cross-project or cross-flow leakage."
    },
    {
      "option": "Cache raw provider stdout before parse.",
      "reason": "Rejected because failures before required parsing must not be cached."
    }
  ],
  "open_questions": [],
  "keywords": [
    "Agent.call",
    "prompt cache",
    "flow-local cache",
    "sha256",
    "commandId",
    "provider",
    "profileKey",
    "profile command",
    "profile args",
    "jsonSchema",
    "fmtFallback",
    "flow-store",
    "agent metrics",
    "cacheHit",
    "cachedResponse",
    "provider invocation"
  ],
  "implementationTargets": [
    "src/lib/agent.js",
    "src/lib/flow-store.js",
    "src/lib/agent-metrics.js",
    "src/flow/lib/get-status.js",
    "src/metrics/commands/token.js",
    "specs/282-flow-local-prompt-cache/tests/"
  ],
  "tasks": [
    {
      "id": "T-1",
      "title": "Add prompt cache storage",
      "goal": "Provide flow-local or spec-local cache storage for Agent.call responses and deterministic cache-key calculation.",
      "acceptance": [
        "R2 is satisfied by deterministic sha256 key calculation over all required identity fields including resolved invocation identity.",
        "R6 is satisfied by cache storage that resolves only from the active flow current-spec artifact and is disabled when no active flow/current spec is resolved."
      ],
      "implementation_notes": "Use Node.js built-in crypto and filesystem APIs only. Resolve cache storage through FlowManager active-flow/current-spec context. Keep cache storage bounded to one current-flow/current-spec artifact and avoid scanning all specs or prompt logs. Do not add a non-active spec-local cache path unless a concrete scope input is introduced.",
      "test_strategy": "Add spec-local tests with `// spec: R2 ...` and `// spec: R6 ...` headers for stable key fields and cross-flow/spec isolation.",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending",
      "key": "T-1"
    },
    {
      "id": "T-2",
      "title": "Use cache in Agent.call",
      "goal": "Integrate cache lookup and save behavior into Agent.call while preserving existing miss-path invocation behavior.",
      "acceptance": [
        "R1 is satisfied by skipping provider invocation on the second identical successful call within the same active flow current-spec scope.",
        "R3 is satisfied by misses for changed provider/profile/prompt identity fields.",
        "R4 is satisfied by not saving empty, failed, or parse-failed responses.",
        "R7 is satisfied by keeping existing retry, parse, stdin fallback, logging, and returned text behavior for misses."
      ],
      "implementation_notes": "Place lookup after provider/profile resolution and before provider spawn. Save only after the existing invocation path returns a successful non-empty parsed text result.",
      "test_strategy": "Add spec-local tests with `// spec: R1 ...`, `// spec: R3 ...`, `// spec: R4 ...`, and `// spec: R7 ...` headers using a test provider command whose invocation count can be observed.",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending",
      "key": "T-2"
    },
    {
      "id": "T-3",
      "title": "Record cache hit evidence",
      "goal": "Expose cache hits through metrics or runtime logs without inflating real provider call counts.",
      "acceptance": [
        "R5 is satisfied by cache-hit evidence visible in metrics or runtime logs.",
        "R5 is satisfied while provider callCount remains the count of real provider invocations."
      ],
      "implementation_notes": "Prefer the existing append-only flow metrics path when active flow context exists. If runtime log evidence is used, keep it explicit enough to distinguish cache hits from provider calls.",
      "test_strategy": "Add spec-local tests with `// spec: R5 ...` headers and update focused shared metric aggregation tests only if production metric shape changes.",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending",
      "key": "T-3"
    }
  ],
  "user_approval": {
    "approved": true,
    "confirmed_at": "2026-06-06T14:06:01.791Z",
    "notes": "autoApprove: spec-gate passed and approval step auto-selected [1]."
  },
  "specId": "282-flow-local-prompt-cache"
}
