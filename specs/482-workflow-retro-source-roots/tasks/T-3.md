# T-3: Update retro eligibility documentation

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Update the user-facing workflow retro documentation so it names retained eligible paths, tracked top-level `src/` production JavaScript implementation eligibility, and retained exclusions for non-code, unsafe, and untracked candidates.

## Acceptance Criteria
- Documentation lists retained legacy eligible paths.
- Documentation states tracked top-level `src/` production JavaScript implementation files are eligible.
- Documentation states docs, skills, agent instructions, specs, fixtures, and test-only candidates remain non-code unless they include implementation paths.

## Implementation Notes
If source skill files are changed rather than only generated plugin artifacts, run `senti upgrade` afterward per project rules.

## Test Strategy
Documentation requirement is not inherently automated; rely on diff review and any existing snapshot or upgrade checks affected by skill generation.

---
Status: pending | Parent: (root) | Added Round: 0
