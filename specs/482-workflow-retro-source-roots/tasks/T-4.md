# T-4: Cover source-root regression

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add regression coverage for the Issue #482 source-root misclassification and retained legacy/safety behavior.

## Acceptance Criteria
- Spec-local tests under `specs/482-workflow-retro-source-roots/tests/` include `// spec: R<N>` headers for testable requirements.
- A fixture with tracked files including `src/flow/lib/example.js` and a candidate whose targetPaths contain only `src/flow/lib/example.js` proves tracked `src/` implementation targets are not filtered as `non-code-target`.
- Spec-local or shared workflow retro service/CLI tests cover display candidate numbering, `retro --add` number selection, existing evidence cache reuse, and filtered/skipped reason parity.

## Implementation Notes
Use repository-neutral fixtures and avoid depending on external GitHub or remote state.

## Test Strategy
Run the spec-local tests under `specs/482-workflow-retro-source-roots/tests/`; if `.senti/plugins/workflow/lib/services/index.js` changes, also run its workflow retro service tests, and if `.senti/plugins/workflow/commands/workflow.js` changes, also run its workflow retro CLI command tests.

---
Status: pending | Parent: (root) | Added Round: 0
