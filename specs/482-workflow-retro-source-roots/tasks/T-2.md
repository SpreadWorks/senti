# T-2: Preserve retro safety filtering

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Keep all existing fail-closed rejection classes intact while applying the broadened code eligibility predicate to display and add through one evaluation path.

## Acceptance Criteria
- Unsafe, absolute, traversal, outside-repository, untracked, empty-target, over-limit, scope-mismatch, docs-only, skill-only, agent-instruction-only, spec-only, fixture-only, and test-only candidates remain ineligible.
- Display `filtered` entries and add `skipped` entries report the same reason for the same candidate.

## Implementation Notes
Do not duplicate predicate logic between display and add. Preserve existing reason strings where the same rejection class is retained.

## Test Strategy
Spec-local tests exercise display and add paths against the same candidates and assert retained skipped/filtered reason classes.

---
Status: pending | Parent: (root) | Added Round: 0
