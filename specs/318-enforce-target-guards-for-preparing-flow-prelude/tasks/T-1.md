# T-1: Expose preparing target context

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Make preparing selection preserve routing state while exposing the selected identity and isolating unknown explicit run IDs.

## Acceptance Criteria
- R1 selection shape and unknown-run isolation pass focused unit coverage.
- R2 base-command validation uses the selected preparing or active target without changing requiresFlow semantics.

## Implementation Notes
Limit production changes to `flow-context.js` and `base-command.js`. Reuse the existing target mismatch helper and return-object context pattern.

## Test Strategy
Extend optional-flow-context tests for known and unknown preparing selection, matching guards, and preserved active selection behavior.

---
Status: pending | Parent: (root) | Added Round: 0
