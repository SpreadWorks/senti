# Draft Review Results

1 issue(s) detected.

### 1. {"type":"thread.started","thread_id":"019ddc8a-755e-7190-a7ba-bf629e373f93"}
{"type":"turn.started"}
{"type":"item.completed","item":{"id":"item_0","type":"agent_message","text":"### 1. PromptBuilder の設計 QA が不足\n**QA:** NEW  \n**Issue:** Issue の中心である `PromptBuilder class` について、配置場所、公開メソッド、戻り値構造、既存プロンプト関数との責務分担が QA で明確になっていない。  \n**Suggestion:** `PromptBuilder` が `systemPrompt`, `userPrompt`, `jsonSchema` をどう保持・出力するか、既存の文字列組み立て関数をどこまで置換するかを問う QA を追加する。\n\n### 2. Q1 の判断根拠が Issue 本文に対して弱い\n**QA:** Q1  \n**Issue:** 「Issue の『保留』は diff 関連の別件改修」という説明が、提示された evidence だけでは裏付けられていない。impl-guardrail を含める判断はスコープ拡大になり得る。  \n**Suggestion:** Issue #290 の該当文言と、`buildGuardrailPromptFromFiltered` の共有範囲を根拠にして、「保留対象」と「今回移行対象」を明確に分ける。\n\n### 3. 呼び出しサイト移行の網羅性が検証不能\n**QA:** Q1  \n**Issue:** 「全 14 箇所」と列挙しているが、どの関数・どの agent 呼び出しが PromptBuilder 化されるのかが曖昧。コードレビュー時に漏れを判定しづらい。  \n**Suggestion:** 対象ファイルごとに「対象関数」「現在の prompt 構築方式」「移行後の Builder 利用箇所」を QA の evidence に含める。\n\n### 4. テスト戦略が callsite の実移行を保証していない\n**QA:** Q2  \n**Issue:** `PromptBuilder` と `_buildInvocation` の単体テスト中心で、「各コマンドが実際に Builder 経由になったか」「旧 FMT 埋め込みが残っていないか」を検知できない。  \n**Suggestion:** 代表 callsite の統合テスト、または prompt 出力スナップショット/構造検査を追加する。少なくとも guardrail, auto-check, enrich の移行後 payload を検証対象にする。\n\n### 5. provider フラグ仕様の evidence が不足\n**QA:** Q3  \n**Issue:** `--json-schema`, `--output-schema`, `--system-prompt` という具体フラグ名が出ているが、provider ごとの対応根拠が示されていない。特に Claude/Codex/User provider で同じ前提を置けるか不明。  \n**Suggestion:** `provider.js` に `jsonSchemaFlag()` の契約を定義し、Claude/Codex/User/custom provider それぞれの戻り値と fallback 条件を QA に明記する。\n\n### 6. structured output の失敗時挙動が未カバー\n**QA:** NEW  \n**Issue:** json schema を provider フラグに分離した場合、CLI が schema file を受け付けない、ファイル書き出しに失敗する、provider が null を返す、AI 出力が schema 違反になる等の境界ケースが QA にない。  \n**Suggestion:** 「structured output が使えない provider ではどうするか」「schema 違反時は既存 parser/validator とどう連携するか」を問う QA を追加する。\n\n### 7. user prompt prefix 最大化の成功条件が曖昧\n**QA:** Q4  \n**Issue:** 「共有パーツを先頭に配置する」だけでは、prefix がどの範囲で一致すれば成功か判断しづらい。system prompt 側の差異がキャッシュにどう影響するかも未説明。  \n**Suggestion:** guardrail の spec/draft/impl など具体ペアについて、`userPrompt` の先頭 N 文字またはセクション列が一致することを検証条件として定義する。\n\n### 8. トークン削減 QA が文字数比較に寄りすぎ\n**QA:** Q6  \n**Issue:** 「正確なトークン数は比較しない」は妥当でも、Issue が token reduction を目的にしているため、文字数だけでは成功基準として弱い。加えて system prompt へ移しただけの内容は総送信量としては残る可能性がある。  \n**Suggestion:** 「user prompt から除去された文字数」「schema flag 化で重複送信されなくなる部分」「cache prefix 一致率」を分けて測る成功基準にする。総 prompt 文字数削減と cache 効率改善を混同しない。"}}
{"type":"turn.completed","usage":{"input_tokens":29844,"cached_input_tokens":29568,"output_tokens":1607,"reasoning_output_tokens":516}}
