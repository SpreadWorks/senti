{"attempts":[{"attempt":1,"legacySource":"review-history/impl-attempt-001.json","detail":{"version":1,"phase":"impl","generatedAt":"2026-06-09T11:02:27.261Z","verdict":"ADVISORY","summary":{"blocking":0,"nonBlocking":203,"total":203},"blockingFindings":[],"nonBlockingImprovements":[{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."}],"excluded":{"missingFile":0,"outOfScope":0},"contractSummary":{"targetStep":"impl-review","artifactPath":"specs/284-plugin-mechanism-workflow-presets/impl-review.json","verdict":"ADVISORY","result":"ADVISORY","blockingCount":0,"failureKind":null,"nextAction":"impl-gate","completionKind":"normal","progressSignature":"9f3b158330607d5939393fcc91d3660d4d424ee51465108eeea85312fd14ce68"},"sourceArtifact":"impl-review.json","attempt":1,"findings":[{"id":"impl-001-non-blocking-001","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Cache Preset Resolution Per Call","body":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","category":"refactor"},{"id":"impl-001-non-blocking-002","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Add Explicit Bounds To Recursive Default Merge","body":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","category":"refactor"},{"id":"impl-001-non-blocking-003","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Bound Plugin Config Bulk Loading","body":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","category":"refactor"},{"id":"impl-001-non-blocking-004","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid Repeated Linear Preset Lookups","body":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","category":"refactor"},{"id":"impl-001-non-blocking-005","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"5. Extract DataSource Class Normalization","body":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","category":"refactor"},{"id":"impl-001-non-blocking-006","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Cache Preset Resolution Per Call","body":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","category":"refactor"},{"id":"impl-001-non-blocking-007","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Add Explicit Bounds To Recursive Default Merge","body":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","category":"refactor"},{"id":"impl-001-non-blocking-008","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Bound Plugin Config Bulk Loading","body":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","category":"refactor"},{"id":"impl-001-non-blocking-009","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid Repeated Linear Preset Lookups","body":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","category":"refactor"},{"id":"impl-001-non-blocking-010","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"5. Extract DataSource Class Normalization","body":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","category":"refactor"},{"id":"impl-001-non-blocking-011","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"5. Extract DataSource Class Normalization","body":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","category":"refactor"},{"id":"impl-001-non-blocking-012","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Cache Preset Resolution Per Call","body":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","category":"refactor"},{"id":"impl-001-non-blocking-013","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Add Explicit Bounds To Recursive Default Merge","body":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","category":"refactor"},{"id":"impl-001-non-blocking-014","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Bound Plugin Config Bulk Loading","body":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","category":"refactor"},{"id":"impl-001-non-blocking-015","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid Repeated Linear Preset Lookups","body":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","category":"refactor"},{"id":"impl-001-non-blocking-016","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"5. Extract DataSource Class Normalization","body":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","category":"refactor"},{"id":"impl-001-non-blocking-017","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Add Explicit Bounds To Recursive Default Merge","body":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","category":"refactor"},{"id":"impl-001-non-blocking-018","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Bound Plugin Config Bulk Loading","body":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","category":"refactor"},{"id":"impl-001-non-blocking-019","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Cache Preset Resolution Per Call","body":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","category":"refactor"},{"id":"impl-001-non-blocking-020","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid Repeated Linear Preset Lookups","body":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","category":"refactor"},{"id":"impl-001-non-blocking-021","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Cache Preset Resolution Per Call","body":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","category":"refactor"},{"id":"impl-001-non-blocking-022","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Add Explicit Bounds To Recursive Default Merge","body":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","category":"refactor"},{"id":"impl-001-non-blocking-023","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Bound Plugin Config Bulk Loading","body":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","category":"refactor"},{"id":"impl-001-non-blocking-024","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid Repeated Linear Preset Lookups","body":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","category":"refactor"},{"id":"impl-001-non-blocking-025","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"5. Extract DataSource Class Normalization","body":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","category":"refactor"},{"id":"impl-001-non-blocking-026","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Cache Preset Resolution Per Call","body":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","category":"refactor"},{"id":"impl-001-non-blocking-027","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Add Explicit Bounds To Recursive Default Merge","body":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","category":"refactor"},{"id":"impl-001-non-blocking-028","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Bound Plugin Config Bulk Loading","body":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","category":"refactor"},{"id":"impl-001-non-blocking-029","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid Repeated Linear Preset Lookups","body":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","category":"refactor"},{"id":"impl-001-non-blocking-030","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"5. Extract DataSource Class Normalization","body":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","category":"refactor"},{"id":"impl-001-non-blocking-031","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Cache Preset Resolution Per Call","body":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","category":"refactor"},{"id":"impl-001-non-blocking-032","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Add Explicit Bounds To Recursive Default Merge","body":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","category":"refactor"},{"id":"impl-001-non-blocking-033","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Bound Plugin Config Bulk Loading","body":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","category":"refactor"},{"id":"impl-001-non-blocking-034","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid Repeated Linear Preset Lookups","body":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","category":"refactor"},{"id":"impl-001-non-blocking-035","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"5. Extract DataSource Class Normalization","body":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","category":"refactor"},{"id":"impl-001-non-blocking-036","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Cache Preset Resolution Per Call","body":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","category":"refactor"},{"id":"impl-001-non-blocking-037","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Add Explicit Bounds To Recursive Default Merge","body":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","category":"refactor"},{"id":"impl-001-non-blocking-038","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Bound Plugin Config Bulk Loading","body":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","category":"refactor"},{"id":"impl-001-non-blocking-039","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid Repeated Linear Preset Lookups","body":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","category":"refactor"},{"id":"impl-001-non-blocking-040","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"5. Extract DataSource Class Normalization","body":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","category":"refactor"},{"id":"impl-001-non-blocking-041","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Cache Preset Resolution Per Call","body":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","category":"refactor"},{"id":"impl-001-non-blocking-042","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Add Explicit Bounds To Recursive Default Merge","body":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","category":"refactor"},{"id":"impl-001-non-blocking-043","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Bound Plugin Config Bulk Loading","body":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","category":"refactor"},{"id":"impl-001-non-blocking-044","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid Repeated Linear Preset Lookups","body":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","category":"refactor"},{"id":"impl-001-non-blocking-045","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"5. Extract DataSource Class Normalization","body":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","category":"refactor"},{"id":"impl-001-non-blocking-046","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Cache Preset Resolution Per Call","body":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","category":"refactor"},{"id":"impl-001-non-blocking-047","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Add Explicit Bounds To Recursive Default Merge","body":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","category":"refactor"},{"id":"impl-001-non-blocking-048","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Bound Plugin Config Bulk Loading","body":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","category":"refactor"},{"id":"impl-001-non-blocking-049","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid Repeated Linear Preset Lookups","body":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","category":"refactor"},{"id":"impl-001-non-blocking-050","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"5. Extract DataSource Class Normalization","body":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","category":"refactor"},{"id":"impl-001-non-blocking-051","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Cache Preset Resolution Per Call","body":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","category":"refactor"},{"id":"impl-001-non-blocking-052","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Add Explicit Bounds To Recursive Default Merge","body":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","category":"refactor"},{"id":"impl-001-non-blocking-053","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Bound Plugin Config Bulk Loading","body":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","category":"refactor"},{"id":"impl-001-non-blocking-054","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid Repeated Linear Preset Lookups","body":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","category":"refactor"},{"id":"impl-001-non-blocking-055","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"5. Extract DataSource Class Normalization","body":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","category":"refactor"},{"id":"impl-001-non-blocking-056","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Cache Preset Resolution Per Call","body":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","category":"refactor"},{"id":"impl-001-non-blocking-057","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Add Explicit Bounds To Recursive Default Merge","body":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","category":"refactor"},{"id":"impl-001-non-blocking-058","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Bound Plugin Config Bulk Loading","body":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","category":"refactor"},{"id":"impl-001-non-blocking-059","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid Repeated Linear Preset Lookups","body":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","category":"refactor"},{"id":"impl-001-non-blocking-060","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"5. Extract DataSource Class Normalization","body":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","category":"refactor"},{"id":"impl-001-non-blocking-061","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Cache Preset Resolution Per Call","body":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","category":"refactor"},{"id":"impl-001-non-blocking-062","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Add Explicit Bounds To Recursive Default Merge","body":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","category":"refactor"},{"id":"impl-001-non-blocking-063","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Bound Plugin Config Bulk Loading","body":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","category":"refactor"},{"id":"impl-001-non-blocking-064","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid Repeated Linear Preset Lookups","body":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","category":"refactor"},{"id":"impl-001-non-blocking-065","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"5. Extract DataSource Class Normalization","body":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","category":"refactor"},{"id":"impl-001-non-blocking-066","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Cache Preset Resolution Per Call","body":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","category":"refactor"},{"id":"impl-001-non-blocking-067","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Add Explicit Bounds To Recursive Default Merge","body":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","category":"refactor"},{"id":"impl-001-non-blocking-068","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Bound Plugin Config Bulk Loading","body":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","category":"refactor"},{"id":"impl-001-non-blocking-069","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid Repeated Linear Preset Lookups","body":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","category":"refactor"},{"id":"impl-001-non-blocking-070","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"5. Extract DataSource Class Normalization","body":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","category":"refactor"},{"id":"impl-001-non-blocking-071","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Cache Preset Resolution Per Call","body":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","category":"refactor"},{"id":"impl-001-non-blocking-072","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Add Explicit Bounds To Recursive Default Merge","body":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","category":"refactor"},{"id":"impl-001-non-blocking-073","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Bound Plugin Config Bulk Loading","body":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","category":"refactor"},{"id":"impl-001-non-blocking-074","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid Repeated Linear Preset Lookups","body":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","category":"refactor"},{"id":"impl-001-non-blocking-075","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"5. Extract DataSource Class Normalization","body":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","category":"refactor"},{"id":"impl-001-non-blocking-076","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Cache Preset Resolution Per Call","body":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","category":"refactor"},{"id":"impl-001-non-blocking-077","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Add Explicit Bounds To Recursive Default Merge","body":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","category":"refactor"},{"id":"impl-001-non-blocking-078","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Bound Plugin Config Bulk Loading","body":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","category":"refactor"},{"id":"impl-001-non-blocking-079","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid Repeated Linear Preset Lookups","body":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","category":"refactor"},{"id":"impl-001-non-blocking-080","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"5. Extract DataSource Class Normalization","body":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","category":"refactor"},{"id":"impl-001-non-blocking-081","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Cache Preset Resolution Per Call","body":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","category":"refactor"},{"id":"impl-001-non-blocking-082","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Add Explicit Bounds To Recursive Default Merge","body":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","category":"refactor"},{"id":"impl-001-non-blocking-083","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Bound Plugin Config Bulk Loading","body":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","category":"refactor"},{"id":"impl-001-non-blocking-084","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid Repeated Linear Preset Lookups","body":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","category":"refactor"},{"id":"impl-001-non-blocking-085","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"5. Extract DataSource Class Normalization","body":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","category":"refactor"},{"id":"impl-001-non-blocking-086","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Cache Preset Resolution Per Call","body":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","category":"refactor"},{"id":"impl-001-non-blocking-087","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Add Explicit Bounds To Recursive Default Merge","body":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","category":"refactor"},{"id":"impl-001-non-blocking-088","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Bound Plugin Config Bulk Loading","body":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","category":"refactor"},{"id":"impl-001-non-blocking-089","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid Repeated Linear Preset Lookups","body":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","category":"refactor"},{"id":"impl-001-non-blocking-090","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"5. Extract DataSource Class Normalization","body":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","category":"refactor"},{"id":"impl-001-non-blocking-091","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Cache Preset Resolution Per Call","body":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","category":"refactor"},{"id":"impl-001-non-blocking-092","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Add Explicit Bounds To Recursive Default Merge","body":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","category":"refactor"},{"id":"impl-001-non-blocking-093","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Bound Plugin Config Bulk Loading","body":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","category":"refactor"},{"id":"impl-001-non-blocking-094","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid Repeated Linear Preset Lookups","body":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","category":"refactor"},{"id":"impl-001-non-blocking-095","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"5. Extract DataSource Class Normalization","body":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","category":"refactor"},{"id":"impl-001-non-blocking-096","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Cache Preset Resolution Per Call","body":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","category":"refactor"},{"id":"impl-001-non-blocking-097","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Add Explicit Bounds To Recursive Default Merge","body":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","category":"refactor"},{"id":"impl-001-non-blocking-098","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Bound Plugin Config Bulk Loading","body":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","category":"refactor"},{"id":"impl-001-non-blocking-099","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid Repeated Linear Preset Lookups","body":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","category":"refactor"},{"id":"impl-001-non-blocking-100","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"5. Extract DataSource Class Normalization","body":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","category":"refactor"},{"id":"impl-001-non-blocking-101","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Cache Preset Resolution Per Call","body":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","category":"refactor"},{"id":"impl-001-non-blocking-102","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Add Explicit Bounds To Recursive Default Merge","body":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","category":"refactor"},{"id":"impl-001-non-blocking-103","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Bound Plugin Config Bulk Loading","body":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","category":"refactor"},{"id":"impl-001-non-blocking-104","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid Repeated Linear Preset Lookups","body":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","category":"refactor"},{"id":"impl-001-non-blocking-105","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"5. Extract DataSource Class Normalization","body":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","category":"refactor"},{"id":"impl-001-non-blocking-106","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Cache Preset Resolution Per Call","body":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","category":"refactor"},{"id":"impl-001-non-blocking-107","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Add Explicit Bounds To Recursive Default Merge","body":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","category":"refactor"},{"id":"impl-001-non-blocking-108","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Bound Plugin Config Bulk Loading","body":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","category":"refactor"},{"id":"impl-001-non-blocking-109","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid Repeated Linear Preset Lookups","body":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","category":"refactor"},{"id":"impl-001-non-blocking-110","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"5. Extract DataSource Class Normalization","body":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","category":"refactor"},{"id":"impl-001-non-blocking-111","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Cache Preset Resolution Per Call","body":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","category":"refactor"},{"id":"impl-001-non-blocking-112","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Add Explicit Bounds To Recursive Default Merge","body":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","category":"refactor"},{"id":"impl-001-non-blocking-113","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Bound Plugin Config Bulk Loading","body":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","category":"refactor"},{"id":"impl-001-non-blocking-114","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid Repeated Linear Preset Lookups","body":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","category":"refactor"},{"id":"impl-001-non-blocking-115","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"5. Extract DataSource Class Normalization","body":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","category":"refactor"},{"id":"impl-001-non-blocking-116","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Reuse plugin skill directory resolution","body":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","category":"refactor"},{"id":"impl-001-non-blocking-117","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Preserve dry-run visibility for plugin migration","body":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","category":"refactor"},{"id":"impl-001-non-blocking-118","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Clarify plugin change summary semantics","body":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","category":"refactor"},{"id":"impl-001-non-blocking-119","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid duplicate unavailable-command message construction","body":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","category":"refactor"},{"id":"impl-001-non-blocking-120","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Reuse plugin skill directory resolution","body":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","category":"refactor"},{"id":"impl-001-non-blocking-121","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Preserve dry-run visibility for plugin migration","body":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","category":"refactor"},{"id":"impl-001-non-blocking-122","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Clarify plugin change summary semantics","body":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","category":"refactor"},{"id":"impl-001-non-blocking-123","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid duplicate unavailable-command message construction","body":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","category":"refactor"},{"id":"impl-001-non-blocking-124","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Reuse plugin skill directory resolution","body":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","category":"refactor"},{"id":"impl-001-non-blocking-125","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Preserve dry-run visibility for plugin migration","body":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","category":"refactor"},{"id":"impl-001-non-blocking-126","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Clarify plugin change summary semantics","body":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","category":"refactor"},{"id":"impl-001-non-blocking-127","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid duplicate unavailable-command message construction","body":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","category":"refactor"},{"id":"impl-001-non-blocking-128","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Reuse plugin skill directory resolution","body":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","category":"refactor"},{"id":"impl-001-non-blocking-129","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Preserve dry-run visibility for plugin migration","body":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","category":"refactor"},{"id":"impl-001-non-blocking-130","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Clarify plugin change summary semantics","body":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","category":"refactor"},{"id":"impl-001-non-blocking-131","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid duplicate unavailable-command message construction","body":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","category":"refactor"},{"id":"impl-001-non-blocking-132","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Reuse plugin skill directory resolution","body":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","category":"refactor"},{"id":"impl-001-non-blocking-133","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Preserve dry-run visibility for plugin migration","body":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","category":"refactor"},{"id":"impl-001-non-blocking-134","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Clarify plugin change summary semantics","body":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","category":"refactor"},{"id":"impl-001-non-blocking-135","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid duplicate unavailable-command message construction","body":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","category":"refactor"},{"id":"impl-001-non-blocking-136","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Reuse plugin skill directory resolution","body":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","category":"refactor"},{"id":"impl-001-non-blocking-137","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Preserve dry-run visibility for plugin migration","body":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","category":"refactor"},{"id":"impl-001-non-blocking-138","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Clarify plugin change summary semantics","body":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","category":"refactor"},{"id":"impl-001-non-blocking-139","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid duplicate unavailable-command message construction","body":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","category":"refactor"},{"id":"impl-001-non-blocking-140","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Reuse plugin skill directory resolution","body":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","category":"refactor"},{"id":"impl-001-non-blocking-141","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Preserve dry-run visibility for plugin migration","body":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","category":"refactor"},{"id":"impl-001-non-blocking-142","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Clarify plugin change summary semantics","body":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","category":"refactor"},{"id":"impl-001-non-blocking-143","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid duplicate unavailable-command message construction","body":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","category":"refactor"},{"id":"impl-001-non-blocking-144","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Reuse plugin skill directory resolution","body":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","category":"refactor"},{"id":"impl-001-non-blocking-145","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Preserve dry-run visibility for plugin migration","body":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","category":"refactor"},{"id":"impl-001-non-blocking-146","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Clarify plugin change summary semantics","body":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","category":"refactor"},{"id":"impl-001-non-blocking-147","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid duplicate unavailable-command message construction","body":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","category":"refactor"},{"id":"impl-001-non-blocking-148","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Reuse plugin skill directory resolution","body":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","category":"refactor"},{"id":"impl-001-non-blocking-149","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Preserve dry-run visibility for plugin migration","body":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","category":"refactor"},{"id":"impl-001-non-blocking-150","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Clarify plugin change summary semantics","body":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","category":"refactor"},{"id":"impl-001-non-blocking-151","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid duplicate unavailable-command message construction","body":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","category":"refactor"},{"id":"impl-001-non-blocking-152","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Reuse plugin skill directory resolution","body":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","category":"refactor"},{"id":"impl-001-non-blocking-153","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Preserve dry-run visibility for plugin migration","body":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","category":"refactor"},{"id":"impl-001-non-blocking-154","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Clarify plugin change summary semantics","body":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","category":"refactor"},{"id":"impl-001-non-blocking-155","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid duplicate unavailable-command message construction","body":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","category":"refactor"},{"id":"impl-001-non-blocking-156","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Reuse plugin skill directory resolution","body":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","category":"refactor"},{"id":"impl-001-non-blocking-157","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Preserve dry-run visibility for plugin migration","body":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","category":"refactor"},{"id":"impl-001-non-blocking-158","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Clarify plugin change summary semantics","body":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","category":"refactor"},{"id":"impl-001-non-blocking-159","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid duplicate unavailable-command message construction","body":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","category":"refactor"},{"id":"impl-001-non-blocking-160","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Reuse plugin skill directory resolution","body":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","category":"refactor"},{"id":"impl-001-non-blocking-161","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Preserve dry-run visibility for plugin migration","body":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","category":"refactor"},{"id":"impl-001-non-blocking-162","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Clarify plugin change summary semantics","body":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","category":"refactor"},{"id":"impl-001-non-blocking-163","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid duplicate unavailable-command message construction","body":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","category":"refactor"},{"id":"impl-001-non-blocking-164","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Reuse plugin skill directory resolution","body":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","category":"refactor"},{"id":"impl-001-non-blocking-165","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Preserve dry-run visibility for plugin migration","body":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","category":"refactor"},{"id":"impl-001-non-blocking-166","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Clarify plugin change summary semantics","body":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","category":"refactor"},{"id":"impl-001-non-blocking-167","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid duplicate unavailable-command message construction","body":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","category":"refactor"},{"id":"impl-001-non-blocking-168","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Reuse plugin skill directory resolution","body":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","category":"refactor"},{"id":"impl-001-non-blocking-169","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Preserve dry-run visibility for plugin migration","body":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","category":"refactor"},{"id":"impl-001-non-blocking-170","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Clarify plugin change summary semantics","body":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","category":"refactor"},{"id":"impl-001-non-blocking-171","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid duplicate unavailable-command message construction","body":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","category":"refactor"},{"id":"impl-001-non-blocking-172","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Reuse plugin skill directory resolution","body":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","category":"refactor"},{"id":"impl-001-non-blocking-173","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Preserve dry-run visibility for plugin migration","body":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","category":"refactor"},{"id":"impl-001-non-blocking-174","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Clarify plugin change summary semantics","body":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","category":"refactor"},{"id":"impl-001-non-blocking-175","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid duplicate unavailable-command message construction","body":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","category":"refactor"},{"id":"impl-001-non-blocking-176","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Reuse plugin skill directory resolution","body":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","category":"refactor"},{"id":"impl-001-non-blocking-177","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Preserve dry-run visibility for plugin migration","body":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","category":"refactor"},{"id":"impl-001-non-blocking-178","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Clarify plugin change summary semantics","body":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","category":"refactor"},{"id":"impl-001-non-blocking-179","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid duplicate unavailable-command message construction","body":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","category":"refactor"},{"id":"impl-001-non-blocking-180","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Reuse plugin skill directory resolution","body":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","category":"refactor"},{"id":"impl-001-non-blocking-181","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Preserve dry-run visibility for plugin migration","body":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","category":"refactor"},{"id":"impl-001-non-blocking-182","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Clarify plugin change summary semantics","body":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","category":"refactor"},{"id":"impl-001-non-blocking-183","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid duplicate unavailable-command message construction","body":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","category":"refactor"},{"id":"impl-001-non-blocking-184","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Reuse plugin skill directory resolution","body":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","category":"refactor"},{"id":"impl-001-non-blocking-185","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Preserve dry-run visibility for plugin migration","body":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","category":"refactor"},{"id":"impl-001-non-blocking-186","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Clarify plugin change summary semantics","body":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","category":"refactor"},{"id":"impl-001-non-blocking-187","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid duplicate unavailable-command message construction","body":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","category":"refactor"},{"id":"impl-001-non-blocking-188","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Reuse plugin skill directory resolution","body":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","category":"refactor"},{"id":"impl-001-non-blocking-189","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Preserve dry-run visibility for plugin migration","body":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","category":"refactor"},{"id":"impl-001-non-blocking-190","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Clarify plugin change summary semantics","body":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","category":"refactor"},{"id":"impl-001-non-blocking-191","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid duplicate unavailable-command message construction","body":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","category":"refactor"},{"id":"impl-001-non-blocking-192","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Reuse plugin skill directory resolution","body":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","category":"refactor"},{"id":"impl-001-non-blocking-193","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Preserve dry-run visibility for plugin migration","body":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","category":"refactor"},{"id":"impl-001-non-blocking-194","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Clarify plugin change summary semantics","body":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","category":"refactor"},{"id":"impl-001-non-blocking-195","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid duplicate unavailable-command message construction","body":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","category":"refactor"},{"id":"impl-001-non-blocking-196","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid duplicate unavailable-command message construction","body":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","category":"refactor"},{"id":"impl-001-non-blocking-197","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Reuse plugin skill directory resolution","body":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","category":"refactor"},{"id":"impl-001-non-blocking-198","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Preserve dry-run visibility for plugin migration","body":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","category":"refactor"},{"id":"impl-001-non-blocking-199","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Clarify plugin change summary semantics","body":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","category":"refactor"},{"id":"impl-001-non-blocking-200","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"4. Avoid duplicate unavailable-command message construction","body":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","category":"refactor"},{"id":"impl-001-non-blocking-201","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"1. Reuse plugin skill directory resolution","body":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","category":"refactor"},{"id":"impl-001-non-blocking-202","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"2. Preserve dry-run visibility for plugin migration","body":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","category":"refactor"},{"id":"impl-001-non-blocking-203","phase":"impl","sourceArtifact":"impl-review.json","attempt":1,"severity":"non-blocking","title":"3. Clarify plugin change summary semantics","body":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","category":"refactor"}]}},{"attempt":2,"legacySource":"impl-review.json","detail":{"version":1,"phase":"impl","generatedAt":"2026-06-09T11:02:27.261Z","verdict":"ADVISORY","summary":{"blocking":0,"nonBlocking":203,"total":203},"blockingFindings":[],"nonBlockingImprovements":[{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Cache Preset Resolution Per Call","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** `resolveChain()` calls `allPresets(projectRoot)` repeatedly, including inside the parent traversal loop. `allPresets()` can load the plugin registry, so parent chains may trigger repeated registry work.\n**Suggestion:** Compute once at the start of `resolveChain()` and reuse a `Map` by key for leaf and parent lookup.","rationale":"Loop review proposal."},{"title":"2. Add Explicit Bounds To Recursive Default Merge","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `mergeMissing()` recursively merges plugin defaults without an explicit depth bound. This violates the `bounded-resource-usage` guardrail for recursive processing.\n**Suggestion:** Add a max merge depth, for example `mergeMissing(target, defaults, depth = 0)` with a constant such as `MAX_PLUGIN_DEFAULT_DEPTH`, and fail clearly when exceeded.","rationale":"Loop review proposal."},{"title":"3. Bound Plugin Config Bulk Loading","failureMode":"refactor","file":"src/lib/config.js","issue":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","suggestion":"**File:** `src/lib/config.js`\n**Issue:** `loadEnabledPluginConfig()` reads schema/default files for every enabled package with no explicit count or file-size bounds. A config with many packages or large contribution files can cause unbounded bulk loading.\n**Suggestion:** Introduce limits such as `MAX_PLUGIN_PACKAGES`, `MAX_PLUGIN_CONFIG_FILE_BYTES`, and validate before reading/parsing contribution files.","rationale":"Loop review proposal."},{"title":"4. Avoid Repeated Linear Preset Lookups","failureMode":"refactor","file":"src/lib/presets.js","issue":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","suggestion":"**File:** `src/lib/presets.js`\n**Issue:** Preset lookups use repeated `.find()` calls over arrays. With plugin presets added, this pattern becomes noisier and less efficient.\n**Suggestion:** Build a `Map` once in `allPresets()` or per resolver call, then use `presetByKey.get(key)` for project overlay, leaf, parent, and safe fallback lookups.","rationale":"Loop review proposal."},{"title":"5. Extract DataSource Class Normalization","failureMode":"refactor","file":"src/docs/lib/data-source-loader.js","issue":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","suggestion":"**File:** `src/docs/lib/data-source-loader.js`\n**Issue:** The inline `Function.prototype.toString` class detection makes `loadDataSources()` harder to read and bakes a brittle distinction into the loop.\n**Suggestion:** Extract a helper such as `resolveDataSourceClass(Source, container, filePath)` that handles class-vs-factory normalization and validation in one place.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."},{"title":"4. Avoid duplicate unavailable-command message construction","failureMode":"refactor","file":"src/senti.js","issue":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","suggestion":"**File:** `src/senti.js`\n**Issue:** The unknown-command branch builds two similar “command unavailable” messages, one for normal unresolved commands and one for plugin resolution failures.\n**Suggestion:** Extract the common message prefix or use a small helper like `printUnavailableCommand(subCmd, reason)` so future wording changes stay consistent.","rationale":"Loop review proposal."},{"title":"1. Reuse plugin skill directory resolution","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `pluginSkillSourceDirs(root)` loads the plugin registry twice, once before deploying plugin skills and again before cleanup. This duplicates registry IO and can make the two operations see different state if registry contents change mid-upgrade.\n**Suggestion:** Resolve once after plugin enablement, e.g. `const pluginSkillsDirs = pluginSkillSourceDirs(root);`, then reuse it for both `deploySkillsFromDir` and `cleanupObsoleteSkills`.","rationale":"Loop review proposal."},{"title":"2. Preserve dry-run visibility for plugin migration","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** Official preset/workflow plugin enablement is skipped entirely under `dryRun`, so dry-run output cannot report that upgrade would enable those plugins. This makes migration behavior less transparent.\n**Suggestion:** Compute whether each official plugin is needed regardless of `dryRun`, set a dry-run summary/log message for pending plugin changes, and only guard the filesystem-writing `ensureOfficialPackage(...)` calls with `!dryRun`.","rationale":"Loop review proposal."},{"title":"3. Clarify plugin change summary semantics","failureMode":"refactor","file":"src/upgrade.js","issue":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","suggestion":"**File:** `src/upgrade.js`\n**Issue:** `summary.plugins.changed` is a boolean while other summary sections track counts. The name also conflates “would change” and “did change,” especially once dry-run behavior is represented.\n**Suggestion:** Rename or structure it more explicitly, such as `summary.plugins.enabled = 0` or `summary.plugins.changedCount`, and increment only when a plugin is newly enabled or would be enabled in dry-run.","rationale":"Loop review proposal."}],"excluded":{"missingFile":0,"outOfScope":0},"contractSummary":{"targetStep":"impl-review","artifactPath":"specs/284-plugin-mechanism-workflow-presets/impl-review.json","verdict":"ADVISORY","result":"ADVISORY","blockingCount":0,"failureKind":null,"nextAction":"impl-gate","completionKind":"normal","progressSignature":"9f3b158330607d5939393fcc91d3660d4d424ee51465108eeea85312fd14ce68"}}}]}
