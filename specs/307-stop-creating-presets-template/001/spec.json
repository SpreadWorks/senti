{
  "goal": "Stop `senti upgrade` from generating unused project-local `.senti/templates/<lang>/docs/creating_presets.md` files while preserving base preset managed-copy syncing.",
  "scope": {
    "in": [
      "Remove automatic creation or update of `.senti/templates/<lang>/docs/creating_presets.md` from the `deployPresetCopies()` upgrade path.",
      "Preserve managed-copy syncing from `src/presets/base/guardrail.json` to `.senti/presets/base/guardrail.json`.",
      "Preserve managed-copy syncing from `src/presets/base/guardrail-rewrite-rubric.md` to `.senti/presets/base/guardrail-rewrite-rubric.md`.",
      "Restrict guardrail rewrite rubric managed-copy behavior to the base preset copy path.",
      "Prevent full `senti upgrade` rename migration from modifying existing `.senti/templates/*/docs/creating_presets.md` files.",
      "Add spec-local regression coverage for `deployPresetCopies()` generation targets and user-owned `creating_presets.md` handling."
    ],
    "out": [
      "Do not change `creating_presets.md` content in `docs/` or any packaged preset documentation.",
      "Do not change `guardrail-rewrite-rubric.md` content.",
      "Do not change preset chapter validation semantics.",
      "Do not add external dependencies.",
      "Do not publish npm packages or change npm dist-tags."
    ]
  },
  "constraints": [
    "Use only Node.js built-in modules and existing project helpers.",
    "`src/` code must remain generic package code and must not contain project-specific paths or assumptions.",
    "Existing project-local `.senti/templates/*/docs/creating_presets.md` files are user-owned; upgrade must not overwrite, append to, delete, or otherwise manage them.",
    "Spec-local tests must live under `specs/307-stop-creating-presets-template/tests/` and include `// spec: R<N>` headers.",
    "Because this spec is expected to modify `src/lib` and spec-local tests only, `senti upgrade` is not required unless implementation changes `src/skills/`, `src/presets/`, or preset template sources."
  ],
  "design_principles": [
    "Fix the generated target set instead of suppressing validation warnings.",
    "Keep `deployPresetCopies()` as the owner for base preset managed copies.",
    "Treat project-local documentation templates as user-owned customization points once they exist."
  ],
  "overview": {
    "modules": [
      {
        "text": "`src/lib/preset-deploy.js` owns copying packaged preset artifacts into `.senti/presets/<key>/` and currently also appends rubric guidance into project-local `.senti/templates/<lang>/docs/creating_presets.md`."
      },
      {
        "text": "`src/upgrade.js` calls `deployPresetCopies(root, { presetKeys: [\"base\"], languages })` during non-dry-run upgrade."
      },
      {
        "text": "`src/upgrade.js` also runs `RenameMigration` before preset copy deployment; that migration is a second upgrade write path for text files."
      },
      {
        "text": "`src/presets/base/preset.json` defines the base chapters as `overview.md`, `stack_and_ops.md`, `project_structure.md`, and `development.md`; `creating_presets.md` is not a base chapter."
      },
      {
        "text": "`src/lib/presets.js` validation warns about project-local docs templates that are not included in effective chapters; that behavior stays unchanged."
      }
    ],
    "data_flow": [
      {
        "text": "`senti upgrade` resolves configured docs languages, then calls `deployPresetCopies()` for the base preset."
      },
      {
        "text": "After this change, `deployPresetCopies()` writes only managed preset copies for the requested base preset artifacts and returns only those written paths."
      },
      {
        "text": "Project-local `.senti/templates/<lang>/docs/creating_presets.md` files remain outside both the preset-copy write path and rename-migration text rewrite path, so validation can no longer report that file due to a newly generated or modified upgrade artifact."
      }
    ],
    "decisions": [
      {
        "text": "[VERIFY] checked `deployPresetCopies()`; result=match with reported bug.",
        "evidence": "`src/lib/preset-deploy.js` copies `guardrail.json`, copies `guardrail-rewrite-rubric.md`, then loops over languages and calls `upsertText()` for `.senti/templates/<lang>/docs/creating_presets.md`."
      },
      {
        "text": "[VERIFY] checked base chapters; result=match with validation warning cause.",
        "evidence": "`src/presets/base/preset.json` lists `overview.md`, `stack_and_ops.md`, `project_structure.md`, and `development.md`; it does not list `creating_presets.md`."
      },
      {
        "text": "[VERIFY] checked upgrade call site; result=match with standard upgrade surface.",
        "evidence": "`src/upgrade.js` calls `deployPresetCopies(root, { presetKeys: [\"base\"], languages: config.docs?.languages?.length ? config.docs.languages : [\"en\", \"ja\"] })` when not dry-run."
      },
      {
        "text": "[VERIFY] checked rename migration; result=match with second write path.",
        "evidence": "`src/upgrade.js` calls `new RenameMigration(root).run({ dryRun })` before preset deployment. `RenameMigration.listTextTargets()` walks project files and currently does not exclude `.senti/templates/*/docs/creating_presets.md`."
      },
      {
        "text": "[CORRECTION] remove project-local template generation from the managed-copy helper.",
        "evidence": "Issue #401 acceptance criteria require no new `.senti/templates/<lang>/docs/creating_presets.md` after upgrade and require existing project-local files to be excluded from automatic generation and automatic updates."
      },
      {
        "text": "[VERIFY] migration parity inventory and mapping.",
        "evidence": "Retained surfaces: `.senti/presets/base/guardrail.json` and `.senti/presets/base/guardrail-rewrite-rubric.md`, still owned by `deployPresetCopies()`. Removed side effect: automatic create/update of `.senti/templates/<lang>/docs/creating_presets.md`, with no replacement owner because existing files are user-owned."
      }
    ]
  },
  "background": "Issue #401 identifies a mismatch between `deployPresetCopies()` generated outputs and base preset chapters. The helper currently creates a project-local `creating_presets.md` file for every configured language, but the base preset does not include that chapter. Existing validation then warns that the generated file is not listed in chapters. The fix is to remove that generated project-local template side effect while keeping the base preset guardrail managed copies that standard upgrade still needs.",
  "requirements": [
    {
      "id": "R1",
      "desc": "`deployPresetCopies(workRoot, { presetKeys: [\"base\"], languages })` must not create, update, or return `.senti/templates/<lang>/docs/creating_presets.md` for any configured language.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R2",
      "desc": "If `.senti/templates/<lang>/docs/creating_presets.md` already exists before full `senti upgrade` or `deployPresetCopies()` runs, its file contents must remain unchanged after the operation, including when legacy rename tokens are present.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R3",
      "desc": "`deployPresetCopies(workRoot, { presetKeys: [\"base\"] })` must continue to create or update `.senti/presets/base/guardrail.json` with content equal to `src/presets/base/guardrail.json`.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R4",
      "desc": "`deployPresetCopies(workRoot, { presetKeys: [\"base\"] })` must continue to create or update `.senti/presets/base/guardrail-rewrite-rubric.md` with content equal to `src/presets/base/guardrail-rewrite-rubric.md`.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R5",
      "desc": "`deployPresetCopies()` must not copy `guardrail-rewrite-rubric.md` for non-base preset keys; the only rubric managed copy path in this change is `.senti/presets/base/guardrail-rewrite-rubric.md`.",
      "priority": "should",
      "status": "done"
    },
    {
      "id": "R6",
      "desc": "Regression tests must prove R1 through R5 using spec-local coverage under `specs/307-stop-creating-presets-template/tests/`.",
      "priority": "must",
      "status": "done"
    }
  ],
  "acceptance_criteria": [
    "AC1: A fresh temporary project calling `deployPresetCopies(tmp, { presetKeys: [\"base\"], languages: [\"ja\", \"en\"] })` has no `.senti/templates/ja/docs/creating_presets.md` and no `.senti/templates/en/docs/creating_presets.md` after the call.",
    "AC2: The result array from that call contains `.senti/presets/base/guardrail.json` and `.senti/presets/base/guardrail-rewrite-rubric.md`, and contains no path ending in `templates/<lang>/docs/creating_presets.md`.",
    "AC3: The generated `.senti/presets/base/guardrail.json` content equals `src/presets/base/guardrail.json`.",
    "AC4: The generated `.senti/presets/base/guardrail-rewrite-rubric.md` content equals `src/presets/base/guardrail-rewrite-rubric.md`.",
    "AC5: A pre-existing `.senti/templates/ja/docs/creating_presets.md` file keeps exactly the same content after `deployPresetCopies()` runs.",
    "AC6: A non-base preset key with a `guardrail-rewrite-rubric.md` fixture or mock source does not produce a `.senti/presets/<non-base>/guardrail-rewrite-rubric.md` managed copy.",
    "AC7: A pre-existing `.senti/templates/ja/docs/creating_presets.md` file containing a legacy rename token keeps exactly the same content after `RenameMigration` or full upgrade migration runs.",
    "AC8: No tests or production code change preset chapter validation semantics."
  ],
  "clarifications": [
    {
      "q": "Should existing project-local `creating_presets.md` files be deleted by upgrade?",
      "a": "No. Existing project-local files are user-owned and must remain unchanged."
    },
    {
      "q": "Should the validation warning be suppressed instead?",
      "a": "No. The Issue marks preset chapter validation changes as out of scope; the generated target set must be corrected."
    },
    {
      "q": "Is `senti upgrade` required for this implementation?",
      "a": "Only if implementation changes `src/skills/`, `src/presets/`, or preset template sources. The planned change is in `src/lib/preset-deploy.js` and spec-local tests."
    }
  ],
  "alternatives_considered": [
    {
      "option": "Keep generating project-local `creating_presets.md` and suppress the validation warning.",
      "reason": "Rejected because it changes preset chapter validation semantics, which Issue #401 lists as out of scope."
    },
    {
      "option": "Move `creating_presets.md` into base preset chapters.",
      "reason": "Rejected because the Issue does not request a new base documentation chapter and explicitly excludes improving `creating_presets.md` content."
    },
    {
      "option": "Continue appending rubric notes to existing project-local `creating_presets.md` files only.",
      "reason": "Rejected because Issue #401 requires existing project-local files to be excluded from automatic updates."
    }
  ],
  "open_questions": [],
  "keywords": [
    "deployPresetCopies",
    "preset-deploy",
    "creating_presets.md",
    "senti upgrade",
    "project-local templates",
    "guardrail-rewrite-rubric",
    "guardrail.json",
    "base preset",
    "validatePresetChain",
    "preset chapters",
    "managed copy",
    "user-owned file"
  ],
  "implementationTargets": [
    "src/lib/preset-deploy.js",
    "src/upgrade.js",
    "specs/307-stop-creating-presets-template/tests/"
  ],
  "tasks": [
    {
      "id": "T-1",
      "title": "Update preset copy targets",
      "goal": "Change `deployPresetCopies()` so it only manages base preset copy outputs needed by upgrade and no longer manages project-local `creating_presets.md` files.",
      "acceptance": [
        "R1 is satisfied.",
        "R3 and R4 remain satisfied.",
        "R5 is satisfied for non-base preset keys."
      ],
      "implementation_notes": "Remove the language loop and note-upsert behavior for `.senti/templates/<lang>/docs/creating_presets.md`. Keep copy behavior for base `guardrail.json` and base `guardrail-rewrite-rubric.md`; do not alter preset validation.",
      "test_strategy": "Covered by the spec-local deployPresetCopies regression test.",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending",
      "key": "T-1"
    },
    {
      "id": "T-2",
      "title": "Add deploy regression tests",
      "goal": "Add spec-local tests that verify the exact generated target set and user-owned `creating_presets.md` behavior.",
      "acceptance": [
        "R6 is satisfied.",
        "AC1 through AC7 are covered by assertions."
      ],
      "implementation_notes": "Use `node:test`, Node built-ins, and existing helper patterns. Import `deployPresetCopies()` from the production source. Include `// spec: R<N>` headers in each spec-local test file.",
      "test_strategy": "Run the new spec-local test file directly with `node --test` and include it in the flow test artifacts.",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending",
      "key": "T-2"
    },
    {
      "id": "T-3",
      "title": "Skip user template migration",
      "goal": "Change the upgrade rename migration path so existing project-local `creating_presets.md` files are not rewritten during full `senti upgrade`.",
      "acceptance": [
        "R2 is satisfied for the rename migration path.",
        "AC7 is covered by spec-local assertions."
      ],
      "implementation_notes": "Exclude `.senti/templates/*/docs/creating_presets.md` from `RenameMigration` text target rewriting. Keep the exclusion narrow so other migration targets continue to work.",
      "test_strategy": "Add a spec-local assertion that a legacy rename token inside `.senti/templates/ja/docs/creating_presets.md` remains unchanged after the migration path runs.",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending",
      "key": "T-3"
    }
  ],
  "user_approval": {
    "approved": true,
    "confirmed_at": "2026-06-18T16:29:22.398Z",
    "notes": "auto-approved after spec-gate PASS for Issue #401"
  },
  "specId": "307-stop-creating-presets-template"
}
