{
  "goal": "spec 236 retro で partial 判定された R4a と R8c を完了し、definition.js を flow 構造の完全な single source of truth にする。gate PASS 副作用の実行と gate phase マッピングの両方を definition のノード属性から駆動する。",
  "scope": {
    "in": [
      "[must] registry.js gate post hook から completeTask/promoteNextPending/mergeOverview ロジックを撤去",
      "[must] definition の sideEffects を参照する gate 副作用実行ロジックの新設",
      "[must] definition.js の gate ノードに gatePhase 属性を追加",
      "[must] gate-step.js の PHASE_TO_STEP_ENTRIES を definition 走査から導出に変更",
      "[should] 既存テストの更新"
    ],
    "out": [
      "retro R1 (fallbacks 属性��追加)",
      "retro R6 (MAX_REVIEW_RETRIES 撤���)",
      "retro R12 (SKILL.md の手動 step 遷移指示の撤去)",
      "definition.js のトラバーサルヘルパーの新規追加（既存APIで十分）"
    ]
  },
  "constraints": [
    "gate-step.js の外部 API シグネチャ (resolveGateStepId, STEP_TO_PHASE, resolveGatePhaseFromState) は変更しない",
    "registry.js の gate post hook 自体は残す（tryUpdateStepStatus, updateGateRetryCounter, appendIssueLogFromGateResult は gate 固有の registry 責務）",
    "外部依存なし（Node.js 組み込みのみ）"
  ],
  "design_principles": [
    "definition.js が flow 構造の single source of truth",
    "宣言的属性から振る舞いを導出するパターン（set-step.js の collectSideEffects() に倣う）"
  ],
  "overview": {
    "modules": [
      {
        "text": "src/flow/definition.js — FlowNode に gatePhase 属性を追加。gate 系ノードに phase 配列を宣言。collectGatePhaseEntries() を export"
      },
      {
        "text": "src/flow/lib/gate-step.js — PHASE_TO_STEP_ENTRIES を collectGatePhaseEntries() からの導出に置換。外部 API (resolveGateStepId, STEP_TO_PHASE, resolveGatePhaseFromState) は不変"
      },
      {
        "text": "src/flow/lib/run-gate.js — gate PASS 時に definition の sideEffects を読み取り、対応する副作用を実行する関数を追加"
      },
      {
        "text": "src/flow/registry.js — gate post hook から completeTask/promoteNextPending/mergeOverview の hardcoded ロジックを撤去。sideEffects 実行を run-gate.js に委譲"
      }
    ],
    "data_flow": [
      {
        "text": "gate 実行 → run-gate.js が result=pass を検出 → deriveNextAction() で sideEffects を取得 → 各副作用を順次実行"
      },
      {
        "text": "モジュール初期化時 → collectGatePhaseEntries() が FLOW_DEFINITION + TASK_DEFINITION を走査 → [phase, stepId] ペアを返す → gate-step.js が PHASE_TO_STEP / STEP_TO_PHASE を構築"
      }
    ],
    "decisions": [
      {
        "text": "gate 副作用実行を run-gate.js に配置する。gate 結果の pass/fail 判定が必要であり、step 遷移時の副作用 (set-step.js) とはトリガー条件が異なるため。",
        "evidence": "registry.js:356 の条件は result?.result === 'pass' && phase === 'task-impl' であり gate 固有のコンテキストが必要。set-step.js:47 は status==='done' 時のみで gate 結果には関与しない。"
      },
      {
        "text": "gatePhase を配列型にする。FLOW_DEFINITION gate-impl は task-impl と integration の2つの phase に対応する既存事実があるため。",
        "evidence": "gate-step.js:13-14 に task-impl→gate-impl, integration→gate-impl の2エントリ。1 step ID : N phase のマッピング。"
      },
      {
        "text": "collectGatePhaseEntries() を definition.js に追加し、gate-step.js から呼ぶ。definition のトラバーサルは definition.js に集約する既存パターンに従う。",
        "evidence": "collectLeafIds(), derivePhaseMap(), resolveNodeFor() など全トラバーサルヘルパーが definition.js に集約されている。"
      }
    ]
  },
  "background": "spec 236 で definition.js を新設し flow 構造の single source of truth とする目標を掲げた。retro で15要件中10件が done、5件が partial と判定された。本 spec は partial 残件のうち R4a（gate PASS 副作用の definition 駆動化）と R8c（PHASE_TO_STEP の definition 派生）の2件を完了する。どちらも definition に属性は宣言済みだが実行側の移行が未達という共通パターン��",
  "requirements": [
    {
      "id": "R1",
      "priority": "must",
      "desc": "FlowNode コンストラクタに gatePhase 属性（文字列配列 or null）を追加する。gate 系ノードには対応する phase の配列を設定する: gate-draft=['draft'], gate=['spec'], gate-impl=['task-impl','integration'] (FLOW), gate-impl=['task-spec'] (TASK)。非 gate ノードは null。",
      "status": "done"
    },
    {
      "id": "R2",
      "priority": "must",
      "desc": "definition.js に collectGatePhaseEntries() を export する。FLOW_DEFINITION と TASK_DEFINITION を走査し、gatePhase が非 null のノードから [phase, stepId] ペアの配列を返す。返り値の順序は定義順。",
      "status": "done"
    },
    {
      "id": "R3",
      "priority": "must",
      "desc": "gate-step.js の PHASE_TO_STEP_ENTRIES を collectGatePhaseEntries() の戻り値から構築する。hardcoded 配列を削除する。外部 API (resolveGateStepId, STEP_TO_PHASE, resolveGatePhaseFromState) のシグネチャと戻り値は変更しない。",
      "status": "done"
    },
    {
      "id": "R4",
      "priority": "must",
      "desc": "run-gate.js に gate PASS 時の副作用実行関数を追加する。gate 結果が pass のとき、deriveNextAction() で取得した sideEffects を読み取り、各副作用（completeTask, promoteNextTask, mergeOverview）を実行する。",
      "status": "done"
    },
    {
      "id": "R5",
      "priority": "must",
      "desc": "registry.js の gate post hook から、task-impl PASS 時の completeTask / promoteNextPending / mergeOverview ロジック（現在の L353-377）を撤去する。副作用実行を run-gate.js の関数に委譲する。",
      "status": "done"
    },
    {
      "id": "R6",
      "priority": "should",
      "desc": "���存テスト（gate-step, run-gate, registry 関連）が全てパスする。PHASE_TO_STEP の導出が hardcoded 版と同一結果であることを検証するテストを追加する。",
      "status": "done"
    }
  ],
  "acceptance_criteria": [
    "gate-step.js に hardcoded の PHASE_TO_STEP_ENTRIES 配列が存在しない",
    "registry.js の gate post hook に completeTask / promoteNextPending / mergeOverview のロジックが存在しない",
    "definition.js の gate 系ノードに gatePhase 属性が宣���されている",
    "npm test が全件パスする",
    "resolveGateStepId / STEP_TO_PHASE / resolveGatePhaseFromState の戻り値が変更前と同一"
  ],
  "clarifications": [
    {
      "q": "既存機能への影響は？",
      "a": "本 spec は内部リファクタのみ。gate-step.js の外部 API (resolveGateStepId, STEP_TO_PHASE, resolveGatePhaseFromState) の戻り値は不変。registry.js gate post hook の step status 更新・gateRetry カウンタ・issue-log 記録は変更なし。副作用ロジックの移動先 (run-gate.js) は registry post hook から呼ばれるため、呼び出しフローの外部観測可能な振る舞いは同一。CLI コマンドのインターフェース変更なし。"
    }
  ],
  "alternatives_considered": [
    {
      "option": "set-step.js に gate 副作用を統合する",
      "reason": "gate PASS 副作用は gate 結果 (pass/fail) と phase を前提条件とし、step の done 遷移とは異なるトリガー条件。責務が混在する。"
    },
    {
      "option": "action='run-gate' でフィルタし gatePhase を省略する",
      "reason": "gate ノードの ID から phase を推測する必要があり、gate→spec の推測が非自明。明示的属性のほうが確実���"
    },
    {
      "option": "phase ごとに別ノードを定義する（gate-impl を task-impl 用と integration 用に分離）",
      "reason": "同一ステップ ID に対する定義の重複が発生し、definition が冗長になる。"
    }
  ],
  "open_questions": [],
  "tasks": [
    {
      "id": "T-pending-spec",
      "title": "Pending spec definition",
      "goal": "Placeholder task until spec.json tasks[] is populated.",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending",
      "key": "T-pending-spec"
    },
    {
      "id": "T-1",
      "title": "Add gatePhase attribute and collectGatePhaseEntries to definition.js",
      "goal": "FlowNode に gatePhase 属性を追加し、gate 系ノードに phase 配列を宣言する。collectGatePhaseEntries() で定義を走査して [phase, stepId] ペアを返す。",
      "acceptance": [
        "FlowNode コンストラクタが gatePhase パラメータを受け取り、配列または null として格納する",
        "FLOW_DEFINITION の gate-draft, gate, gate-impl に正しい gatePhase が宣言されている",
        "TASK_DEFINITION の gate-impl に gatePhase=['task-spec'] が宣言されている",
        "collectGatePhaseEntries() が [['draft','gate-draft'],['spec','gate'],['task-impl','gate-impl'],['integration','gate-impl'],['task-spec','gate-impl']] と等価な結果を返す"
      ],
      "implementation_notes": "gatePhase は Object.freeze([...gatePhase]) で不変化する。collectGatePhaseEntries() は既存の collectLeafIds() と同様のトラバーサルパターン。FLOW_DEFINITION と TASK_DEFINITION の両方を走査する。",
      "test_strategy": "unit: collectGatePhaseEntries() の戻り値が期待配列と一致するテスト。非 gate ノードの gatePhase が null であるテスト。",
      "parent": null,
      "origin": "plan",
      "added_round": 1,
      "status": "pending",
      "key": "T-1"
    },
    {
      "id": "T-2",
      "title": "Derive PHASE_TO_STEP from definition in gate-step.js",
      "goal": "gate-step.js の hardcoded PHASE_TO_STEP_ENTRIES を collectGatePhaseEntries() からの導出に置換する。",
      "acceptance": [
        "gate-step.js の PHASE_TO_STEP_ENTRIES が collectGatePhaseEntries() の戻り値から構築されている",
        "hardcoded の配列リテラルが削除されている",
        "resolveGateStepId / STEP_TO_PHASE / resolveGatePhaseFromState の戻り値が変更前と同一",
        "既存の gate-step テストが全てパスする"
      ],
      "implementation_notes": "PHASE_TO_STEP_ENTRIES = Object.freeze(collectGatePhaseEntries()) に置換。PHASE_TO_STEP, STEP_TO_PHASE, TASK_STEP_TO_PHASE の導出ロジックは変更不要（入力が同一のため）。",
      "test_strategy": "unit: PHASE_TO_STEP_ENTRIES が definition と一致することの検証テスト。既存 gate-step テストの回帰確認。",
      "parent": null,
      "origin": "plan",
      "added_round": 1,
      "status": "pending",
      "key": "T-2"
    },
    {
      "id": "T-3",
      "title": "Move gate PASS side effects from registry to run-gate",
      "goal": "registry.js gate post hook の completeTask/promoteNextPending/mergeOverview ロジックを run-gate.js に移動し、definition の sideEffects 属性から駆動する。",
      "acceptance": [
        "run-gate.js に gate PASS 副作用を実行する関数が存在する",
        "当該関数は deriveNextAction() から sideEffects を取得し、completeTask / promoteNextTask / mergeOverview を条件付き実行する",
        "registry.js gate post hook から task-impl PASS 時の副作用ロジック (L353-377) が撤去されている",
        "registry.js gate post hook は副作用実行を run-gate.js に委譲する",
        "既存の gate 関連テストが全てパスする"
      ],
      "implementation_notes": "副作用実行関数は sideEffects 配列をイテレートし、各キーに対応する処理を dispatch する。completeTask は flowManager.completeTask()、promoteNextTask は promoteNextPending()、mergeOverview は RunUpdateOverviewCommand を呼ぶ。registry post hook は result/phase/ctx を渡して当該関数を呼ぶだけになる。",
      "test_strategy": "unit: sideEffects が空のとき副作用が実行されないテスト。sideEffects に completeTask を含むとき completeTask が呼ばれるテスト。既存 registry テストの回帰確認。",
      "parent": null,
      "origin": "plan",
      "added_round": 1,
      "status": "pending",
      "key": "T-3"
    }
  ],
  "user_approval": {
    "approved": true,
    "confirmed_at": "2026-04-27T23:57:35.479Z",
    "notes": "autoApprove"
  },
  "specId": "237-definition-driven-gate-ops"
}
