# T-3: Verify conflict recovery

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add behavior-level spec-local and worktree CLI/E2E coverage for the complete normal conflict recovery path.

## Acceptance Criteria
- Tests use an actually conflicting base branch and feature worktree.
- Tests cover evidence commit, clean rebase preparation, manual rebase, retry, sync, and cleanup.
- Spec-local files tag R1-R6 with required headers.

## Implementation Notes
Retain shared regression tests and add spec-local coverage rather than substituting one for the other.

## Test Strategy
Run spec-local tests plus targeted shared finalize unit and CLI/E2E tests.

---
Status: pending | Parent: (root) | Added Round: 0
