# T-5: Update tests/unit/flow/gate-repeat-escalate.test.js for new comparison

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
既存の tests/unit/flow/gate-repeat-escalate.test.js を新方式 (normalize / jaccard / 新 assertNoRepeatedFail) に対応する形に更新する。古い normalizeReason describe ブロックを削除、新 normalize / jaccard / 新 assertNoRepeatedFail ブロックを追加。spec 固有の網羅検証は specs/253-*/tests/ に分離する (T-5b)。

## Acceptance Criteria
- 古い normalizeReason describe ブロックが削除されている
- 新 normalize / jaccard / findPreviousFailedEvaluations / assertNoRepeatedFail のテストブロックが追加されている
- buildFailedEvaluations / appendIssueLogFromGateResult の既存テストブロックは保持 (永続スキーマ非変更を担保)
- test ファイル全体が新実装でパスする

## Implementation Notes
境界テスト (jaccard ちょうど 0.5) は浮動小数点誤差考慮 (3 vs 3 で 2 共通 → 2/4=0.5)。同点処理 test は 2 prior に同じ reason を仕込んで oldest が priorReason に入ることを検証。

## Test Strategy
unit (本タスクが test 自身)

---
Status: pending | Parent: (root) | Added Round: 0
