# T-6: Refresh prompts

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Update prompt instructions so agents delegate semantic retry exhaustion to acceptance-review instead of stopping in review, gate, and test-review prompts.

## Acceptance Criteria
- R8 plan and impl prompt files no longer instruct STOP for deferrable semantic retry-limit cases.
- R8 prompt text still tells agents to stop for structured non-semantic failures that require recovery or override evidence.
- senti upgrade is run if changed source prompt/template files require propagation.

## Implementation Notes
Search all prompt files for REVIEW_MAX_ATTEMPTS_EXCEEDED, GATE_MAX_ATTEMPTS, ESCALATE_RETRY_EXHAUSTED, retry limit, and STOP. Keep prompt guidance placement tests updated if shared prompt guidance moves.

## Test Strategy
Static tests for prompt text and existing instruction coverage tests. Run senti upgrade when required by changed source paths.

---
Status: pending | Parent: (root) | Added Round: 0
