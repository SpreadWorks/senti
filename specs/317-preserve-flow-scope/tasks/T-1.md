# T-1: Pin flow-level mutation scope

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Make every Issue #430 flow-level mutation boundary select the top-level step explicitly while retaining task routing, header validation, and target guards.

## Acceptance Criteria
- R1 and R3 pass `taskId: null` at the listed flow-level production boundaries without changing FlowStore or CLI syntax.
- R2 and R4 derive runtime metadata scope from the resolved step so task-impl, task-review, and task-gate remain scoped to the selected task.
- R5 target mismatches stop before lifecycle mutation.
- AC1-AC7 are covered by focused shared unit tests and spec-local tests.

## Implementation Notes
Keep production changes limited to `set-step.js`, `dispatcher.js`, and `registry.js`. Reuse existing fixture helpers where setup repeats. Do not introduce a new scope abstraction unless two or more edited call sites require identical non-trivial selection logic.

## Test Strategy
Add `// spec: R1 R2 R3 R4 R5` behavior coverage under the spec tests. Extend `set-step.test.js` for active-task/header paths, `dispatcher.test.js` for runtime metadata options, and related registry lifecycle suites for flow/task routing. Run focused unit tests, configured targeted regression, and final full regression through Senti.

---
Status: pending | Parent: (root) | Added Round: 0
