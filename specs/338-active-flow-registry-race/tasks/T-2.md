# T-2: Cover registry preservation regression

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add requirement-mapped regression coverage for concurrent active flows so pointer preservation and existing recovery behavior are protected from future lifecycle changes.

## Acceptance Criteria
- Spec-local coverage maps headers R1 through R5 to the registry regression scenarios.
- The test matrix covers multi-flow success, all R4 failure boundaries, one-entry accept_risk_and_continue, and guarded parked-resume restoration.

## Implementation Notes
Place new spec-local evidence under specs/338-active-flow-registry-race/tests/. Extend a shared acceptance-decision test when src/flow/lib/acceptance-review-artifacts.js behavior changes, or a shared registry test when src/lib/active-flow-registry.js behavior changes; otherwise keep the coverage spec-local. Do not replace spec-local coverage with shared tests.

## Test Strategy
During test-execute, run the spec-local Node tests and the shared tests that directly exercise the changed acceptance lifecycle or registry authority. The downstream final-regression step runs the full npm test command before finalize.

---
Status: pending | Parent: (root) | Added Round: 0
