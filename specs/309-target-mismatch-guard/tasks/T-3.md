# T-3: Cover retained behavior

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add regression coverage proving mismatch stops have no side effects and target-matched retained behavior still works.

## Acceptance Criteria
- Mismatch test proves get-next-action is not executed for another active flow.
- Mismatch test proves representative run, repair, and finalize cleanup paths are not executed for another active flow.
- Target-match tests cover next-action, run command, repair path, finalize leaf precondition or recovery, and autoApprove shortcut.
- Manual recovery exception remains visible for finalize-cleanup target-match scenarios.

## Implementation Notes
Use spec-local tests with `// spec: R<N>` headers and update shared unit tests where command contracts change.

## Test Strategy
Spec-local tests with `// spec: R<N>` headers for issue target mismatch and retained behavior. Shared unit tests may be updated only when production command contracts change.

---
Status: pending | Parent: (root) | Added Round: 0
