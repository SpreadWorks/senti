# T-3: Cover prompt reduction

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add spec-local regression tests that verify prompt size reduction conditions and project regression coverage for the gate-impl prompt changes.

## Acceptance Criteria
- A test compares a representative full-spec prompt to the file-map excerpt prompt and asserts the file-map prompt omits unrelated full spec text.
- A test asserts the Spec or Requirements prompt section for a single target requirement is less than 50% of the full spec fixture character count.
- A test asserts integration missing-file-map trust failure happens before any fallback agent call.
- Every spec-local test file includes a `// spec: R<N> ...` header.
- node --test specs/279-reduce-gate-impl-prompt-size/tests/*.test.js passes.
- npm test passes.

## Implementation Notes
Use deterministic fixtures and mocked agent calls; do not require live AI providers or prompt log files.

## Test Strategy
Run spec-local tests and the project test command.

---
Status: pending | Parent: (root) | Added Round: 0
