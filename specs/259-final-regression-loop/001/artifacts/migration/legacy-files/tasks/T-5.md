# T-5: Finalize coverage

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add spec-local tests with requirement headers and run the focused test set needed to validate the completed flow contract.

## Acceptance Criteria
- specs/259-final-regression-loop/tests contains // spec: R<N> headers for testable requirements.
- Focused tests for final-regression, test-regression policy, get-next-action, flow steps, and raw evidence validation pass.
- The final implementation records evidence that full npm test -- is outside normal repair-loop test-execute by default.

## Implementation Notes
Keep tests focused. Shared regression tests may cover public flow contracts, but spec-local tests remain required by the spec-test-coverage guardrail.

## Test Strategy
Run spec-local tests first, then focused shared unit tests, then final-regression before finalize.

---
Status: pending | Parent: (root) | Added Round: 0
