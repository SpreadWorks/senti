{
  "version": 1,
  "phase": "test",
  "generatedAt": "2026-06-11T16:09:09.448Z",
  "verdict": "FAIL",
  "counts": {
    "blocking": 4,
    "advisory": 1,
    "total": 5
  },
  "coverageArtifact": "specs/289-fix-plugin-agent-default/test-coverage.json",
  "blockingFindings": [
    {
      "kind": "blocking",
      "title": "R1 fallback chain lacks active/default profile prefix coverage",
      "target": "specs/289-fix-plugin-agent-default/tests/agent-resolution.test.js",
      "issue": "R1 requires plugin command resolution through active profile prefix match, then default profile prefix match, then generic default fallback, but the tests only exercise the generic default path when the active profile has no plugin mapping. There is no spec-local executable test for an active profile prefix mapping or a default profile prefix fallback mapping for a plugin command.",
      "requiredChange": "Add the smallest executable tests that resolve a plugin command through an active profile prefix mapping and through a default profile prefix mapping before generic default fallback.",
      "whyBlocking": "The coverage artifact marks R1 covered, but a required acceptance behavior has no corresponding spec-local test coverage."
    },
    {
      "kind": "blocking",
      "title": "R3 bare claude normalization is untested",
      "target": "specs/289-fix-plugin-agent-default/tests/agent-resolution.test.js",
      "issue": "R3 requires both bare built-in defaults to normalize before lookup: codex to codex/gpt-5.4 and claude to claude/sonnet. The current tests only assert the codex mapping.",
      "requiredChange": "Add an executable assertion that agent.default: \"claude\" resolves to profileKey \"claude/sonnet\".",
      "whyBlocking": "A must requirement branch has no direct regression coverage."
    },
    {
      "kind": "blocking",
      "title": "R4 diagnostic content is only partially asserted",
      "target": "specs/289-fix-plugin-agent-default/tests/agent-resolution.test.js",
      "issue": "R4 requires unresolved-provider errors to include commandId, explicit provider override state, explicit/profile environment selection, active profile name, default key, and a concise failed-lookup reason. The test checks commandId, active profile, default key, and a generic provider override phrase, but does not clearly assert the explicit/profile environment selection or the failed-lookup reason.",
      "requiredChange": "Extend the R4 assertion to check the explicit/profile environment selection and a concise lookup-failure reason in the diagnostic message.",
      "whyBlocking": "The acceptance requirement for diagnostic fields is broader than the executable assertions, so an implementation could omit required context and still pass."
    },
    {
      "kind": "blocking",
      "title": "Absolute path redaction assertion does not exercise the agent path",
      "target": "specs/289-fix-plugin-agent-default/tests/agent-resolution.test.js",
      "issue": "The R5 test checks err.message against a RegExp built from tmpDir() inside the assertion predicate. That creates a new unrelated temporary directory, not the root or config path used by the Agent under test, so the test would pass even if the actual local path leaked.",
      "requiredChange": "Capture the Agent test root/path used by makeAgent, or allow makeAgent to accept a caller-supplied root, and assert that the actual absolute path does not appear in the diagnostic.",
      "whyBlocking": "This is a static anti-pattern that would pass without exercising the required production behavior for absolute local config path redaction."
    }
  ],
  "advisoryFindings": [
    {
      "kind": "advisory",
      "title": "Aggregate R7 smoke test adds no coverage",
      "target": "specs/289-fix-plugin-agent-default/tests/agent-resolution.test.js",
      "improvement": "Remove or replace the final R7 assert.ok(true) test with concrete assertions, since R7 is already represented by the individual requirement tests.",
      "whyNonBlocking": "It is harmless as a coverage index marker, but it does not validate behavior and may make coverage look stronger than it is."
    }
  ],
  "contractSummary": {
    "targetStep": "test-review",
    "artifactPath": "specs/289-fix-plugin-agent-default/test-review.json",
    "verdict": "FAIL",
    "result": "FAIL",
    "blockingCount": 4,
    "failureKind": null,
    "nextAction": null,
    "completionKind": "non_normal",
    "progressSignature": "785d44de921a83f21b8307ee1295166d40cdb6522cee4c0a00a7a2f074c2e43d"
  },
  "sourceArtifact": "test-review.json",
  "attempt": 1,
  "findings": [
    {
      "id": "test-001-blocking-001",
      "phase": "test",
      "sourceArtifact": "test-review.json",
      "attempt": 1,
      "severity": "blocking",
      "title": "R1 fallback chain lacks active/default profile prefix coverage",
      "body": "R1 requires plugin command resolution through active profile prefix match, then default profile prefix match, then generic default fallback, but the tests only exercise the generic default path when the active profile has no plugin mapping. There is no spec-local executable test for an active profile prefix mapping or a default profile prefix fallback mapping for a plugin command.",
      "category": "unknown"
    },
    {
      "id": "test-001-blocking-002",
      "phase": "test",
      "sourceArtifact": "test-review.json",
      "attempt": 1,
      "severity": "blocking",
      "title": "R3 bare claude normalization is untested",
      "body": "R3 requires both bare built-in defaults to normalize before lookup: codex to codex/gpt-5.4 and claude to claude/sonnet. The current tests only assert the codex mapping.",
      "category": "unknown"
    },
    {
      "id": "test-001-blocking-003",
      "phase": "test",
      "sourceArtifact": "test-review.json",
      "attempt": 1,
      "severity": "blocking",
      "title": "R4 diagnostic content is only partially asserted",
      "body": "R4 requires unresolved-provider errors to include commandId, explicit provider override state, explicit/profile environment selection, active profile name, default key, and a concise failed-lookup reason. The test checks commandId, active profile, default key, and a generic provider override phrase, but does not clearly assert the explicit/profile environment selection or the failed-lookup reason.",
      "category": "unknown"
    },
    {
      "id": "test-001-blocking-004",
      "phase": "test",
      "sourceArtifact": "test-review.json",
      "attempt": 1,
      "severity": "blocking",
      "title": "Absolute path redaction assertion does not exercise the agent path",
      "body": "The R5 test checks err.message against a RegExp built from tmpDir() inside the assertion predicate. That creates a new unrelated temporary directory, not the root or config path used by the Agent under test, so the test would pass even if the actual local path leaked.",
      "category": "unknown"
    },
    {
      "id": "test-001-non-blocking-005",
      "phase": "test",
      "sourceArtifact": "test-review.json",
      "attempt": 1,
      "severity": "non-blocking",
      "title": "Aggregate R7 smoke test adds no coverage",
      "body": "Aggregate R7 smoke test adds no coverage",
      "category": "unknown"
    }
  ]
}
