# T-4: Cover Agent regressions

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add spec-local tests and update shared Agent tests for all Issue #378 resolution and diagnostic requirements.

## Acceptance Criteria
- R7 and AC1 through AC8 are covered by tests with // spec: R<N> headers under specs/289-fix-plugin-agent-default/tests/.
- Shared tests under tests/unit/lib/ are updated only for the public Agent contract changes.
- The targeted unit test command and project regression command pass.

## Implementation Notes
Keep tests free of real AI CLI calls by using dry-run paths, synthetic ProviderRegistry config, or direct resolve assertions.

## Test Strategy
Run the spec-local tests and the relevant shared unit tests; final-regression will run the configured full project test command before finalize.

---
Status: pending | Parent: (root) | Added Round: 0
