# T-2: Cover status payload contract

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add spec-local tests with requirement headers and update shared regression tests for the new status output contract.

## Acceptance Criteria
- Spec-local tests under `specs/280-flow-get-status-summary/tests/` include `// spec: R<N>` headers.
- Tests cover default omission, `--details` inclusion, runId parity, inactive behavior, and option validation.
- Shared tests that assert old status payload behavior are updated to use `--details` when they need audit fields.

## Implementation Notes
Keep production changes and test-only coverage separate in review. Existing shared unit tests may be updated because the production CLI contract changes, but they do not replace spec-local coverage.

## Test Strategy
Run the flow test step for spec-local tests and the relevant shared unit scope before final regression.

---
Status: pending | Parent: (root) | Added Round: 0
