# T-2: Cover metadata boundary

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add spec-local tests that lock metadata-only commit behavior, target-external dirty no-op behavior, and skipped downstream reset compatibility.

## Acceptance Criteria
- Tests include // spec headers covering R1 through R5.
- Metadata-only dirty test proves the worktree is clean after the helper commits.
- Target-external dirty test proves no commit is created and the dirty file remains dirty.
- Downstream reset test proves skipped finalize-sync/finalize-cleanup reset still commits flow.json when no target-external dirty path exists.
- External dirty plus skipped reset test proves no reset mutation or commit is created.

## Implementation Notes
Keep fixture state to three explicit cases: metadata-only dirty, metadata plus target-external dirty, downstream skipped reset. Do not depend on network or gh.

## Test Strategy
Run the spec-local tests through test-execute, then run targeted unit tests if shared helpers are touched.

---
Status: pending | Parent: (root) | Added Round: 0
