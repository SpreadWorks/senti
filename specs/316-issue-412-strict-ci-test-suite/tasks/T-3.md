# T-3: Compose deterministic CI tests

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Expose the exact four-stage credential-free test:ci suite and preserve explicit test:agent.

## Acceptance Criteria
- R3 tests/ci.js runs the named unit, e2e, stub acceptance, and CLI smoke commands sequentially.
- The schema-aware stub satisfies docs enrich/text and quality contracts without credentials.
- R5 repairs report.test.js imports/base fixture while real-provider tests remain behind test:agent.

## Implementation Notes
Add a schema-aware stub helper and isolated CI tests; do not add dependencies or provider configuration.

## Test Strategy
Test stage order/fail-fast behavior, run npm run test:ci with credential variables absent, and verify tests/agent is excluded while its fixture/import resolution remains valid.

---
Status: pending | Parent: (root) | Added Round: 0
