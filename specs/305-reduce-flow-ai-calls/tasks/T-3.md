# T-3: Define fallback policies

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Define fallback and staticization policies for context.search, auto-check, finalize-sync/docs build, docs staticization, JSON normalization, findings aggregation, triage/repair artifacts, and requirement compliance prechecks.

## Acceptance Criteria
- R5 is satisfied for context.search and auto-check fallback policy.
- R6 is satisfied for finalize-sync/docs build and docs staticization policy.
- R7 is satisfied for JSON normalizer, findings aggregation, triage/repair artifact, and requirement compliance precheck policies.

## Implementation Notes
Each policy must name the deterministic candidate, AI fallback condition, hard-fail boundary if any, audit evidence, and quality verification.

## Test Strategy
Add spec-local tests with `// spec: R5 ...`, `// spec: R6 ...`, and `// spec: R7 ...` headers that validate required policy sections and fallback fields.

---
Status: pending | Parent: (root) | Added Round: 0
