# T-1: Add specCount and cacheHitRate to row data model

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
buildRows で specCount をカウントし、finalizeRow で cacheHitRate を算出してフィールドに追加する。

## Acceptance Criteria
- createEmptyRow に specCount (初期値 0) を追加
- applyPhaseMetrics で specCount をインクリメント
- finalizeRow で cacheHitRate を算出 (cacheRead / (tokenInput + cacheRead), 0 除算時は null)
- buildRowsFromMetrics ���同様に specCount=1 を設定

## Implementation Notes
buildRows 内の for ループで各 flow entry が applyPhaseMetrics を呼ぶたびに specCount をインクリメント。finalizeRow で cacheHitRate を計算して出力に含める。

## Test Strategy
unit: buildRowsFromMetrics の戻り値に specCount, cacheHitRate が含まれることを検証

---
Status: pending | Parent: (root) | Added Round: 0
