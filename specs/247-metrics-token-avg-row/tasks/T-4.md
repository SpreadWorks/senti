# T-4: Update formatCsv and formatJson for new fields

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
CSV ヘッダーに specCount, cacheHitRate を追加。JSON の phaseSummary にも新フィールドを反映。

## Acceptance Criteria
- CSV ヘッダーに specCount, cacheHitRate カラムが追加される
- CSV の cost は toFixed(1)
- JSON rows[] に specCount, cacheHitRate が含まれる
- JSON phaseSummary に avgSpecCount, avgDifficulty, avgCallCount が含まれる
- JSON の cost は数値のまま

## Implementation Notes
formatCsv のヘッダー行とデータ行に新カラム追加。formatJson は rows をそのまま出力するので行データの変更で自動反映。phaseSummary は computePhaseSummary の戻り値変更で反映。

## Test Strategy
e2e: CSV ヘッダー検証、JSON phaseSummary 検証

---
Status: pending | Parent: (root) | Added Round: 0
