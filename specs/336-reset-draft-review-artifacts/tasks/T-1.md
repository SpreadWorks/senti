# T-1: Replace PASS route artifacts

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Make the shared PASS lifecycle replace stale current triage and repair artifacts for both draft-review routes while preserving rewind history and all existing contracts.

## Acceptance Criteria
- R1 and R2 replace questions-route triage and repair artifacts with canonical empty current-attempt documents.
- R3 applies identical route-correct replacement to draft-coverage.
- R4 keeps the PASS step transitions and removes invalidated findings from the current view.
- R5 preserves non-PASS behavior, audit/history, paths, schemas, routing, accounting, and Issue #443 boundaries.
- R6 is covered by finite behavior-level sequences for both routes.

## Implementation Notes
Change the shared writeEmptyDraftReviewRouteArtifacts behavior in src/flow/definition.js. Reuse DraftReviewRoute metadata and the existing registry lifecycle hook. Do not modify run-reopen-draft or transition guards unless source verification disproves the recorded contracts.

## Test Strategy
Add spec-local node:test scenarios under specs/336-reset-draft-review-artifacts/tests/ with // spec: R1 R2 R3 R4 R5 R6 headers. For questions and coverage, run the actual non-PASS review post hook, derive the prompt-owned triage/repair JSON fixture from returned findings, advance with actual set-step, retain artifacts through actual reopen-draft, then run the registry PASS hook. Run relevant existing lifecycle and reopen-draft unit tests as targeted regression.

---
Status: pending | Parent: (root) | Added Round: 0
