# T-3: get-next-action の definition 駆動化と context-rules.json 廃止

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
get-next-action.js を definition の導出関数を呼ぶアダプタに変更し、context-rules.json を廃止する。

## Acceptance Criteria
- flow get next-action が definition から宣言的にアクションを導出する
- prompt / schema / contextKinds を definition のノード属性から参照する
- context-rules.json が削除されている
- 既存の get-next-action テストが通る

## Implementation Notes
get-next-action.js の resolveTarget / fallback promotion を definition.deriveNextAction に置換。loadJson(context-rules.json) の参照を definition のノード属性参照に置換。get-step-instructions.js の instructionsKey 解決は維持（prompt ファイルの物理配置は変えない）。

## Test Strategy
既存の get-next-action テストを nested 構造に対応させて通す。REQ-11 (data-only extensibility) テストは definition 側で新たに検証。

---
Status: pending | Parent: (root) | Added Round: 1
