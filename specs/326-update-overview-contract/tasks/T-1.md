# T-1: Unify update-overview payload validation

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Make the update-overview help and command boundary consume the existing merge contract so every accepted payload is persistable without shape translation.

## Acceptance Criteria
- R1 rejects the complete invalid-shape matrix before state lookup or persistence and observes the exact 50-entry/500-character boundaries.
- R2 accepts and forwards the complete three-category string-array payload unchanged and documents that same shape in CLI help.
- R3 preserves deterministic pure merge, task stamping, rendering, and rollback behavior.
- R4 preserves parse error codes and the locked source/test boundaries without dependency, allowlist, skip, or assertion reduction.
- AC1-AC8 are evidenced by spec-local and allowed shared tests.

## Implementation Notes
Import and reuse the existing additions validator from overview-merge.js in the command boundary. Keep the next-action schema and merge semantics unchanged. Product edits are limited to run-update-overview.js and registry.js unless source evidence during implementation contradicts this spec, in which case return through the guarded spec-correction path rather than expanding scope.

## Test Strategy
Capture the current object-entry acceptance as the pre-fix reproduction. Add one spec-local contract test with the required header, extend only the allowed shared update-overview tests where needed, run the focused set, fix the commit, obtain one independent AC audit, then allow exactly one full npm test execution.

---
Status: pending | Parent: (root) | Added Round: 0
