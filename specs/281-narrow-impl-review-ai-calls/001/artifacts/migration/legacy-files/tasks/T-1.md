# T-1: Bound loop review calls

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Update impl loop review so per-chunk AI calls are capped at 16, duplicate chunk inputs are skipped within one execution, and cross-check runs only for multi-chunk proposal output.

## Acceptance Criteria
- R1 and R2 are satisfied by the batching limit.
- R3 and R4 are satisfied by the cross-check condition.
- R5 is satisfied by same-execution duplicate chunk hash handling.
- R7 is satisfied by calling loop review from active impl review before artifact writing.

## Implementation Notes
Keep existing chunking and proposal expansion behavior. Use Node.js built-in APIs only. Do not introduce persistent cache state. The active `runReview` impl path must call the bounded loop review for applicable scenarios and normalize its result into the existing impl-review artifact flow.

## Test Strategy
Add spec-local tests under specs/281-narrow-impl-review-ai-calls/tests/ with `// spec: R<N> ...` headers. Cover R1, R2, R3, R4, R5, and R7 by stubbing review agent calls and verifying active-path call counts for limit, cross-check, and duplicate chunk scenarios.

---
Status: pending | Parent: (root) | Added Round: 0
