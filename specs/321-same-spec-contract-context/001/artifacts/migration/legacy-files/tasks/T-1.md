# T-1: Add same-spec contract context

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Build and integrate the bounded authoritative same-spec contract context into spec-level integration requirement prompts while preserving existing gate lifecycle contracts.

## Acceptance Criteria
- R1-R4 context content, ordering, bounds, truncation, exact zero-based JSON locators, and byte determinism are implemented.
- R5 positive replacement and negative current-contract preservation cases are verified.
- R6-R7 call count, schema, cache algorithm, counters, retries, artifacts, routing, and other gate behavior remain unchanged.

## Implementation Notes
Add dedicated invariant-enforcing context value classes beside existing requirement prompt/batch types in run-gate.js. Pass parent structured spec into integration batch planning without changing task-impl planning. Serialize one context per existing batch and add it to buildImplCheckPrompt only for integration batches. Format locators exactly as requirements[<index>], overview.decisions[<index>], or clarifications[<index>] and use fixed truncation records.

## Test Strategy
Write deterministic contract tests under specs/321-same-spec-contract-context/tests with // spec: R<N> headers, extend tests/unit/flow/gate-evaluation-schema.test.js where shared schema/call assertions belong, and run tests/e2e/flow/gate-impl-integration.test.js. Full project regression remains final-regression.

---
Status: pending | Parent: (root) | Added Round: 0
