# T-32: Move raw output log into spec directory

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
raw output ログの配置を .tmp/test-execution-<spec>.log から specs/<spec>/tests/.raw/test-execution.log に変更する

## Acceptance Criteria
- test-execute prompt / 実装が raw output を specs/<spec>/tests/.raw/test-execution.log に書き出す
- .gitignore で specs/<spec>/tests/.raw/ を除外していない (= commit 可能、ただし finalize-commit の境界は T-19 で制御)
- raw output が finalize 後も spec 成果物として保持される
- test-execute-result.json の raw_output_path フィールドが新パスを指す
- テストファイル先頭に `// spec: R33` ヘッダーを記載

## Implementation Notes
プロジェクトの .tmp/ 利用慣行 (logs等) と分離するため spec 内に永続化。finalize 時の commit 境界は T-19 で扱う

## Test Strategy
tests/unit/flow/run-test-execute*.test.js で raw output 書き出し先を verify

---
Status: pending | Parent: (root) | Added Round: 0
