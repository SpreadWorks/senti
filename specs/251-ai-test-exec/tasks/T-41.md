# T-41: Update gate-step phase resolver for new flow context

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
src/flow/lib/gate-step.js の resolveGateStepId / resolveGatePhaseFromState を更新し、flow-level (integration) と task-level の gate-impl phase を正しく区別する

## Acceptance Criteria
- gate-step.js が in-progress step が flow-level の gate-impl の場合に integration phase を返す
- task subflow の gate-impl の場合に task-impl phase を返す
- tests/unit/flow/gate-phase-inference*.test.js を新規作成または既存更新
- テストファイル先頭に `// spec: R44` ヘッダーを記載

## Implementation Notes
現状の resolveGatePhaseFromState ロジックを scope (FLOW_DEFINITION vs TASK_DEFINITION) で分岐

## Test Strategy
phase inference の単体テスト

---
Status: pending | Parent: (root) | Added Round: 0
