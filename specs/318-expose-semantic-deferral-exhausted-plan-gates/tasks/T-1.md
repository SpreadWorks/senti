# T-1: Expose guarded semantic deferral

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Connect exhausted draft/spec next-action rendering to read-only durable classification and the existing guarded run-gate semantic-deferral transition while retaining stopped non-semantic gates and changed-fingerprint one-attempt recovery for task-impl and integration.

## Acceptance Criteria
- R1-R2 emit a target-guarded continuation only for canonical durable semantic_findings evidence.
- R3-R4 preserve attempts and provider calls while persisting findings, completing the gate, advancing the flow, and rejecting mismatched targets before mutation.
- R5-R6 preserve stopped non-semantic plan gates plus changed-fingerprint eligibility, audited reset commands, max-minus-one grants, and pre-write target guards for task-impl and integration.
- AC1-AC8 are covered by spec-local unit and CLI end-to-end scenarios plus existing recovery regression tests.

## Implementation Notes
Keep mutation in run-gate. Expose the smallest read-only durable classification helper required by get-next-action, or extract one shared helper only if both modules otherwise duplicate the same classification behavior. Build the continuation from the selected flow state and route execution back through `flow run gate --phase <phase>` so registry target guards execute before mutation.

## Test Strategy
Add spec-local node:test cases for draft/spec semantic eligibility, all named non-deferable classifications, missing/corrupt sources, exact guard composition, guard mismatch immutability, 5/5 attempt preservation, provider-call preservation, finding persistence, gate completion, next-step advancement, and task/integration recovery parity. Run the spec test, affected shared unit/e2e suites, and the configured final regression.

---
Status: pending | Parent: (root) | Added Round: 0
