# T-1: Create production acceptance fixture

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Provide reusable test input assembly for current acceptance-review production contracts without creating a replacement outcome builder.

## Acceptance Criteria
- R1 removes the deleted contract without adding a compatibility export.
- R2 assembles complete current flow, evidence, and fingerprint inputs and invokes existing production exports.
- The helper contains no independent verdict, normalization, persistence, or flow transition implementation.

## Implementation Notes
The approved overview confirms a shared class-based fixture for the repeated bounded temporary repository, repair baseline, mechanical evidence, deferred source evidence, and acceptance input assembly used by multiple target regressions. Keep verdict derivation, normalization, persistence, and flow transitions in existing production modules; keep scenario-specific producer and assertion setup in its owning historical test.

## Test Strategy
Add spec-local structural and behavior checks for R1 and R2, then exercise the helper through migrated complete regression files rather than testing a parallel fake contract.

---
Status: pending | Parent: (root) | Added Round: 0
