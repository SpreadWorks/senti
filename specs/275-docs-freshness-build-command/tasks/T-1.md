# T-1: Update freshness guidance

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Change freshness check help and text result output so every docs regeneration hint points to `sdd-forge docs build`.

## Acceptance Criteria
- Help text uses `sdd-forge docs build` for docs regeneration guidance.
- `stale` text output uses `run: sdd-forge docs build`.
- `never-built` text output uses `run: sdd-forge docs build`.

## Implementation Notes
Edit only `src/check/commands/freshness.js` unless tests reveal another freshness guidance source. Do not add a top-level build alias.

## Test Strategy
Add a spec-local Node test under `specs/275-docs-freshness-build-command/tests/` with `// spec: R<N> ...` headers covering R1, R2, R3, and R4. The test should exercise help output, stale output, never-built output, JSON output shape, exit codes, and unsupported `--format` validation through the CLI.

---
Status: pending | Parent: (root) | Added Round: 0
