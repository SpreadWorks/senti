# T-2: Prevalidate Markdown render plans

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Move CLI and internal-view rendering behind one complete validated collection, output-path, and selected-spec context plan before any filesystem side effect.

## Acceptance Criteria
- R3-R5 construct every task path and render context before spec.md, --out, tasks directory, or task-file writes.
- R7 valid CLI/view output, stdout/changed paths, optional-missing result, and additive orphan behavior remain unchanged.
- Foreign ambient flow metadata never appears; absent or mismatched selected-spec metadata renders the selected-spec defaults.

## Implementation Notes
Update `runSpecRender` and `renderSpecView` to consume the shared classes. Keep `renderSpecMarkdown` and `renderTaskMarkdown` deterministic. Do not delete orphan task files or constrain the existing explicit --out destination beyond current CLI validation.

## Test Strategy
Exercise production CLI and view entry points with valid and invalid collections, explicit spec B plus active flow A, pre-existing outputs/orphans, absent tasks directories, and byte snapshots taken before invocation.

---
Status: pending | Parent: T-1 | Added Round: 0
