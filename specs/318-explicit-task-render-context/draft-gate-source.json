{
  "version": 1,
  "phase": "draft",
  "result": "fail",
  "evaluations": [
    {
      "guardrail_id": "unambiguous-requirements",
      "result": "fail",
      "reason": "The requirement uses the vague phrase `意味のある値` without stating verifiable conditions for which values must be represented as dedicated classes.",
      "category": "code-quality",
      "title": "Unambiguous Requirements",
      "observations": [
        {
          "kind": "violation",
          "failureMode": "guardrail-violation",
          "requirementRef": "unambiguous-requirements",
          "where": {
            "file": "content",
            "locator": "decisionMap.resolvedByProjectRules[1]"
          },
          "observed": "The requirement uses the vague phrase `意味のある値` without stating verifiable conditions for which values must be represented as dedicated classes.",
          "severity": "blocking",
          "refs": [
            "unambiguous-requirements"
          ]
        }
      ]
    },
    {
      "guardrail_id": "complete-context",
      "result": "fail",
      "reason": "The requirement `意味のある値と invariant は object literal ではなく専用 class で表現し` is subjective because reviewers could disagree on what counts as `意味のある値`; it needs measurable criteria.",
      "category": "process",
      "title": "Complete Context",
      "observations": [
        {
          "kind": "violation",
          "failureMode": "guardrail-violation",
          "requirementRef": "complete-context",
          "where": {
            "file": "content",
            "locator": "decisionMap.resolvedByProjectRules[1]"
          },
          "observed": "The requirement `意味のある値と invariant は object literal ではなく専用 class で表現し` is subjective because reviewers could disagree on what counts as `意味のある値`; it needs measurable criteria.",
          "severity": "blocking",
          "refs": [
            "complete-context"
          ]
        }
      ]
    }
  ],
  "observations": [
    {
      "kind": "violation",
      "failureMode": "guardrail-violation",
      "requirementRef": "unambiguous-requirements",
      "where": {
        "file": "content",
        "locator": "decisionMap.resolvedByProjectRules[1]"
      },
      "observed": "The requirement uses the vague phrase `意味のある値` without stating verifiable conditions for which values must be represented as dedicated classes.",
      "severity": "blocking",
      "refs": [
        "unambiguous-requirements"
      ]
    },
    {
      "kind": "violation",
      "failureMode": "guardrail-violation",
      "requirementRef": "complete-context",
      "where": {
        "file": "content",
        "locator": "decisionMap.resolvedByProjectRules[1]"
      },
      "observed": "The requirement `意味のある値と invariant は object literal ではなく専用 class で表現し` is subjective because reviewers could disagree on what counts as `意味のある値`; it needs measurable criteria.",
      "severity": "blocking",
      "refs": [
        "complete-context"
      ]
    }
  ]
}
