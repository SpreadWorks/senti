# T-3: Cover retained output surfaces

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add behavior-level regression coverage for the public `presets list` output surfaces retained during the inventory migration.

## Acceptance Criteria
- R4 tests assert root format, branch connectors, labels, aliases, scan markers, template availability marker, and child alphabetical ordering.
- R4 tests assert `(no base preset found)` fallback when inventory has no base preset.
- R6 tests assert entry-count, depth, and cycle bounds.
- R5 existing setup and resolver tests continue passing.

## Implementation Notes
Use focused fixture preset objects for renderer-level output surfaces where possible; use plugin-backed temp project only for registry integration behavior.

## Test Strategy
Spec-local tests under `specs/298-fix-presets-list-tree/tests/` with `// spec: R<N> ...` headers. Run targeted spec tests and relevant existing preset/setup tests.

---
Status: pending | Parent: (root) | Added Round: 0
