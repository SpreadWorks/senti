# T-2: Render project preset tree

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Update `senti presets list` to render project-aware preset inventory while preserving existing tree formatting behavior.

## Acceptance Criteria
- R2 command output shows `base > webapp > js-webapp > nextjs` in a plugin-backed project.
- R3 base-only command output remains successful without plugin registry.
- R4 retained formatting surfaces are preserved.
- R6 renderer traversal enforces depth 16 and visited-key cycle prevention.

## Implementation Notes
Keep renderer behavior separate from inventory lookup. Consider making the renderer accept an explicit preset list so missing-base and formatting behavior can be tested without filesystem-heavy setup.

## Test Strategy
Add spec-local command or renderer tests covering plugin-backed tree output, base-only output, and missing-base fallback.

---
Status: pending | Parent: (root) | Added Round: 0
