# T-3: Expose contract evidence

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add contract summary and progress evidence to target artifacts while preserving existing verdict / path / failureKind meanings for current readers.

## Acceptance Criteria
- Target artifacts include contract summary with the fields listed in R6.
- Existing readers still obtain verdict / path / failureKind values with the same meaning.
- Spec-local tests cover R6 and R8, including valid override and missing override cases.

## Implementation Notes
Update artifact builders and schemas for review, gate, test-result-review, and final-regression outputs. Persist integration gate verdicts to `specs/<spec>/impl-gate-result.json`. Keep existing required fields unless the same commit updates every current reader.

## Test Strategy
Spec-local artifact tests under `specs/271-flow-judgment-contract/tests/` include `// spec: R6 R8 ...` headers, then run `node tests/run.js --scope unit`.

---
Status: pending | Parent: (root) | Added Round: 0
