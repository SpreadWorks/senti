# T-3: Add contract tests

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add focused tests that lock the new specId API and CLI propagation behavior.

## Acceptance Criteria
- Spec-local tests include headers for R1 through R8.
- Shared tests are updated only when needed for existing contract coverage.
- Tests prove active-flow registry independent updates and preservation of existing fallback behavior.

## Implementation Notes
Prefer existing helpers in tests/helpers/flow-setup.js and existing flow-state test patterns. Keep tests deterministic and file-system local.

## Test Strategy
Run spec-local tests first, then targeted unit scope, then project regression through the SDD flow test steps.

---
Status: pending | Parent: (root) | Added Round: 0
