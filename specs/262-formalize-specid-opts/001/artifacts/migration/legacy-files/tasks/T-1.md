# T-1: Formalize flow-state API

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add effective specId handling to FlowStore and FlowManager, including construction-time binding through forRoot(root, { specId }).

## Acceptance Criteria
- FlowManager facade methods accept opts where needed and forward effective specId.
- forRoot(root, { specId }) binds a default specId without changing unbound behavior.
- Per-call opts.specId overrides a bound specId.

## Implementation Notes
Touch src/lib/flow-store.js and src/lib/flow-manager.js. Keep specId selection separate from taskId scope. Preserve ambient metric no-op when no specId or active path exists.

## Test Strategy
Unit or spec-local tests should cover per-call specId, bound specId, override precedence, taskId preservation, and ambient no-op.

---
Status: pending | Parent: (root) | Added Round: 0
