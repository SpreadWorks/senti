# T-2: Add finalize retro report regression test

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
finalize 経由の report.json と report text が retro.json summary を表示することを spec-local test で固定する。

## Acceptance Criteria
- specs/272-finalize-retro-report/tests/ 配下に `// spec: R1 R3 R4` header を持つ test がある。
- test は retro.json を配置した一時 spec で executeCommitPost を実行する。
- test は report.json data.retro と report text の Retro 集計行を assert する。

## Implementation Notes
既存 unit test の executeCommitPost 呼び出しパターンを使う。git summary や issue comment は linked issue なしの ctx にして副作用を最小化する。

## Test Strategy
node --test specs/272-finalize-retro-report/tests/*.test.js と unit scope regression で検証する。

---
Status: pending | Parent: (root) | Added Round: 0
