During the draft phase, the AI over-applies the Single Responsibility guardrail and proposes splitting a scope the user defined as a single Issue into multiple specs. Whether an Issue constitutes a single concern should be left to the user's judgment. Occurred in the draft of #284.

<details>
<summary>ja</summary>

[BUG] AI が Issue のスコープを勝手に分割する問題

draft フェーズで AI が Single Responsibility guardrail を過剰適用し、ユーザーが1つの Issue として定義したスコープを勝手に複数 spec に分割提案する。Issue が1つの concern として成立しているかどうかの判断はユーザーに委ねるべき。#284 の draft で発生。

</details>