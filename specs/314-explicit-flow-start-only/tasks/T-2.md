# T-2: Revise flow entry guidance

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Update senti.flow metadata and entry branching so non-explicit ordinary requests do not enter route choice, while explicit start and active continuation remain documented.

## Acceptance Criteria
- Skill metadata names explicit start and active flow continuation as supported entry conditions.
- A.0 Route choice for non-explicit feature/fix requests is removed.
- B. Prelude and C. Dispatcher loop remain reachable for explicit start and active continuation.

## Implementation Notes
Update src/skills/senti.flow/SKILL.md and keep dispatcher/prelude instructions intact. Preserve target-aware status guard behavior.

## Test Strategy
Update existing skill placement tests and add spec-local checks for explicit-start and active-continuation guidance. Spec-local files must start with `// spec: R<N> ...` headers.

---
Status: pending | Parent: (root) | Added Round: 0
