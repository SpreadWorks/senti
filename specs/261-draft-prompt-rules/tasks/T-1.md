# T-1: Add shared draft rules

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Create the shared draft QA rules partial and wire flow step instruction loading so prompt includes resolve within maximum depth 8 and maximum include count 32 before next-action returns instructions.

## Acceptance Criteria
- `draft-qa-rules.md` contains the shared QA schema, boundary, checklist, premise, decision, and coverage rules.
- `getStepInstructions` expands include directives using `resolveIncludes` with maximum depth 8 and maximum include count 32.
- Missing include paths fail loudly with the include path in the error.

## Implementation Notes
Use `resolveIncludes` from `src/lib/include.js`. Pass the containing prompt directory as `baseDir` and `PKG_DIR` as `pkgDir`. Preserve existing bounds: maximum depth 8 and maximum include count 32. Do not add a new directive parser.

## Test Strategy
Unit tests for successful include expansion and missing include failure.

---
Status: pending | Parent: (root) | Added Round: 0
