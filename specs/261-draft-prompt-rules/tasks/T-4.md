# T-4: Add regression coverage

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add spec-local and project unit tests that lock the include, shared prompt, lifecycle, and review formatting contracts.

## Acceptance Criteria
- Spec-local tests cover the new behavior with `// spec: R<N>` headers.
- Existing unit tests are updated or extended where they directly exercise changed contracts.
- The targeted unit test command passes.

## Implementation Notes
Prefer focused unit tests over agent tests because this change is deterministic prompt loading and formatting logic.

## Test Strategy
Run the relevant unit scope and the spec-local test runner in the test-execute step.

---
Status: pending | Parent: (root) | Added Round: 0
