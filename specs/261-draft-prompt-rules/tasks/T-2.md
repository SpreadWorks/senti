# T-2: Align draft lifecycle

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Update the runtime draft QA contract so `considered` is initialized, validated, and available to review formatting.

## Acceptance Criteria
- New draft skeletons include `considered: ""` for QA entries or examples.
- Draft lifecycle validation recognizes `considered` as an allowed QA field.
- Pending, approved, and dropped entries require `considered` to be empty; answered entries may contain a non-empty value.

## Implementation Notes
Update the field whitelist and status-specific validation in `draft-lifecycle.js`; update prepare-spec skeleton/schema prompt examples where QA fields are listed.

## Test Strategy
Unit tests in draft lifecycle/check-draft coverage for accepted answered entries and rejected non-empty `considered` on pending/dropped entries.

---
Status: pending | Parent: (root) | Added Round: 0
