# T-5: Wire candidate approval flow into finalize-cleanup.md template

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
finalize-cleanup.md に、flowIntegration=enable のとき候補取得→Choice Format 承認→承認分を workflow add で作成する条件ブロックを追加する。

## Acceptance Criteria
- finalize-cleanup.md が .sdd-forge/config.json の flowIntegration を読み enable のときのみ発火する記述を含む。
- cleanup 前に issue-log-import --spec <currentSpecPath> で候補取得し Choice Format で提示、承認分のみ候補ごとに workflow add を呼ぶ指示が含まれる。
- 承認なし・確認スキップ時は workflow add を呼ばない記述が含まれる。
- 条件不成立時は従来の finalize-cleanup 手順が不変。

## Implementation Notes
src/flow/prompts/impl/finalize-cleanup.md に条件ブロックを追記。承認 UI は skill の Choice Format、作成は既存 workflow add。flow ロジック層は変更しない。R5。

## Test Strategy
unit: テンプレート文字列に flowIntegration 読取・issue-log-import 呼出・Choice Format 承認・workflow add 呼出の各記述が存在することを検証。

---
Status: pending | Parent: (root) | Added Round: 0
