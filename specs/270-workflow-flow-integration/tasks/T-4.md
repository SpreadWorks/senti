# T-4: Wire issue-start into draft.md template

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
draft.md に、.sdd-forge/config.json の flowIntegration を読み enable かつ issue リンクありのとき issue-start を呼ぶ条件ブロックを追加する。

## Acceptance Criteria
- draft.md が .sdd-forge/config.json の workflow.flowIntegration を（config.lang と同じ要領で）読む記述を含む。
- flowIntegration=enable かつ issue 番号ありを条件として issue-start を呼ぶ指示が含まれる。
- 条件不成立時は従来の draft 手順が不変。

## Implementation Notes
src/flow/prompts/plan/draft.md に条件ブロックを追記。テンプレートは config.json から flag を読む（既存 config.lang 参照に倣う）。flow ロジック層は変更しない。issue-start は冪等前提。R4。

## Test Strategy
unit: テンプレート文字列に条件ブロックと issue-start 呼び出しが存在することを検証（spec 269 の prompt 参照テストに倣う）。

---
Status: pending | Parent: (root) | Added Round: 0
