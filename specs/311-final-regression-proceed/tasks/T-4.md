# T-4: Cover parity behavior

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add spec-local and shared tests proving new record-and-proceed behavior and retained final-regression pass, skipped, fail-closed, evidence, and issue-log behavior.

## Acceptance Criteria
- Spec-local tests include `// spec: R<N>` headers covering R1 through R9.
- Shared unit tests cover runner, schema, registry, report, and prompt behavior where existing test patterns support it.
- Existing migration parity surfaces listed in R9 are behavior-tested.

## Implementation Notes
Keep test fixtures scoped to final-regression and avoid changing tests to mask product failures.

## Test Strategy
Run targeted unit/spec-local tests first, then `node tests/run.js --scope unit` before implementation gate.

---
Status: pending | Parent: (root) | Added Round: 0
