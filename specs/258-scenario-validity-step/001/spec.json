{
  "goal": "Add a plan-phase scenario-validity step that runs spec-local requirement tests before implementation, records whether each testable requirement fails for the expected pre-implementation reason, and blocks invalid tests before implementation starts.",
  "keywords": [
    "scenario-validity",
    "plan test",
    "review-test",
    "test-execute",
    "spec-local tests",
    "expected_fail",
    "test artifacts",
    "flow definition",
    "flow registry",
    "test headers",
    "anti-pattern guidance",
    "sdd-forge upgrade"
  ],
  "scope": {
    "in": [
      "[must] Add a scenario-validity leaf after plan/test and before review-test.",
      "[must] Execute only specs/<spec>/tests/ spec-local tests in the current plan worktree before implementation.",
      "[must] Persist scenario-validity-result.json and tests/.raw/scenario-validity.log with per-requirement outcomes.",
      "[must] Block when any testable requirement lacks expected_fail or has unexpected_pass, invalid_test, skipped, or not_run.",
      "[must] Update plan/test, review-test, and impl/test-execute prompts to describe the new phase split.",
      "[must] Update flow definition, registry command wiring, schemas, finalize artifact path handling, tests, and generated skill templates.",
      "[must] Run sdd-forge upgrade after template changes."
    ],
    "out": [
      "Changing root npm test behavior outside the scenario-validity integration.",
      "Running project-wide regression during scenario-validity.",
      "Adding external dependencies or a new test framework.",
      "Retrofitting historical specs with scenario-validity artifacts.",
      "Publishing an npm release."
    ]
  },
  "constraints": [
    "Use only Node.js built-in modules.",
    "scenario-validity runs in the active plan worktree and must fail before test execution if git diff --name-only <baseBranch> -- src/ tests/ package.json .sdd-forge/config.json contains a path outside specs/<spec>/tests/, specs/<spec>/spec.json, specs/<spec>/draft.json, specs/<spec>/issue-log.json, specs/<spec>/spec-review.md, or specs/<spec>/draft-review-*.md.",
    "scenario-validity runs spec-local tests only; root regression remains the responsibility of impl/test-execute.",
    "The new flow command must return non-zero through the envelope failure path when scenario-validity blocks progress.",
    "bounded-resource-usage is acknowledged because the scenario-validity retry limit is exactly 3 and test discovery is bounded to files under specs/<spec>/tests/ whose basenames match *.{test,spec}.{js,ts,mjs}.",
    "Template changes must be propagated with sdd-forge upgrade."
  ],
  "design_principles": [
    "Treat scenario-validity as a runtime gate for test validity, not as a replacement for implementation-phase regression.",
    "Keep test-execute as the impl-phase regression and final verification execution point.",
    "Use existing flow definition and registry patterns instead of adding parallel sequencing logic."
  ],
  "overview": {
    "modules": [
      {
        "text": "src/flow/definition.js: add the scenario-validity leaf between test and review-test with maxAttempts 3."
      },
      {
        "text": "src/flow/registry.js: add flow run scenario-validity command metadata and post-hook step completion behavior."
      },
      {
        "text": "src/flow/lib/run-scenario-validity.js: execute spec-local tests, classify outcomes, write artifacts, and return pass/block envelope data."
      },
      {
        "text": "src/flow/lib/test-artifacts.js: expose durable/resettable scenario-validity artifact paths and validation helpers."
      },
      {
        "text": "src/flow/prompts/plan/test.md, src/flow/prompts/plan/review-test.md, src/flow/prompts/impl/test-execute.md: update user-facing step guidance."
      },
      {
        "text": "src/templates/skills/sdd-forge.flow/SKILL.md: propagate dispatcher guidance for the new plan-phase execution point."
      }
    ],
    "data_flow": [
      {
        "text": "plan/test writes specs/<spec>/tests/* with // spec: Rn headers -> scenario-validity runs node --test over those files in the current worktree -> scenario-validity-result.json and tests/.raw/scenario-validity.log are written -> review-test statically reviews tests and anti-patterns -> impl/test-execute runs post-implementation verification and root regression."
      }
    ],
    "decisions": [
      {
        "text": "[VERIFY] Flow definition currently has plan/test followed by review-test and no scenario-validity or gate-test leaf.",
        "evidence": "src/flow/definition.js defines plan children in order and shows test followed by createPlanReviewNode({ id: \"review-test\" })."
      },
      {
        "text": "[VERIFY] plan/test currently tells authors to write specs/<spec>/tests/ and not run tests because execution is centralized later.",
        "evidence": "src/flow/prompts/plan/test.md states Write test code under specs/<spec>/tests/, Tests should fail initially, and Do NOT run tests here."
      },
      {
        "text": "[VERIFY] impl/test-execute currently owns post-implementation test-execute-result.json and tests/.raw/test-execution.log.",
        "evidence": "src/flow/prompts/impl/test-execute.md and src/flow/lib/run-test-execute.js define test-execute-result.json version 2 and raw output at tests/.raw/test-execution.log."
      },
      {
        "text": "[VERIFY] Existing test header validation provides requirement-to-file coverage for spec-local tests.",
        "evidence": "src/flow/lib/test-headers.js discovers specs/<spec>/tests/*.{test,spec}.{js,ts,mjs}, parses // spec: Rn headers, and validates coverage for requirements where testable !== false."
      },
      {
        "text": "scenario-validity is the blocking runtime gate immediately after plan/test; no separate gate-test step is added.",
        "evidence": "Draft q1 user answer selected this placement to avoid introducing a separate gate-test concept."
      },
      {
        "text": "scenario-validity runs in the current plan worktree and treats implementation source changes as git diff paths under src/, tests/, package.json, or .sdd-forge/config.json outside the current spec artifact allow-list.",
        "evidence": "Draft q2 user answer selected current worktree execution instead of a detached base worktree."
      },
      {
        "text": "Each testable requirement must have expected_fail; unexpected_pass, invalid_test, skipped, and not_run block progress.",
        "evidence": "Draft q3 user answer selected strict per-requirement validation."
      },
      {
        "text": "review-test keeps static anti-pattern guidance for six listed classes while scenario-validity provides runtime verification.",
        "evidence": "Draft q7 user answer selected all six Issue #329 anti-pattern candidates."
      }
    ]
  },
  "background": "Issue #329 documents a prior failure where an impossible assertion passed static review-test and was only discovered after implementation. The current plan/test step writes spec-local tests but does not execute them. The current impl/test-execute step runs after implementation, so it cannot prove that tests fail before implementation. The new step adds runtime evidence at the point where test validity must be checked.",
  "requirements": [
    {
      "id": "R1",
      "desc": "FLOW_DEFINITION includes a plan-phase leaf id scenario-validity after test and before review-test. The leaf action is run-scenario-validity, its instructions key is plan.scenario-validity, its output schema is next-action/scenario-validity.schema.json, and its maxAttempts resolves to 3.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R2",
      "desc": "The flow registry exposes sdd-forge flow run scenario-validity as an internal flow command that requires an active flow, runs the scenario-validity command implementation, accepts no user-facing positional arguments, and marks the scenario-validity step done only when the result passes.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R3",
      "desc": "scenario-validity discovers and runs only spec-local test files under specs/<spec>/tests/ whose basenames match *.{test,spec}.{js,ts,mjs}. It does not run the project root regression command.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R4",
      "desc": "Before running tests, scenario-validity runs git diff --name-only <baseBranch> -- src/ tests/ package.json .sdd-forge/config.json. If any returned path is outside specs/<spec>/tests/, specs/<spec>/spec.json, specs/<spec>/draft.json, specs/<spec>/issue-log.json, specs/<spec>/spec-review.md, and specs/<spec>/draft-review-*.md, it writes a block result with classification invalid_test and does not execute tests.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R5",
      "desc": "scenario-validity writes specs/<spec>/scenario-validity-result.json and specs/<spec>/tests/.raw/scenario-validity.log on every invocation. The result file contains version, raw_output_path, command, process.started, process.exitCode, process.signal, process.timedOut, process.spawnError, and one summary entry for every testable requirement.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R6",
      "desc": "Each scenario-validity summary entry uses one of expected_fail, unexpected_pass, invalid_test, skipped, or not_run. The step passes only when every testable requirement has expected_fail and none has any blocking classification.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R7",
      "desc": "scenario-validity validates that each summary entry has evidence containing test_file, test_name, command, and raw_output_lines, and that raw_output_lines points into tests/.raw/scenario-validity.log.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R8",
      "desc": "test-artifact helpers add scenario-validity-result.json and tests/.raw/scenario-validity.log to DURABLE_TEST_ARTIFACT_RELATIVE_PATHS and RESETTABLE_TEST_ARTIFACT_RELATIVE_PATHS. run-finalize-commit excludes both files from the implementation commit artifact staging, and rerunning scenario-validity removes both stale files before writing new artifacts.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R9",
      "desc": "plan/test, plan/scenario-validity, plan/review-test, and impl/test-execute prompts describe the phase split: plan/test writes tests, scenario-validity performs pre-implementation spec-local runtime validation, review-test performs static anti-pattern review, and impl/test-execute performs post-implementation verification and root regression.",
      "priority": "must",
      "status": "done",
      "testable": false
    },
    {
      "id": "R10",
      "desc": "review-test prompt guidance lists all six anti-pattern classes from Issue #329: assertions that do not go through production code, input-as-expected round trips, always-matching regex, existence-only checks, catch-all PASS handling, and split-removed separator literal assertions.",
      "priority": "must",
      "status": "done",
      "testable": false
    },
    {
      "id": "R11",
      "desc": "Generated sdd-forge.flow skill template and installed skill output include scenario-validity in the plan-phase dispatcher guidance after running sdd-forge upgrade.",
      "priority": "must",
      "status": "done"
    },
    {
      "id": "R12",
      "desc": "Automated tests cover the new flow definition order, registry command, scenario-validity pass/block classifications, artifact validation, prompt wording, template upgrade output, and coexistence with test-execute-result.json plus tests/.raw/test-execution.log.",
      "priority": "must",
      "status": "done"
    }
  ],
  "acceptance_criteria": [
    "flow get next-action returns scenario-validity after test and before review-test in a plan flow.",
    "sdd-forge flow run scenario-validity writes scenario-validity-result.json and tests/.raw/scenario-validity.log.",
    "scenario-validity passes when every testable requirement has expected_fail evidence.",
    "scenario-validity blocks on unexpected_pass, invalid_test, skipped, not_run, missing evidence, or implementation-target changes before the step.",
    "impl/test-execute still writes test-execute-result.json and tests/.raw/test-execution.log and remains responsible for root regression.",
    "review-test prompt includes the six anti-pattern classes from Issue #329.",
    "sdd-forge upgrade updates generated skill/template output for the new step.",
    "node tests/run.js --scope unit passes for affected unit tests."
  ],
  "clarifications": [
    {
      "q": "Is scenario-validity a new gate-test step?",
      "a": "No. It is a scenario-validity step that behaves as the blocking runtime gate for plan-phase test validity. A separate gate-test step is not added."
    },
    {
      "q": "Does scenario-validity run project regression?",
      "a": "No. It only runs spec-local tests under specs/<spec>/tests/. Project regression remains in impl/test-execute."
    },
    {
      "q": "What is expected_fail?",
      "a": "expected_fail means the requirement's spec-local test ran before implementation and failed in a way attributable to the missing implementation behavior, not because the test was invalid, skipped, not run, or unrelated to the requirement."
    }
  ],
  "alternatives_considered": [
    {
      "option": "Add review-test -> scenario-validity -> gate-test as a three-step sequence",
      "reason": "Rejected by draft q1 because it introduces a separate gate-test concept not present in the current flow and overlaps with scenario-validity's blocking responsibility."
    },
    {
      "option": "Run scenario-validity in a detached base-branch worktree",
      "reason": "Rejected by draft q2 because the current plan worktree should not yet contain implementation changes and avoids copying newly authored spec-local tests into a second worktree."
    },
    {
      "option": "Allow aggregate success when at least one spec-local test fails",
      "reason": "Rejected by draft q3 because it would allow some testable requirements to remain unverified before implementation."
    },
    {
      "option": "Run root regression during scenario-validity",
      "reason": "Rejected by draft q4 because it mixes pre-implementation scenario validity with post-implementation project regression."
    }
  ],
  "open_questions": [],
  "tasks": [
    {
      "id": "T-1",
      "title": "Add scenario-validity flow node",
      "goal": "Add the scenario-validity plan leaf and registry command so the dispatcher can run the new step between test and review-test.",
      "acceptance": [
        "FLOW_DEFINITION orders test -> scenario-validity -> review-test",
        "deriveNextAction exposes run-scenario-validity with maxAttempts 3",
        "flow registry exposes run scenario-validity and marks the step done on pass"
      ],
      "implementation_notes": "Touch src/flow/definition.js, src/flow/registry.js, next-action schema files, and help text as needed.",
      "test_strategy": "Unit tests for flow definition order, next-action envelope, registry command metadata, and post-hook behavior.",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending",
      "key": "T-1"
    },
    {
      "id": "T-2",
      "title": "Implement scenario-validity runner",
      "goal": "Run spec-local tests before implementation, classify per-requirement outcomes, and write scenario-validity artifacts.",
      "acceptance": [
        "Runner discovers existing spec-local test files only",
        "Runner writes scenario-validity-result.json and tests/.raw/scenario-validity.log",
        "Runner blocks on implementation-target changes and invalid outcome classes",
        "Runner validates per-requirement evidence and raw output line ranges"
      ],
      "implementation_notes": "Prefer existing helpers from test-headers.js, test-artifacts.js, and test-regression.js. Add dedicated classes for outcome records and artifacts rather than object-literal discriminated unions.",
      "test_strategy": "Unit tests for pass, unexpected pass, invalid test, skipped, not-run, missing evidence, and precondition block cases.",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending",
      "key": "T-2"
    },
    {
      "id": "T-3",
      "title": "Extend artifact handling",
      "goal": "Make scenario-validity artifacts durable and resettable alongside existing test artifacts.",
      "acceptance": [
        "test-artifacts.js exports scenario-validity artifact names and validation helpers",
        "finalize commit artifact exclusion and follow-up artifact commit include the new durable files",
        "reruns remove stale scenario-validity artifacts before rebuilding them"
      ],
      "implementation_notes": "Keep test-execute-result.json semantics unchanged; add parallel scenario-validity artifact constants.",
      "test_strategy": "Unit tests for artifact path lists and validation failure messages.",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending",
      "key": "T-3"
    },
    {
      "id": "T-4",
      "title": "Update prompts and templates",
      "goal": "Update user-facing flow guidance so each phase describes the correct test execution responsibility.",
      "acceptance": [
        "plan/test says tests are written there and executed next by scenario-validity",
        "plan/scenario-validity prompt exists and states artifact and blocking rules",
        "plan/review-test includes the six anti-pattern classes",
        "impl/test-execute describes impl-phase regression/final verification",
        "sdd-forge upgrade updates generated skill output"
      ],
      "implementation_notes": "Touch src/flow/prompts and src/templates/skills/sdd-forge.flow/SKILL.md, then run sdd-forge upgrade.",
      "test_strategy": "Prompt/template unit tests assert required phrases and upgrade-generated files match templates.",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending",
      "key": "T-4"
    },
    {
      "id": "T-5",
      "title": "Cover integration behavior",
      "goal": "Add focused tests that prove scenario-validity integrates with the plan flow and leaves impl/test-execute behavior intact.",
      "acceptance": [
        "A flow-level test observes scenario-validity between test and review-test",
        "A command-level test verifies pass and block envelopes",
        "Existing test-execute artifact validation continues to pass",
        "Affected unit scope passes"
      ],
      "implementation_notes": "Prefer existing flow command test patterns. Keep spec-local fixture tests under specs/258-scenario-validity-step/tests/ for this spec's requirements.",
      "test_strategy": "Spec-local tests for R1-R8/R11/R12 plus affected unit tests under tests/unit or tests/e2e as appropriate.",
      "parent": null,
      "origin": "plan",
      "added_round": 0,
      "status": "pending",
      "key": "T-5"
    }
  ],
  "user_approval": {
    "approved": true,
    "confirmed_at": "2026-05-17T12:16:27.345Z",
    "notes": "User approved gate-passed spec for Issue #329"
  },
  "specId": "258-scenario-validity-step"
}
