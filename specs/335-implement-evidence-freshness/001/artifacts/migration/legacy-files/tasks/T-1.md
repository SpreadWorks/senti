# T-1: Enforce implement evidence freshness

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Filter implement completion evidence through the existing latest-plan-rewind boundary while preserving no-rewind and mechanical-validation behavior.

## Acceptance Criteria
- R1-R4 pass production-path fixtures for stale, missing, malformed, current, and no-rewind evidence.
- R5 provides retained, skipped, regenerated, exact-boundary, and no-rewind spec-local coverage with requirement headers.
- AC1-AC7 pass without changing artifact schemas or deleting retained evidence.

## Implementation Notes
Add one shared eligibility classification inside `set-step.js`, reuse `isPlanArtifactFresh`, and pass only eligible artifacts into the existing readiness and completion-adapter paths.

## Test Strategy
Create table-driven temporary spec fixtures around the exported pre-validator, control artifact mtimes relative to rewoundAt, assert exact issue-code distinctions, then run the focused test and affected shared regression.

---
Status: pending | Parent: (root) | Added Round: 0
