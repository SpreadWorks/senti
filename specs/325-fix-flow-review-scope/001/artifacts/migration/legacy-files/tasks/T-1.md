# T-1: Resolve implementation review scope

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Make impl review execution, retry, artifacts, outcomes, and lifecycle consume one active-leaf scope decision while preserving task cursor and broad-mode contracts.

## Acceptance Criteria
- R1 and R5 resolve or reject every active-leaf matrix state before subprocess or durable mutation.
- R2 uses the resolved decision for task spec, retry, StepAttempt, artifact taskId, and broad metadata.
- R3 and R4 preserve flow PASS/ADVISORY/FAIL and task review lifecycle behavior.
- R6 preserves Issue #325 broad-mode and target-guard rejection boundaries.
- AC1-AC8 are evidenced by spec-local and named shared tests without skip, allowlist, or assertion reduction.

## Implementation Notes
Use the existing `TaskScopeDecision` class. Keep production edits in task-scope.js and run-review.js. Treat definition.js, registry.js, dispatcher.js, finding disposition, and artifact serializer as unchanged consumers unless source evidence contradicts this spec, in which case return through the guarded spec-correction flow rather than expanding implementation scope.

## Test Strategy
First capture the stale-cursor pre-fix reproduction. Add one spec-local regression file covering the positive/negative matrix with `// spec: R1 R2 R3 R4 R5 R6`. Extend the two focused shared unit suites only for changed public contracts, then run the named Issue #325 task lifecycle regressions and one full npm test execution on the fixed commit.

---
Status: pending | Parent: (root) | Added Round: 0
