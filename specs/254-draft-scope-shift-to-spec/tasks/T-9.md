# T-9: Skip phase=[] guardrails in lint validation

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
lint 検証経路 (validateLintGuardrails と lint 実行) で phase 空配列の guardrail を skip するよう変更する

## Acceptance Criteria
- validateLintGuardrails は phase 空配列の guardrail に対して warning を出さない
- lint 実行は phase 空配列の guardrail を評価対象から除外する
- unit test で phase 空配列 guardrail が lint 経路で skip されることが確認される

## Implementation Notes
src/lib/lint.js の validateLintGuardrails と lint 実行ロジックを編集する。T-1 の loader 変更が前提。テストは specs/254-draft-scope-shift-to-spec/tests/lint-skip.test.js に `// spec: R11` header 付きで作成する。

## Test Strategy
unit test で phase 空配列 + meta.lint を持つ guardrail が skip されることを検証する

---
Status: pending | Parent: (root) | Added Round: 0
