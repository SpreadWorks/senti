# T-4: Add creation-time guidance to spec creation prompt

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
spec creation prompt に synthesize ルール / ソース矛盾 carve-out / draft↔source verification step / 方針修正時のユーザー確認指示 / spec render 実行手順を creation-time 指示として組み込む

## Acceptance Criteria
- spec creation prompt に synthesize / 直接コピー禁止 / 捏造禁止 / ソース矛盾時 correction 許可の文言が含まれる
- spec creation prompt に draft 実装方針を source code に突き合わせる verification step の指示が含まれる
- spec creation prompt に方針修正時の Choice Format でのユーザー確認指示が含まれる
- spec creation prompt に verification 結果と correction 記録の prefix 規約 (VERIFY / CORRECTION)、text maxLength 500 制約と evidence 活用の指針が示される
- spec creation prompt に spec.json 更新後 sdd-forge spec render を実行する手順が明記される

## Implementation Notes
src/flow/prompts/plan/spec.md を編集する。decisions[].text の prefix 規約 (VERIFY / CORRECTION) と長さ制約 (text maxLength 500、超過時は evidence へ移動 / decision 分割) を明示する。autoApprove 分岐ロジックは書かない。trigger 条件 (実装方針置換 / 要件 reject / 未記載要件追加) と非 trigger 条件 (文言改善 / タイポ / rationale 追記) を例示する。spec.json 編集 → spec render → step done の順序を明記する。

## Test Strategy
fixture test で spec creation prompt に必須文言が含まれることを検証する

---
Status: pending | Parent: (root) | Added Round: 0
