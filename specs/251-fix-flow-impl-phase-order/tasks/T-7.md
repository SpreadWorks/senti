# T-7: Reorder gatePhase and add TASK_STEP_TO_PHASE entry for gate-impl auto-resolution

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
FLOW gate-impl の gatePhase 配列を `["integration", "task-impl"]` に並べ替え、TASK_STEP_TO_PHASE に `"gate-impl": "task-impl"` を追加することで、scope に応じた phase 自動解決を実現する。

## Acceptance Criteria
- `STEP_TO_PHASE["gate-impl"]` が `"integration"` を返す
- `TASK_STEP_TO_PHASE["gate-impl"]` が `"task-impl"` を返す
- `PHASE_TO_STEP["integration"]` および `PHASE_TO_STEP["task-impl"]` の双方が `"gate-impl"` を返す (collectGatePhaseEntries の挙動)

## Implementation Notes
src/flow/definition.js line 230-238 周辺の FLOW gate-impl ノードで gatePhase 値を変更。src/flow/lib/gate-step.js の TASK_STEP_TO_PHASE 定義に新エントリを追加。

## Test Strategy
unit (T-9 で追加する gate-phase-inference 更新済みテストで検証)。

---
Status: pending | Parent: (root) | Added Round: 0
