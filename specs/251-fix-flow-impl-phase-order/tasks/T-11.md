# T-11: Add integration gate test for omitted --phase + flow-level + finalize-commit next

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
flow-level gate-impl が `--phase` 省略で integration phase に解決され PASS 時の envelope `next` が `"finalize-commit"` を返すことを検証するテストを spec ローカル `specs/251-fix-flow-impl-phase-order/tests/gate-impl-integration.test.js` として追加する。

## Acceptance Criteria
- 新規 spec ローカル test ファイル `specs/251-fix-flow-impl-phase-order/tests/gate-impl-integration.test.js` が存在する
- scope=flow + step=gate-impl in_progress + `--phase` 省略 → 実行が integration phase で行われる
- PASS 時 envelope `data.next` が `"finalize-commit"`
- テストが PASS する

## Implementation Notes
spec ローカルに配置することで spec test coverage guardrail を満たす。AI 呼び出しを避けるため `--skip-guardrail` で軽量化、もしくは `gatePass` builder 単体を呼ぶ unit test として実装する選択肢も可。

## Test Strategy
unit / e2e (CLI envelope 検証)。

---
Status: pending | Parent: (root) | Added Round: 0
