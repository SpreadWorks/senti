# T-4: Update SKILL.md template wording for new impl phase order

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
src/templates/skills/sdd-forge.flow/SKILL.md の frontmatter description および Hard Stops 文言を新順序に整合させる。

## Acceptance Criteria
- frontmatter description の `implementation (code → gate → review)` が `implementation (code → review → gate)` に変更されている
- line 157 周辺の `re-PASSed after review auto-corrections` の workaround 文言が削除され、`Do not finalize before the impl-phase gate has PASSed.` のみが残っている
- grep で旧文言が hit しない

## Implementation Notes
src/templates/skills/sdd-forge.flow/SKILL.md を Edit する。本文の他の箇所は変更しない。

## Test Strategy
grep ベースの検証 (acceptance_criteria に明記)。

---
Status: pending | Parent: (root) | Added Round: 0
