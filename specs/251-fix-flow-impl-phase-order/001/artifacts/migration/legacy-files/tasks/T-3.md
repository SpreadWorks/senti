# T-3: Add state-based progression unit test via real next-action path

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
GetNextActionCommand の実経路 (state save/reload + findActiveNode 経由) で implement done → review、review done → gate-impl、gate-impl done → finalize-commit の 3 段階遷移を検証する unit test を spec ローカルテストとして追加する。

## Acceptance Criteria
- 新規 test ファイル `specs/251-fix-flow-impl-phase-order/tests/impl-progression.test.js` が存在する
- test が CLI envelope (`sdd-forge flow get next-action`) または `GetNextActionCommand.execute` を経由する
- implement done → next-action.data.step が `review`
- review done → next-action.data.step が `gate-impl`
- gate-impl done → next-action.data.step が `finalize-commit`

## Implementation Notes
tests/unit/flow/get-next-action.test.js の既存パターン (createTmpDir + setupActiveFlow + makeFlowManager + runCli) を参考にする。spec ローカルに配置することで spec test coverage guardrail を満たす。

## Test Strategy
unit (CLI envelope レベルで遷移 assertion)。

---
Status: pending | Parent: (root) | Added Round: 0
