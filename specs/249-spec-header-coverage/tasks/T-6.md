# T-6: Annotate testable: false in AI prompt builders

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
run-retro.js の requirementsAsText (line 25) と review.js の extractRequirements (line 665) を更新し、testable === false の要件出力末尾に ` (testing not required)` 注釈を付ける。

## Acceptance Criteria
- requirementsAsText の出力で testable: false 要件が ` (testing not required)` 付きで含まれる
- extractRequirements も同様
- testable: true / 未設定 の要件は注釈なし (現行と同じ出力)

## Implementation Notes
両関数は `${r.id}${priority}: ${r.desc}` の形式で出力している。testable === false の場合に末尾追加。

## Test Strategy
tests/unit/ で両関数のユニットテスト (testable 各値での出力)。

---
Status: pending | Parent: (root) | Added Round: 0
