# T-1: Prepare plugin workspace

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Create or attach an in-flow copy/worktree of the external workflow plugin repository so plugin-side edits and verification remain inside the active flow boundary.

## Acceptance Criteria
- R1 and AC1 are satisfied before plugin-side files are edited.
- The in-flow plugin workspace records its source commit or branch for later application to the external repository.
- No direct edits are made to /home/nakano/workspace/senti-workflow-plugin while the core flow is active.

## Implementation Notes
Use a path under the active flow worktree. Keep generated or temporary plugin workspace paths out of src/ package code.

## Test Strategy
Spec-local verification checks the recorded plugin workspace path is under the active flow worktree and that plugin-side implementation evidence comes from that path.

---
Status: pending | Parent: (root) | Added Round: 0
