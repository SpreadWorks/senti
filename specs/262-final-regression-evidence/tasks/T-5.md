# T-5: Update guidance outputs

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Align prompts, report summaries, and durable artifact guidance with the renamed failureKind and attempt-log contract.

## Acceptance Criteria
- final-regression prompt describes `unattributed_existing_failure` and no longer lists `pre_existing` as active.
- Report summaries do not show past failureKind from pass artifacts.
- Durable artifact guidance retains or validates `final-regression-attempt-*.log` paths.

## Implementation Notes
If template files change, run `sdd-forge upgrade` and include generated skill updates when produced.

## Test Strategy
Static/unit tests verify relevant guidance strings and report output expectations where existing patterns support it.

---
Status: pending | Parent: (root) | Added Round: 0
