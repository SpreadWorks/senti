# T-1: Relocate workflow source to src/workflow/

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
experimental/workflow.js とその依存一式を src/workflow/ へ移設し、import パスを src/ 構造に合わせて調整する。

## Acceptance Criteria
- src/workflow/ にディスパッチャ・registry・lib・lib/commands が存在する
- 移設ファイルの相対 import が src/lib/ 等を正しく解決する

## Implementation Notes
現状 dispatcher は ../src/lib/cli.js 等を import。src/workflow/ 配下では ../lib/cli.js 等に調整。ディスパッチャの配置（src/workflow.js か src/workflow/index.js）は NAMESPACE_SCRIPTS の解決規約（PKG_DIR/<name>.js）に合わせる。

## Test Strategy
unit: 移設後の dispatcher が registry を解決しコマンドを実行できる（既存 workflow-dispatcher.test.js を移設）。

---
Status: pending | Parent: (root) | Added Round: 0
