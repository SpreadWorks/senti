# T-4: Snapshot flow hooks

<!-- Auto-generated by `senti spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Persist hook plans into flow.json during prepare and execute snapshot hooks through the flow command lifecycle.

## Acceptance Criteria
- R4 and R5 acceptance are covered for prepare.post order and later command hook execution.
- Snapshot entries contain no absolute paths.
- Missing or disabled snapshot plugins fail with restore/enable guidance.

## Implementation Notes
prepare.post order is command body, existing prepare.post, discovery/validation, snapshot write, snapshot prepare.post plugin run. Later lifecycle points read only the snapshot.

## Test Strategy
Spec-local flow prepare and lifecycle tests using temporary plugin fixtures and flow.json assertions.

---
Status: pending | Parent: (root) | Added Round: 0
