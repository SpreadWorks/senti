# T-7: Refresh prompts and fixtures

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Update prompts, schemas, fixtures, and snapshots so the full flow describes and verifies the v2 project regression contract.

## Acceptance Criteria
- test-execute and test-result-review prompts describe the v2 artifact and deterministic runner ownership boundaries.
- test-execute prompt and system prompt use the R4 discovery order and exclude README/Python auto-guessing/commands.test discovery.
- implement.md no longer tells test-only specs to skip the flow-level gate-impl regression gate.
- Fixtures and snapshots no longer expect v1 raw_output_lines arrays.
- Existing unit/e2e tests that create v1 artifacts are updated.
- next-action test-execute schema remains consistent with the new persisted artifact contract and does not describe obsolete discovery behavior.
- Spec-local tests live under specs/257-project-level-test-breakage-detection/tests/ with spec headers.

## Implementation Notes
Search all readers and writers of test-execute-result.json and test-result-review.json and update every in-scope consumer in the same issue.

## Test Strategy
Run the new spec-local deterministic tests and the existing unit/e2e tests affected by artifact, prompt, report, retro, config, and gate changes.

---
Status: pending | Parent: (root) | Added Round: 0
