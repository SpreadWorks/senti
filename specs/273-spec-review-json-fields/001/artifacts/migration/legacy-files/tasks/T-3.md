# T-3: Cover spec review recovery

<!-- Auto-generated by `sdd-forge spec render` from spec.json.tasks[]. Do not edit manually. -->

## Goal
Add spec-local and unit regression tests that prove the prompt contract, missing-array normalization, schema-repair retry, rejection behavior, and artifact compatibility.

## Acceptance Criteria
- R6 spec-local tests contain a `// spec: R1 R2 R3 R4 R5 R6` style header mapping requirements to assertions.
- Existing review command unit tests still pass.
- Project regression includes the changed unit test scope.

## Implementation Notes
Prefer direct helper-level tests when existing exports allow it; otherwise extend local exports in the same pattern used by the current review command tests.

## Test Strategy
Run the spec-local test file, the relevant unit review test file, and the project regression command required by the flow.

---
Status: pending | Parent: (root) | Added Round: 0
